# Sprint Plan: Follow-up: 1 unmet criteria

**Sprint ID:** ITR-1779130310081-followup-1
**Spec:** [2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md](docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md)
**Confidence:** 0.9

## Context

G6 follow-up: previous attempt completed all structural changes (error-pages.ts literals, login flow redirects, recovery_summary in setup 403) but had 27 test failures. Commit 1ca5bf3 (add-setup-window-ms-import-to-error-pages) resolved those failures — 272/272 tests now pass. This sprint confirms that state is stable.

## Risk flags

- RF1: KV expirationTtl in checkAndIncrement (src/abuse/rate-limit.ts:52) uses Math.max(1, ...) which can produce a value below Miniflare's 60-second minimum when tests run within 60s of an hour boundary — tests were passing at time of planning but could intermittently fail; fix is to change Math.max(1, ...) to Math.max(60, ...) if failures occur.

## Task T-1: Verify full test suite passes

**Type:** infrastructure (modelTier: 1)

### Step 1

Run `pnpm test` in the workspace root and confirm all test files pass with zero failures. Expected output: 272 tests passed across 35 test files. If any test fails, read the failure message — the most likely cause is a KV expirationTtl value below Miniflare's 60-second minimum in src/abuse/rate-limit.ts (the checkAndIncrement function computes TTL as seconds until the next hour boundary; if the test runs within 60 seconds of an hour boundary, the computed TTL can be less than 60). In that case, fix src/abuse/rate-limit.ts line 52 to use `Math.max(60, Math.ceil(msUntilNextHour / 1000))` instead of `Math.max(1, ...)`, then re-run the test suite.

**File reference:** src/abuse/rate-limit.ts
**Verification:** Exit code 0 and output contains '272 passed' (or higher if new tests were added).

### Task verification

**Structural:**
- `grep -n 'dash.cloudflare.com' src/views/error-pages.ts` (ws: primary)
- `grep -n 'setup_window_start' src/views/error-pages.ts` (ws: primary)
- `grep -n 'recovery_summary' src/routes/setup.ts` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779130310081-followup-1",
  "sprintName": "Follow-up: 1 unmet criteria",
  "specPath": "docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md",
  "phaseContext": "G6 follow-up: previous attempt completed all structural changes (error-pages.ts literals, login flow redirects, recovery_summary in setup 403) but had 27 test failures. Commit 1ca5bf3 (add-setup-window-ms-import-to-error-pages) resolved those failures — 272/272 tests now pass. This sprint confirms that state is stable.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Verify full test suite passes",
      "type": "infrastructure",
      "modelTier": 1,
      "files": {
        "modify": [],
        "create": []
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run `pnpm test` in the workspace root and confirm all test files pass with zero failures. Expected output: 272 tests passed across 35 test files. If any test fails, read the failure message — the most likely cause is a KV expirationTtl value below Miniflare's 60-second minimum in src/abuse/rate-limit.ts (the checkAndIncrement function computes TTL as seconds until the next hour boundary; if the test runs within 60 seconds of an hour boundary, the computed TTL can be less than 60). In that case, fix src/abuse/rate-limit.ts line 52 to use `Math.max(60, Math.ceil(msUntilNextHour / 1000))` instead of `Math.max(1, ...)`, then re-run the test suite.",
          "fileReference": "src/abuse/rate-limit.ts",
          "verification": "Exit code 0 and output contains '272 passed' (or higher if new tests were added)."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -n 'dash.cloudflare.com' src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -n 'setup_window_start' src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -n 'recovery_summary' src/routes/setup.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: KV expirationTtl in checkAndIncrement (src/abuse/rate-limit.ts:52) uses Math.max(1, ...) which can produce a value below Miniflare's 60-second minimum when tests run within 60s of an hour boundary — tests were passing at time of planning but could intermittently fail; fix is to change Math.max(1, ...) to Math.max(60, ...) if failures occur."
  ],
  "confidence": 0.9
}
```
