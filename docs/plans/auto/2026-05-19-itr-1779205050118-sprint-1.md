# Sprint Plan: Stale Assertion Cleanup

**Sprint ID:** ITR-1779205050118-sprint-1
**Spec:** [2026-05-19-final-checkpoint-human-acceptance-of-v2-migration-remediation-2.md](docs/specs/auto/2026-05-19-final-checkpoint-human-acceptance-of-v2-migration-remediation-2.md)
**Confidence:** 0.95

## Context

G9-remediation-2: mechanical test assertion cleanup following acceptance of all 10 G9-remediation-1 view-layer fixes. The /login?next= redirect contract was established in G4-round-3; 3 test files still assert the old /admin/login path.

## Risk flags

- RF1: src/__tests__/admin/admin.test.ts lines 229-230 already contain toContain("next=") and toContain("%2Fadmin") — after FIX 3b adds toContain("/login?next=") the test will have three assertions; executor must verify adjacent assertions are not accidentally duplicated or removed.

## Task T-1: Write skill-load receipt with updated iteration_id and emit skill_loaded events

**Type:** code (modelTier: 2)

**Files to modify:**
- .strategos/skill-load-receipt.json (workspace: primary)

### Step 1

Emit [STRATEGOS-LOG] {"event":"skill_loaded","data":{"skill":"ui-ux-pro-max:ui-ux-pro-max"}} to stdout.

**Verification:** Log line appears in output.

### Step 2

Emit [STRATEGOS-LOG] {"event":"skill_loaded","data":{"skill":"frontend-design:frontend-design"}} to stdout.

**Verification:** Log line appears in output.

### Step 3

Overwrite .strategos/skill-load-receipt.json with updated iteration_id. The file currently contains iteration_id "ITR-1779199973900". Replace with the current iteration_id "ITR-1779205050118". Keep skills_invoked array and update invoked_at to current ISO-8601 timestamp. Final content: {"skills_invoked":["ui-ux-pro-max:ui-ux-pro-max","frontend-design:frontend-design"],"invoked_at":"<ISO8601-now>","iteration_id":"ITR-1779205050118"}

**File reference:** .strategos/skill-load-receipt.json
**Verification:** grep "ITR-1779205050118" .strategos/skill-load-receipt.json exits 0.

### Step 4

Stage and commit the receipt: git add .strategos/skill-load-receipt.json && git commit -m "chore: write skill-load receipt for ui-ux-pro-max + frontend-design [skip ci]"

**Verification:** git log --oneline -1 shows the chore commit on the current branch.

### Task verification

**Structural:**
- `fs:exists .strategos/skill-load-receipt.json` (ws: primary)
- `fs:contains .strategos/skill-load-receipt.json "ITR-1779205050118"` (ws: primary)
- `fs:contains .strategos/skill-load-receipt.json "ui-ux-pro-max:ui-ux-pro-max"` (ws: primary)
- `fs:contains .strategos/skill-load-receipt.json "frontend-design:frontend-design"` (ws: primary)

**Functional:**
- `git log --oneline -1 | grep -q 'skill-load-receipt'` (ws: primary)

## Task T-2: Update 3 stale /admin/login assertions to match /login?next= redirect contract

**Type:** code (modelTier: 2)

**Depends on:** T-1

**Files to modify:**
- src/__tests__/integration/setup-without-cf.test.ts (workspace: primary)
- src/test/admin.test.ts (workspace: primary)
- src/__tests__/admin/admin.test.ts (workspace: primary)

### Step 1

In src/__tests__/integration/setup-without-cf.test.ts at line 212, replace the single assertion:
  expect(adminRes.headers.get("location")).toContain("/admin/login");
with two assertions:
  expect(adminRes.headers.get("location")).toContain("/login?next=");
  expect(adminRes.headers.get("location")).toContain("%2Fadmin");
Do NOT change any other lines in this file.

**File reference:** src/__tests__/integration/setup-without-cf.test.ts
**Verification:** grep -n '/admin/login' src/__tests__/integration/setup-without-cf.test.ts returns no output (exit 1 from grep, meaning no match).

### Step 2

In src/test/admin.test.ts at line 178, replace the single assertion:
  expect(res.headers.get("location")).toContain("/admin/login");
with two assertions:
  expect(res.headers.get("location")).toContain("/login?next=");
  expect(res.headers.get("location")).toContain("%2Fadmin");
Do NOT change any other lines in this file.

**File reference:** src/test/admin.test.ts
**Verification:** grep -n '/admin/login' src/test/admin.test.ts returns no output.

### Step 3

In src/__tests__/admin/admin.test.ts:
(a) At line ~221, update the test title from:
  it("(e) GET /admin with no session cookie returns 303 redirect to /admin/login?next=%2Fadmin", ...
to:
  it("(e) GET /admin with no session cookie returns 303 redirect to /login?next=%2Fadmin", ...

(b) At line ~228, replace the assertion:
  expect(location).toContain("/admin/login");
with:
  expect(location).toContain("/login?next=");

Keep the adjacent assertions at lines 229-230 (toContain("next=") and toContain("%2Fadmin")) UNCHANGED. Do NOT touch any other lines.

**File reference:** src/__tests__/admin/admin.test.ts
**Verification:** grep -n '/admin/login' src/__tests__/admin/admin.test.ts returns no output.

### Step 4

Run pnpm build to confirm no TypeScript errors were introduced by the assertion changes.

**Verification:** pnpm build exits 0.

### Step 5

Run the full test suite to confirm all 3 previously-failing tests now pass and no regressions were introduced.

**Verification:** pnpm test exits 0.

### Step 6

Run the login-flow integration test in isolation to confirm no regression on the G4-round-3 return-URL contract.

**Verification:** pnpm exec vitest run src/__tests__/integration/login-flow.test.ts exits 0.

### Step 7

Stage the 3 modified test files and commit: git add src/__tests__/integration/setup-without-cf.test.ts src/test/admin.test.ts src/__tests__/admin/admin.test.ts && git commit -m "fix(tests): update stale /admin/login assertions to match /login?next= redirect contract"

**Verification:** git log --oneline -1 shows the fix commit; git diff HEAD shows no unstaged changes in the 3 test files.

### Step 8

Push the branch to origin: git push origin HEAD

**Verification:** git push exits 0.

### Task verification

**Structural:**
- `grep -c '/admin/login' src/__tests__/integration/setup-without-cf.test.ts src/test/admin.test.ts src/__tests__/admin/admin.test.ts | grep -v ':0' | wc -l | grep -q '^0$'` (ws: primary)
- `fs:contains src/__tests__/integration/setup-without-cf.test.ts "/login?next="` (ws: primary)
- `fs:contains src/test/admin.test.ts "/login?next="` (ws: primary)
- `fs:contains src/__tests__/admin/admin.test.ts "/login?next="` (ws: primary)

**Functional:**
- `pnpm build` (ws: primary)
- `pnpm test` (ws: primary)
- `pnpm exec vitest run src/__tests__/integration/setup-without-cf.test.ts` (ws: primary)
- `pnpm exec vitest run src/__tests__/integration/login-flow.test.ts` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779205050118-sprint-1",
  "sprintName": "Stale Assertion Cleanup",
  "specPath": "docs/specs/auto/2026-05-19-final-checkpoint-human-acceptance-of-v2-migration-remediation-2.md",
  "phaseContext": "G9-remediation-2: mechanical test assertion cleanup following acceptance of all 10 G9-remediation-1 view-layer fixes. The /login?next= redirect contract was established in G4-round-3; 3 test files still assert the old /admin/login path.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Write skill-load receipt with updated iteration_id and emit skill_loaded events",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": ".strategos/skill-load-receipt.json"
          }
        ],
        "create": []
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Emit [STRATEGOS-LOG] {\"event\":\"skill_loaded\",\"data\":{\"skill\":\"ui-ux-pro-max:ui-ux-pro-max\"}} to stdout.",
          "verification": "Log line appears in output."
        },
        {
          "stepNumber": 2,
          "instruction": "Emit [STRATEGOS-LOG] {\"event\":\"skill_loaded\",\"data\":{\"skill\":\"frontend-design:frontend-design\"}} to stdout.",
          "verification": "Log line appears in output."
        },
        {
          "stepNumber": 3,
          "instruction": "Overwrite .strategos/skill-load-receipt.json with updated iteration_id. The file currently contains iteration_id \"ITR-1779199973900\". Replace with the current iteration_id \"ITR-1779205050118\". Keep skills_invoked array and update invoked_at to current ISO-8601 timestamp. Final content: {\"skills_invoked\":[\"ui-ux-pro-max:ui-ux-pro-max\",\"frontend-design:frontend-design\"],\"invoked_at\":\"<ISO8601-now>\",\"iteration_id\":\"ITR-1779205050118\"}",
          "fileReference": ".strategos/skill-load-receipt.json",
          "verification": "grep \"ITR-1779205050118\" .strategos/skill-load-receipt.json exits 0."
        },
        {
          "stepNumber": 4,
          "instruction": "Stage and commit the receipt: git add .strategos/skill-load-receipt.json && git commit -m \"chore: write skill-load receipt for ui-ux-pro-max + frontend-design [skip ci]\"",
          "verification": "git log --oneline -1 shows the chore commit on the current branch."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "fs:exists .strategos/skill-load-receipt.json"
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains .strategos/skill-load-receipt.json \"ITR-1779205050118\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains .strategos/skill-load-receipt.json \"ui-ux-pro-max:ui-ux-pro-max\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains .strategos/skill-load-receipt.json \"frontend-design:frontend-design\""
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "git log --oneline -1 | grep -q 'skill-load-receipt'"
          }
        ]
      }
    },
    {
      "id": "T-2",
      "title": "Update 3 stale /admin/login assertions to match /login?next= redirect contract",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/__tests__/integration/setup-without-cf.test.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/test/admin.test.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/__tests__/admin/admin.test.ts"
          }
        ],
        "create": []
      },
      "dependencies": [
        "T-1"
      ],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "In src/__tests__/integration/setup-without-cf.test.ts at line 212, replace the single assertion:\n  expect(adminRes.headers.get(\"location\")).toContain(\"/admin/login\");\nwith two assertions:\n  expect(adminRes.headers.get(\"location\")).toContain(\"/login?next=\");\n  expect(adminRes.headers.get(\"location\")).toContain(\"%2Fadmin\");\nDo NOT change any other lines in this file.",
          "fileReference": "src/__tests__/integration/setup-without-cf.test.ts",
          "verification": "grep -n '/admin/login' src/__tests__/integration/setup-without-cf.test.ts returns no output (exit 1 from grep, meaning no match)."
        },
        {
          "stepNumber": 2,
          "instruction": "In src/test/admin.test.ts at line 178, replace the single assertion:\n  expect(res.headers.get(\"location\")).toContain(\"/admin/login\");\nwith two assertions:\n  expect(res.headers.get(\"location\")).toContain(\"/login?next=\");\n  expect(res.headers.get(\"location\")).toContain(\"%2Fadmin\");\nDo NOT change any other lines in this file.",
          "fileReference": "src/test/admin.test.ts",
          "verification": "grep -n '/admin/login' src/test/admin.test.ts returns no output."
        },
        {
          "stepNumber": 3,
          "instruction": "In src/__tests__/admin/admin.test.ts:\n(a) At line ~221, update the test title from:\n  it(\"(e) GET /admin with no session cookie returns 303 redirect to /admin/login?next=%2Fadmin\", ...\nto:\n  it(\"(e) GET /admin with no session cookie returns 303 redirect to /login?next=%2Fadmin\", ...\n\n(b) At line ~228, replace the assertion:\n  expect(location).toContain(\"/admin/login\");\nwith:\n  expect(location).toContain(\"/login?next=\");\n\nKeep the adjacent assertions at lines 229-230 (toContain(\"next=\") and toContain(\"%2Fadmin\")) UNCHANGED. Do NOT touch any other lines.",
          "fileReference": "src/__tests__/admin/admin.test.ts",
          "verification": "grep -n '/admin/login' src/__tests__/admin/admin.test.ts returns no output."
        },
        {
          "stepNumber": 4,
          "instruction": "Run pnpm build to confirm no TypeScript errors were introduced by the assertion changes.",
          "verification": "pnpm build exits 0."
        },
        {
          "stepNumber": 5,
          "instruction": "Run the full test suite to confirm all 3 previously-failing tests now pass and no regressions were introduced.",
          "verification": "pnpm test exits 0."
        },
        {
          "stepNumber": 6,
          "instruction": "Run the login-flow integration test in isolation to confirm no regression on the G4-round-3 return-URL contract.",
          "verification": "pnpm exec vitest run src/__tests__/integration/login-flow.test.ts exits 0."
        },
        {
          "stepNumber": 7,
          "instruction": "Stage the 3 modified test files and commit: git add src/__tests__/integration/setup-without-cf.test.ts src/test/admin.test.ts src/__tests__/admin/admin.test.ts && git commit -m \"fix(tests): update stale /admin/login assertions to match /login?next= redirect contract\"",
          "verification": "git log --oneline -1 shows the fix commit; git diff HEAD shows no unstaged changes in the 3 test files."
        },
        {
          "stepNumber": 8,
          "instruction": "Push the branch to origin: git push origin HEAD",
          "verification": "git push exits 0."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -c '/admin/login' src/__tests__/integration/setup-without-cf.test.ts src/test/admin.test.ts src/__tests__/admin/admin.test.ts | grep -v ':0' | wc -l | grep -q '^0$'"
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains src/__tests__/integration/setup-without-cf.test.ts \"/login?next=\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains src/test/admin.test.ts \"/login?next=\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains src/__tests__/admin/admin.test.ts \"/login?next=\""
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm exec vitest run src/__tests__/integration/setup-without-cf.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm exec vitest run src/__tests__/integration/login-flow.test.ts"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: src/__tests__/admin/admin.test.ts lines 229-230 already contain toContain(\"next=\") and toContain(\"%2Fadmin\") — after FIX 3b adds toContain(\"/login?next=\") the test will have three assertions; executor must verify adjacent assertions are not accidentally duplicated or removed."
  ],
  "confidence": 0.95
}
```
