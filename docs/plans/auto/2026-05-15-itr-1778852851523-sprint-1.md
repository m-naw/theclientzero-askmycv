{"sprintId":"ITR-1778852851523-sprint-1","sprintName":"Body-Shape Assertion + Final Verification","specPath":"docs/specs/auto/2026-05-15-auth-setup-state-machine-behavior-f2-f3.md","phaseContext":"G4 final sprint — all routing, JWT auth, state machine, and view rendering are already implemented. This sprint closes the single remaining gap: asserting the Anthropic outgoing request body shape in the key-rejection acceptance test, then verifying the full suite (pnpm build/typecheck/test/lint) passes clean.","tasks":[{"id":"T-1","title":"Add Anthropic outgoing body-shape assertion to Test 6 and commit","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/__tests__/setup/setup-acceptance.test.ts"}],"create":[]},"artifact_refs":[],"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Read src/__tests__/setup/setup-acceptance.test.ts lines 318–344 (Test 6: 'returns 400 when Anthropic rejects the key'). Confirm the current interceptor uses the positional-args form: .reply(401, JSON.stringify({...}), { headers: {...} }). This is the code to be replaced.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"Lines 333–338 show pool.intercept(...).reply(401, JSON.stringify({...}), { headers: {...} }) with no callback."},{"stepNumber":2,"instruction":"Before modifying the test, resolve the undici reply-callback signature. Run: node -e \"const u = require('undici'); console.log(u.version || 'no version export')\" and also check node_modules/undici/package.json for the 'version' field. If undici >= 5.x is present, the MockAgent reply() accepts a callback (opts) => ({ statusCode, data, responseOptions }) per the undici docs. If the version is not resolvable this way, check node_modules/@cloudflare/vitest-pool-workers to find which undici it bundles. Record the resolved version in a code comment ONLY if it represents a non-obvious constraint (per project comments policy: only write comments when the WHY is non-obvious).","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"Undici version is known. The reply callback signature is confirmed: (opts: { body: string | Buffer | null }) => { statusCode: number; data: unknown; responseOptions?: { headers: Record<string,string> } }."},{"stepNumber":3,"instruction":"In Test 6 ('returns 400 when Anthropic rejects the key', lines 318–344), replace the plain reply() call with a callback form that captures the request body. Specifically:\n\n(a) Declare 'let capturedBody: unknown;' immediately before the pool.intercept(...).reply(...) call.\n\n(b) Replace the current .reply(401, JSON.stringify({...}), { headers: {...} }) with:\n  .reply((opts) => {\n    capturedBody = JSON.parse(opts.body as string);\n    return {\n      statusCode: 401,\n      data: { error: { type: 'authentication_error' } },\n      responseOptions: { headers: { 'content-type': 'application/json' } },\n    };\n  });\n\nIf the installed undici version does NOT support the callback form for reply(), use the alternative: intercept the request via pool.intercept({...}).replyWithError is NOT the right path; instead keep the positional reply() but hook body capture via pool.intercept({...}).on('request', (req) => { capturedBody = req.body; }) BEFORE the .reply() chain — only if the on('request') API is available. If neither works, fall back to capturing via a Request clone inside the Worker handler (but this violates the accept-from-mock strategy); prefer mock-side capture.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"src/__tests__/setup/setup-acceptance.test.ts contains 'let capturedBody: unknown' and a reply() callback that assigns capturedBody inside Test 6."},{"stepNumber":4,"instruction":"After the 'await runFetch(setupRequest(jwt, validFormBody()))' call in Test 6 and after 'expect(res.status).toBe(400)', add the body-shape assertion:\n\n  expect(capturedBody).toMatchObject({\n    model: expect.any(String),\n    system: expect.any(String),\n    messages: expect.any(Array),\n    max_tokens: expect.any(Number),\n  });\n\nKeep the existing assertions (json.field === 'anthropic_api_key', config is null) intact.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"src/__tests__/setup/setup-acceptance.test.ts contains 'toMatchObject' and all four keys (model, system, messages, max_tokens) inside Test 6."},{"stepNumber":5,"instruction":"Stage ALL currently modified/unstaged content in src/__tests__/setup/setup-acceptance.test.ts (this includes the pre-existing unstaged daily_budget_usd validation tests AND the new body-shape assertion changes). Commit with message: 'test: add Anthropic body-shape assertion and daily_budget_usd validation tests'. Do NOT amend any prior commit.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"git status shows no unstaged changes to src/__tests__/setup/setup-acceptance.test.ts. git log --oneline -1 shows the new commit message."}],"verification":{"structural":[{"workspaceId":"primary","command":"grep -n 'capturedBody' src/__tests__/setup/setup-acceptance.test.ts"},{"workspaceId":"primary","command":"grep -n 'toMatchObject' src/__tests__/setup/setup-acceptance.test.ts"},{"workspaceId":"primary","command":"grep -n 'max_tokens' src/__tests__/setup/setup-acceptance.test.ts"},{"workspaceId":"primary","command":"grep -n 'daily_budget_usd' src/__tests__/setup/setup-acceptance.test.ts"}],"functional":[{"workspaceId":"primary","command":"pnpm vitest run src/__tests__/setup/setup-acceptance.test.ts --reporter=verbose 2>&1 | tail -30"}]}},{"id":"T-2","title":"Run full verification suite and fix any failures","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/__tests__/setup/setup-acceptance.test.ts"}],"create":[]},"artifact_refs":[],"dependencies":["T-1"],"steps":[{"stepNumber":1,"instruction":"Run pnpm build. If it exits 0, proceed. If it fails, read the error output, identify the failing file and error, then fix it. Common causes: TypeScript compilation errors in test files (e.g., 'capturedBody' typed incorrectly, undici reply callback type mismatch). Fix only what is broken — do not refactor surrounding code.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"pnpm build exits 0 with no errors."},{"stepNumber":2,"instruction":"Run pnpm typecheck. If it exits 0, proceed. If it fails, read the diagnostic lines, identify the type error (likely 'opts.body' type or 'capturedBody' usage), and apply a minimal type assertion or annotation to resolve it. Do not change logic — only fix types.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"pnpm typecheck exits 0 with no errors."},{"stepNumber":3,"instruction":"Run pnpm lint. If it exits 0, proceed. If it fails, read the lint errors, identify the rule violation (common: no-explicit-any on capturedBody, async-await lint rule), and fix minimally. Do not silence rules with eslint-disable unless there is no type-correct alternative.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"pnpm lint exits 0 with no errors."},{"stepNumber":4,"instruction":"Run pnpm test and verify: (a) exit code is 0, (b) total passing tests reported in the workers pool is >= 80. To count: look for the summary line '✓ X tests passed' or equivalent from vitest. If test count is < 80, do NOT invent new tests — flag this as a discrepancy and report the actual count. If any test is failing (not just the new assertion), diagnose before fixing: confirm whether the failure is in the new capturedBody assertion (mock API mismatch) or a pre-existing test. For mock API mismatch: try the alternative reply() approach from T-1 step 3.","fileReference":"src/__tests__/setup/setup-acceptance.test.ts","verification":"pnpm test exits 0. Output contains a passing count >= 80 in the workers pool summary line."},{"stepNumber":5,"instruction":"If any fixes were applied in steps 1–4, commit the fix changes with a message: 'fix: resolve build/typecheck/lint failures from body-shape assertion'. If no fixes were needed (all commands already exit 0), skip this step.","verification":"git status is clean (no unstaged or uncommitted changes to tracked files)."}],"verification":{"structural":[{"workspaceId":"primary","command":"git status --short"}],"functional":[{"workspaceId":"primary","command":"pnpm build 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm typecheck 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm lint 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm test 2>&1 | tail -20"}]}}],"riskFlags":["RF1: undici MockAgent reply() callback signature — the @cloudflare/vitest-pool-workers fetchMock wraps undici; the callback form (opts) => { statusCode, data } is documented for undici 5.x+ but the exact shape (responseOptions vs third positional arg) must be confirmed against the installed version. If the callback form is unsupported, the implementer must use the on('request') event-based body capture instead. The spec explicitly names this caveat."],"confidence":0.9}