# Sprint Plan: Final Verification Sweep

**Sprint ID:** ITR-1779074505515-sprint-1
**Spec:** [2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md](docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md)
**Confidence:** 0.87

## Context

G3 goal closure sprint — all 26 structural criteria confirmed MET via direct file inspection; this sprint runs the four verification commands (pnpm test, pnpm build, pnpm typecheck, node scripts/check-contract-parity.mjs) to confirm no regressions before closing the goal. Minimal fixes only if a specific command reports a failure.

## Risk flags

- RF1: pnpm build or pnpm typecheck may fail due to a type or syntax error introduced in recent commits (81be9f4, d995c37) — if either fails, executor must read the specific error output and apply the minimal targeted fix without touching unrelated code

## Task T-1: Run final verification sweep and apply minimal fixes if needed

**Type:** code (modelTier: 2)

### Step 1

Run `pnpm test` and capture output. Expect: exit 0 with exactly 240 tests passing across 32 files. If the count is wrong or any test fails, read the failure output carefully and apply the minimal source fix — do NOT modify tests speculatively.

**File reference:** src/test/admin.test.ts
**Verification:** pnpm test exits 0; output contains '240' and no 'failed'

### Step 2

Run `pnpm build` and capture output. Expect: exit 0. The build script is `wrangler deploy --dry-run`. If it fails, the error message will identify the offending file and line — read and fix only that specific error.

**File reference:** src/worker.ts
**Verification:** pnpm build exits 0

### Step 3

Run `pnpm typecheck` and capture output. Expect: exit 0. The typecheck script is `tsc --noEmit`. If TypeScript reports errors, fix only the reported lines — do not introduce new types or refactor surrounding code.

**File reference:** src/types/env.ts
**Verification:** pnpm typecheck exits 0

### Step 4

Run `node scripts/check-contract-parity.mjs` and capture output. Expect: exit 0 with '[OK] all fields match'. If it exits 1 with a diff, the form HTML or route handler has a missing field — add ONLY the reported missing field to reconcile the diff.

**File reference:** scripts/check-contract-parity.mjs
**Verification:** node scripts/check-contract-parity.mjs exits 0

### Step 5

Confirm all 26 structural criteria via grep evidence. Run each command and verify it exits 0 or returns a match:

C1: grep '"bcryptjs"' package.json
C2: grep 'saltRounds = 12' src/auth/password.ts
C3: grep 'SHA-256' src/auth/session.ts
C4: grep 'setTimeout' src/routes/admin.ts
C5: grep 'admin_password_hash' src/types/auth.ts
C6: grep 'cookie_signing_secret' src/types/auth.ts
C7: grep 'askmycv_admin_session' src/auth/session.ts
C8: grep 'DELETE ALL CONFIG' src/test/admin.test.ts
C9: grep 'SETUP_WINDOW_MS = 600_000' src/state/machine.ts
C10: grep 'HttpOnly' src/auth/session.ts
C11: grep 'SameSite=Lax' src/auth/session.ts
C12: grep 'session cookie only' src/test/admin.test.ts
C13: test -f docs/specs/auth-surface.md && echo MET
C14: grep 'admin_password_hash' docs/specs/auth-surface.md
C15: grep 'cookie_signing_secret' docs/specs/auth-surface.md
C16: test -f scripts/check-contract-parity.mjs && echo MET
C17: grep 'Danger zone' src/views/admin-form.ts
C18: grep 'Reset all configuration' src/views/admin-form.ts
C19: grep 'public_url' src/routes/setup.ts
C20: grep 'admin_url' src/routes/setup.ts
C21: grep '5000' src/test/smoke.test.ts
C22: grep '50.000\|50_000' src/types/config.ts
C23: grep '100 \* 1024' src/routes/setup.ts && grep '413' src/routes/setup.ts
C24: grep -r 'console.log' src/ --include='*.ts' | grep -iv 'test' | wc -l (should be 0 secret-related)
C25: node scripts/check-contract-parity.mjs (already run in step 4)
C26: pnpm test (already run in step 1)

**File reference:** docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md
**Verification:** All 26 grep commands return matches or exit 0

### Task verification

**Structural:**
- `grep '"bcryptjs"' package.json` (ws: primary)
- `grep 'saltRounds = 12' src/auth/password.ts` (ws: primary)
- `grep 'SHA-256' src/auth/session.ts` (ws: primary)
- `grep 'setTimeout' src/routes/admin.ts` (ws: primary)
- `grep 'admin_password_hash' src/types/auth.ts` (ws: primary)
- `grep 'cookie_signing_secret' src/types/auth.ts` (ws: primary)
- `grep 'askmycv_admin_session' src/auth/session.ts` (ws: primary)
- `grep 'DELETE ALL CONFIG' src/test/admin.test.ts` (ws: primary)
- `grep 'SETUP_WINDOW_MS = 600_000' src/state/machine.ts` (ws: primary)
- `grep 'HttpOnly' src/auth/session.ts` (ws: primary)
- `grep 'SameSite=Lax' src/auth/session.ts` (ws: primary)
- `grep 'session cookie only' src/test/admin.test.ts` (ws: primary)
- `test -f docs/specs/auth-surface.md` (ws: primary)
- `grep 'admin_password_hash' docs/specs/auth-surface.md` (ws: primary)
- `grep 'cookie_signing_secret' docs/specs/auth-surface.md` (ws: primary)
- `test -f scripts/check-contract-parity.mjs` (ws: primary)
- `grep 'Danger zone' src/views/admin-form.ts` (ws: primary)
- `grep 'Reset all configuration' src/views/admin-form.ts` (ws: primary)
- `grep 'public_url' src/routes/setup.ts` (ws: primary)
- `grep 'admin_url' src/routes/setup.ts` (ws: primary)
- `grep '5000' src/test/smoke.test.ts` (ws: primary)
- `grep -E '50.000|50_000' src/types/config.ts` (ws: primary)
- `grep '100 \* 1024' src/routes/setup.ts` (ws: primary)
- `grep '413' src/routes/setup.ts` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)
- `pnpm build` (ws: primary)
- `pnpm typecheck` (ws: primary)
- `node scripts/check-contract-parity.mjs` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779074505515-sprint-1",
  "sprintName": "Final Verification Sweep",
  "specPath": "docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md",
  "phaseContext": "G3 goal closure sprint — all 26 structural criteria confirmed MET via direct file inspection; this sprint runs the four verification commands (pnpm test, pnpm build, pnpm typecheck, node scripts/check-contract-parity.mjs) to confirm no regressions before closing the goal. Minimal fixes only if a specific command reports a failure.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Run final verification sweep and apply minimal fixes if needed",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run `pnpm test` and capture output. Expect: exit 0 with exactly 240 tests passing across 32 files. If the count is wrong or any test fails, read the failure output carefully and apply the minimal source fix — do NOT modify tests speculatively.",
          "fileReference": "src/test/admin.test.ts",
          "verification": "pnpm test exits 0; output contains '240' and no 'failed'"
        },
        {
          "stepNumber": 2,
          "instruction": "Run `pnpm build` and capture output. Expect: exit 0. The build script is `wrangler deploy --dry-run`. If it fails, the error message will identify the offending file and line — read and fix only that specific error.",
          "fileReference": "src/worker.ts",
          "verification": "pnpm build exits 0"
        },
        {
          "stepNumber": 3,
          "instruction": "Run `pnpm typecheck` and capture output. Expect: exit 0. The typecheck script is `tsc --noEmit`. If TypeScript reports errors, fix only the reported lines — do not introduce new types or refactor surrounding code.",
          "fileReference": "src/types/env.ts",
          "verification": "pnpm typecheck exits 0"
        },
        {
          "stepNumber": 4,
          "instruction": "Run `node scripts/check-contract-parity.mjs` and capture output. Expect: exit 0 with '[OK] all fields match'. If it exits 1 with a diff, the form HTML or route handler has a missing field — add ONLY the reported missing field to reconcile the diff.",
          "fileReference": "scripts/check-contract-parity.mjs",
          "verification": "node scripts/check-contract-parity.mjs exits 0"
        },
        {
          "stepNumber": 5,
          "instruction": "Confirm all 26 structural criteria via grep evidence. Run each command and verify it exits 0 or returns a match:\n\nC1: grep '\"bcryptjs\"' package.json\nC2: grep 'saltRounds = 12' src/auth/password.ts\nC3: grep 'SHA-256' src/auth/session.ts\nC4: grep 'setTimeout' src/routes/admin.ts\nC5: grep 'admin_password_hash' src/types/auth.ts\nC6: grep 'cookie_signing_secret' src/types/auth.ts\nC7: grep 'askmycv_admin_session' src/auth/session.ts\nC8: grep 'DELETE ALL CONFIG' src/test/admin.test.ts\nC9: grep 'SETUP_WINDOW_MS = 600_000' src/state/machine.ts\nC10: grep 'HttpOnly' src/auth/session.ts\nC11: grep 'SameSite=Lax' src/auth/session.ts\nC12: grep 'session cookie only' src/test/admin.test.ts\nC13: test -f docs/specs/auth-surface.md && echo MET\nC14: grep 'admin_password_hash' docs/specs/auth-surface.md\nC15: grep 'cookie_signing_secret' docs/specs/auth-surface.md\nC16: test -f scripts/check-contract-parity.mjs && echo MET\nC17: grep 'Danger zone' src/views/admin-form.ts\nC18: grep 'Reset all configuration' src/views/admin-form.ts\nC19: grep 'public_url' src/routes/setup.ts\nC20: grep 'admin_url' src/routes/setup.ts\nC21: grep '5000' src/test/smoke.test.ts\nC22: grep '50.000\\|50_000' src/types/config.ts\nC23: grep '100 \\* 1024' src/routes/setup.ts && grep '413' src/routes/setup.ts\nC24: grep -r 'console.log' src/ --include='*.ts' | grep -iv 'test' | wc -l (should be 0 secret-related)\nC25: node scripts/check-contract-parity.mjs (already run in step 4)\nC26: pnpm test (already run in step 1)",
          "fileReference": "docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md",
          "verification": "All 26 grep commands return matches or exit 0"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep '\"bcryptjs\"' package.json"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'saltRounds = 12' src/auth/password.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'SHA-256' src/auth/session.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'setTimeout' src/routes/admin.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'admin_password_hash' src/types/auth.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'cookie_signing_secret' src/types/auth.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'askmycv_admin_session' src/auth/session.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'DELETE ALL CONFIG' src/test/admin.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'SETUP_WINDOW_MS = 600_000' src/state/machine.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'HttpOnly' src/auth/session.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'SameSite=Lax' src/auth/session.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'session cookie only' src/test/admin.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/specs/auth-surface.md"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'admin_password_hash' docs/specs/auth-surface.md"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'cookie_signing_secret' docs/specs/auth-surface.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f scripts/check-contract-parity.mjs"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'Danger zone' src/views/admin-form.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'Reset all configuration' src/views/admin-form.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'public_url' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'admin_url' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep '5000' src/test/smoke.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -E '50.000|50_000' src/types/config.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep '100 \\* 1024' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep '413' src/routes/setup.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm typecheck"
          },
          {
            "workspaceId": "primary",
            "command": "node scripts/check-contract-parity.mjs"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: pnpm build or pnpm typecheck may fail due to a type or syntax error introduced in recent commits (81be9f4, d995c37) — if either fails, executor must read the specific error output and apply the minimal targeted fix without touching unrelated code"
  ],
  "confidence": 0.87
}
```
