{"sprintId":"ITR-1778847016321-sprint-1","sprintName":"Auth Module & State Machine","specPath":"docs/specs/auto/2026-05-15-auth-setup-state-machine-behavior-f2-f3.md","phaseContext":"G4: Wire unconfigured→configured state machine — JWT auth module and KV-based state detection are the two foundational modules that all route logic (G4 routes, G5+ chat) builds on. This sprint delivers only the auth and state modules with unit tests; route wiring happens in the next sprint.","tasks":[{"id":"T-1","title":"JWT Authentication Module (src/auth/jwt.ts) + Unit Tests","type":"code","modelTier":3,"files":{"create":[{"workspaceId":"primary","path":"src/auth/jwt.ts"},{"workspaceId":"primary","path":"src/__tests__/state-machine/jwt.test.ts"}],"modify":[]},"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Create src/auth/jwt.ts. This module is the sole owner of JWT verification logic. It must never be used as a gate by itself — callers check the return value. Full implementation:\n\n```typescript\nimport { createRemoteJWKSet, jwtVerify } from \"jose\";\nimport type { Env } from \"../env\";\n\nexport interface JwtClaims {\n  email: string;\n  aud: string | string[];\n  iss: string;\n  exp: number;\n}\n\nconst ISS_RE = /^\\/\\/https:\\/\\/[^.]+\\.cloudflareaccess\\.com$/;\n\n// Validates iss matches *.cloudflareaccess.com\nfunction validIss(iss: unknown): iss is string {\n  return typeof iss === \"string\" && /^https:\\/\\/[^.]+\\.cloudflareaccess\\.com$/.test(iss);\n}\n\nexport async function verifyAccessJwt(\n  request: Request,\n  env: Env,\n): Promise<JwtClaims | null> {\n  const token = request.headers.get(\"cf-access-jwt-assertion\");\n  if (!token) return null;\n\n  let jwksUrl: string;\n  if (env.ACCESS_JWKS_URL_OVERRIDE) {\n    // Test-harness injection: skip iss-based derivation.\n    jwksUrl = env.ACCESS_JWKS_URL_OVERRIDE;\n  } else {\n    // Decode iss from unverified payload to derive the Cloudflare JWKS endpoint.\n    // Cryptographic binding happens in jwtVerify below; this only determines\n    // which team's JWKS to fetch.\n    try {\n      const [, b64] = token.split(\".\");\n      const decoded = JSON.parse(\n        atob(b64.replace(/-/g, \"+\").replace(/_/g, \"/\")),\n      ) as { iss?: unknown };\n      if (!validIss(decoded.iss)) return null;\n      jwksUrl = `${decoded.iss}/cdn-cgi/access/certs`;\n    } catch {\n      return null;\n    }\n  }\n\n  try {\n    // Per-call JWKS client — no in-memory key cache leaks across requests.\n    const jwks = createRemoteJWKSet(new URL(jwksUrl));\n    const { payload } = await jwtVerify(token, jwks);\n    // jose auto-validates exp; manual iss check is an additional guard.\n    if (!validIss(payload.iss)) return null;\n    const email = typeof payload.email === \"string\" ? payload.email : null;\n    if (!email) return null;\n    return {\n      email,\n      aud: payload.aud ?? \"\",\n      iss: payload.iss as string,\n      exp: payload.exp ?? 0,\n    };\n  } catch {\n    return null;\n  }\n}\n```","fileReference":"src/auth/jwt.ts","verification":"Read the file and confirm: exports `JwtClaims` interface, exports `verifyAccessJwt` async function, imports from `jose` and `../env`, uses `createRemoteJWKSet` per-call (inside the try block), validates iss with regex after jwtVerify, reads `cf-access-jwt-assertion` header (lowercase)."},{"stepNumber":2,"instruction":"Create src/__tests__/state-machine/jwt.test.ts. These are unit tests for verifyAccessJwt covering all sub-cases from spec §12 Test 4 (absent header, forged signature, wrong iss, expired token, valid token). Use the project's fetchMock + createJwksMock pattern from src/test/harness/jwks-mock.ts — the same pattern established in src/test/smoke.test.ts.\n\n```typescript\nimport { afterEach, beforeEach, describe, expect, it } from \"vitest\";\n// @ts-expect-error — provided by @cloudflare/vitest-pool-workers at runtime\nimport { env, fetchMock } from \"cloudflare:test\";\nimport { createJwksMock } from \"../../test/harness/jwks-mock\";\nimport { verifyAccessJwt } from \"../../auth/jwt\";\n\nconst MOCK_JWKS_URL = \"https://test.cloudflareaccess.com/cdn-cgi/access/certs\";\nconst VALID_ISS = \"https://test.cloudflareaccess.com\";\n\ndescribe(\"verifyAccessJwt\", () => {\n  let jwks: Awaited<ReturnType<typeof createJwksMock>>;\n\n  beforeEach(async () => {\n    jwks = await createJwksMock();\n    fetchMock.activate();\n    fetchMock.disableNetConnect();\n    (env as Record<string, string>).ACCESS_JWKS_URL_OVERRIDE = MOCK_JWKS_URL;\n    const jwksBody = JSON.stringify(await jwks.getJwks());\n    // Pre-register enough intercepts for all sub-cases in this suite.\n    const pool = fetchMock.get(\"https://test.cloudflareaccess.com\");\n    for (let i = 0; i < 8; i++) {\n      pool\n        .intercept({ path: \"/cdn-cgi/access/certs\", method: \"GET\" })\n        .reply(200, jwksBody, { headers: { \"content-type\": \"application/json\" } });\n    }\n  });\n\n  afterEach(() => {\n    fetchMock.deactivate();\n    (env as Record<string, string>).ACCESS_JWKS_URL_OVERRIDE = \"\";\n  });\n\n  it(\"returns JwtClaims for a valid JWKS-signed token with correct iss and future exp\", async () => {\n    const token = await jwks.issueJwt({\n      aud: \"aud-test-001\",\n      iss: VALID_ISS,\n      email: \"owner@example.com\",\n    });\n    const req = new Request(\"https://worker.test/\", {\n      headers: { \"cf-access-jwt-assertion\": token },\n    });\n    const claims = await verifyAccessJwt(req, env as never);\n    expect(claims).not.toBeNull();\n    expect(claims!.email).toBe(\"owner@example.com\");\n    expect(claims!.iss).toBe(VALID_ISS);\n    expect(claims!.aud).toBe(\"aud-test-001\");\n    expect(claims!.exp).toBeGreaterThan(Math.floor(Date.now() / 1000));\n  });\n\n  it(\"returns null when cf-access-jwt-assertion header is absent\", async () => {\n    const req = new Request(\"https://worker.test/\");\n    // No JWKS fetch happens — no intercept consumed.\n    const claims = await verifyAccessJwt(req, env as never);\n    expect(claims).toBeNull();\n  });\n\n  it(\"returns null for a forged token (wrong signature, spec §12 Test 4a)\", async () => {\n    const forged = await jwks.issueJwt({\n      aud: \"aud-test-001\",\n      iss: VALID_ISS,\n      email: \"attacker@evil.com\",\n      forge: true,\n    });\n    const req = new Request(\"https://worker.test/\", {\n      headers: { \"cf-access-jwt-assertion\": forged },\n    });\n    const claims = await verifyAccessJwt(req, env as never);\n    expect(claims).toBeNull();\n  });\n\n  it(\"returns null for an expired token (spec §12 Test 4c)\", async () => {\n    const expiredToken = await jwks.issueJwt({\n      aud: \"aud-test-001\",\n      iss: VALID_ISS,\n      email: \"owner@example.com\",\n      exp: Math.floor(Date.now() / 1000) - 60,\n    });\n    const req = new Request(\"https://worker.test/\", {\n      headers: { \"cf-access-jwt-assertion\": expiredToken },\n    });\n    const claims = await verifyAccessJwt(req, env as never);\n    expect(claims).toBeNull();\n  });\n\n  it(\"returns null when iss does not match *.cloudflareaccess.com (spec §12 Test 4b)\", async () => {\n    // Token is signed by the test key so signature passes;\n    // post-verify iss regex rejects it.\n    const wrongIssToken = await jwks.issueJwt({\n      aud: \"aud-test-001\",\n      iss: \"https://attacker.evil.com\",\n      email: \"attacker@evil.com\",\n    });\n    const req = new Request(\"https://worker.test/\", {\n      headers: { \"cf-access-jwt-assertion\": wrongIssToken },\n    });\n    const claims = await verifyAccessJwt(req, env as never);\n    expect(claims).toBeNull();\n  });\n});\n```","fileReference":"src/__tests__/state-machine/jwt.test.ts","verification":"File exists at src/__tests__/state-machine/jwt.test.ts. Contains 5 `it()` blocks. Imports from `../../auth/jwt` and `../../test/harness/jwks-mock`. Uses `fetchMock.activate()`/`fetchMock.deactivate()` in beforeEach/afterEach."},{"stepNumber":3,"instruction":"Run `pnpm typecheck` and confirm exit 0. If there are TypeScript errors in src/auth/jwt.ts (e.g., around `payload.aud` type from jose), fix them: jose's JWTPayload types aud as `string | string[] | undefined` — cast or add a null-coalescing fallback as shown in the implementation.","fileReference":"src/auth/jwt.ts","verification":"`pnpm typecheck` exits 0 with no errors mentioning src/auth/jwt.ts or src/__tests__/state-machine/jwt.test.ts."},{"stepNumber":4,"instruction":"Run `pnpm test` and confirm all jwt.test.ts cases pass. If the forged-token test fails with an unexpected pass (returns non-null claims instead of null), verify that the `forge: true` path in jwks-mock.ts uses `foreign.privateKey` — inspect src/test/harness/jwks-mock.ts line 59-60 to confirm. If the expired-token test fails because jose does not throw on expired tokens in the miniflare runtime, add `clockTolerance: 0` to jwtVerify options explicitly.","fileReference":"src/__tests__/state-machine/jwt.test.ts","verification":"`pnpm test` exits 0. Output shows 5 passing tests in `jwt.test.ts`. No tests in other files are broken (confirm smoke.test.ts still passes)."}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:exists src/auth/jwt.ts"},{"workspaceId":"primary","command":"fs:contains src/auth/jwt.ts \"export async function verifyAccessJwt\""},{"workspaceId":"primary","command":"fs:contains src/auth/jwt.ts \"JwtClaims\""},{"workspaceId":"primary","command":"fs:contains src/auth/jwt.ts \"cf-access-jwt-assertion\""},{"workspaceId":"primary","command":"fs:contains src/auth/jwt.ts \"cloudflareaccess\\.com\""},{"workspaceId":"primary","command":"fs:exists src/__tests__/state-machine/jwt.test.ts"},{"workspaceId":"primary","command":"fs:contains src/__tests__/state-machine/jwt.test.ts \"forge: true\""}],"functional":[{"workspaceId":"primary","command":"pnpm typecheck"},{"workspaceId":"primary","command":"pnpm test"}]}},{"id":"T-2","title":"State Machine Module (src/state/machine.ts) + Unit Tests","type":"code","modelTier":2,"files":{"create":[{"workspaceId":"primary","path":"src/state/machine.ts"},{"workspaceId":"primary","path":"src/__tests__/state-machine/state-machine.test.ts"}],"modify":[]},"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Create src/state/machine.ts. Key design note from spec: STATE_B is NOT a KV state — detectState() never returns it. Callers overlay JWT presence on top of STATE_A to differentiate STATE_A (no JWT) from STATE_B (JWT present). The boundary condition for STATE_D is strictly greater than 30 minutes (not >=).\n\n```typescript\nimport type { Env } from \"../env\";\n\nexport enum WorkerState {\n  STATE_A_NO_ACCESS = \"STATE_A\",\n  STATE_B_WITH_ACCESS = \"STATE_B\",\n  STATE_C_CONFIGURED = \"STATE_C\",\n  STATE_D_EXPIRED = \"STATE_D\",\n}\n\nconst SETUP_WINDOW_MS = 30 * 60 * 1000;\n\nexport async function detectState(env: Env, now: number): Promise<WorkerState> {\n  const config = await env.STATE.get(\"config\");\n  if (config !== null) return WorkerState.STATE_C_CONFIGURED;\n\n  const windowStart = await env.STATE.get(\"setup_window_start\");\n  if (windowStart !== null) {\n    const elapsed = now - parseInt(windowStart, 10);\n    if (elapsed > SETUP_WINDOW_MS) return WorkerState.STATE_D_EXPIRED;\n    // Within window — STATE_A, no write needed.\n    return WorkerState.STATE_A_NO_ACCESS;\n  }\n\n  // First visit: record the window start timestamp.\n  await env.STATE.put(\"setup_window_start\", now.toString());\n  return WorkerState.STATE_A_NO_ACCESS;\n}\n```","fileReference":"src/state/machine.ts","verification":"File exists. Exports `WorkerState` enum with four members including `STATE_B_WITH_ACCESS`. Exports `detectState(env, now)`. Uses `> SETUP_WINDOW_MS` (strict greater-than). Writes `setup_window_start` only when absent (inside the `windowStart === null` branch)."},{"stepNumber":2,"instruction":"Create src/__tests__/state-machine/state-machine.test.ts. This test file covers DW3 (setup_window_start written on first visit), DW11 (STATE_D when > 30 min), DW12 (recovery after key deletion), and the exact 30-minute boundary. The beforeEach must delete both `config` and `setup_window_start` from shared KV because vitest.config.ts uses `singleWorker: true` — all test files share one KV instance.\n\n```typescript\nimport { beforeEach, describe, expect, it } from \"vitest\";\n// @ts-expect-error — provided by @cloudflare/vitest-pool-workers at runtime\nimport { env } from \"cloudflare:test\";\nimport { detectState, WorkerState } from \"../../state/machine\";\n\ndescribe(\"detectState\", () => {\n  let kv: KVNamespace;\n  const NOW = 1_700_000_000_000; // fixed epoch ms for deterministic boundary tests\n\n  beforeEach(async () => {\n    kv = (env as { STATE: KVNamespace }).STATE;\n    await kv.delete(\"config\");\n    await kv.delete(\"setup_window_start\");\n  });\n\n  it(\"STATE_C: returns STATE_C_CONFIGURED when config key is present\", async () => {\n    await kv.put(\"config\", JSON.stringify({ display_name: \"Test\" }));\n    const state = await detectState(env as never, NOW);\n    expect(state).toBe(WorkerState.STATE_C_CONFIGURED);\n  });\n\n  it(\"STATE_A first-visit: returns STATE_A and writes setup_window_start (DW3)\", async () => {\n    const state = await detectState(env as never, NOW);\n    expect(state).toBe(WorkerState.STATE_A_NO_ACCESS);\n    const written = await kv.get(\"setup_window_start\");\n    expect(written).not.toBeNull();\n    expect(Number(written)).toBe(NOW);\n  });\n\n  it(\"STATE_A within-window: returns STATE_A when setup_window_start is within 30 min\", async () => {\n    await kv.put(\"setup_window_start\", (NOW - 10 * 60 * 1000).toString());\n    const state = await detectState(env as never, NOW);\n    expect(state).toBe(WorkerState.STATE_A_NO_ACCESS);\n  });\n\n  it(\"boundary: exactly 30 min elapsed is NOT expired (STATE_A)\", async () => {\n    await kv.put(\"setup_window_start\", (NOW - 30 * 60 * 1000).toString());\n    const state = await detectState(env as never, NOW);\n    expect(state).toBe(WorkerState.STATE_A_NO_ACCESS);\n  });\n\n  it(\"boundary: 30 min + 1 ms elapsed IS expired (STATE_D, DW11)\", async () => {\n    await kv.put(\"setup_window_start\", (NOW - 30 * 60 * 1000 - 1).toString());\n    const state = await detectState(env as never, NOW);\n    expect(state).toBe(WorkerState.STATE_D_EXPIRED);\n  });\n\n  it(\"STATE_D: returns STATE_D_EXPIRED when setup_window_start is 31 min old\", async () => {\n    await kv.put(\"setup_window_start\", (NOW - 31 * 60 * 1000).toString());\n    const state = await detectState(env as never, NOW);\n    expect(state).toBe(WorkerState.STATE_D_EXPIRED);\n  });\n\n  it(\"recovery: returns STATE_A after deleting expired setup_window_start (DW12)\", async () => {\n    await kv.put(\"setup_window_start\", (NOW - 31 * 60 * 1000).toString());\n    expect(await detectState(env as never, NOW)).toBe(WorkerState.STATE_D_EXPIRED);\n\n    await kv.delete(\"setup_window_start\");\n    const recovered = await detectState(env as never, NOW);\n    expect(recovered).toBe(WorkerState.STATE_A_NO_ACCESS);\n    // setup_window_start must be re-written on recovery (DW3 post-recovery).\n    const rewritten = await kv.get(\"setup_window_start\");\n    expect(rewritten).not.toBeNull();\n    expect(Number(rewritten)).toBe(NOW);\n  });\n\n  it(\"does not overwrite existing setup_window_start when within window\", async () => {\n    const fiveMinsAgo = NOW - 5 * 60 * 1000;\n    await kv.put(\"setup_window_start\", fiveMinsAgo.toString());\n    await detectState(env as never, NOW);\n    const afterCall = await kv.get(\"setup_window_start\");\n    expect(Number(afterCall)).toBe(fiveMinsAgo);\n  });\n});\n```","fileReference":"src/__tests__/state-machine/state-machine.test.ts","verification":"File exists. Contains 8 `it()` blocks. Imports `detectState` and `WorkerState` from `../../state/machine`. Has `beforeEach` that deletes both KV keys. Uses fixed `NOW` constant for deterministic boundary assertions."},{"stepNumber":3,"instruction":"Run `pnpm typecheck`. If TypeScript errors appear around `KVNamespace` type in the test file, add `/// <reference types=\"@cloudflare/workers-types\" />` at the top of the test file, or rely on the global types already configured by the project (check tsconfig.json for `types` field). The env.ts already defines `STATE: KVNamespace` so the cast `(env as { STATE: KVNamespace }).STATE` should resolve correctly.","fileReference":"src/__tests__/state-machine/state-machine.test.ts","verification":"`pnpm typecheck` exits 0 with no errors in src/state/machine.ts or the new test file."},{"stepNumber":4,"instruction":"Run `pnpm test` and confirm all 8 state-machine tests pass alongside the existing smoke tests and the jwt tests from T-1 (if T-1 ran first). If the boundary test `exactly 30 min = STATE_A` fails, verify the condition in machine.ts is `elapsed > SETUP_WINDOW_MS` (strictly greater than), not `>=`.","fileReference":"src/__tests__/state-machine/state-machine.test.ts","verification":"`pnpm test` exits 0. Output shows 8 passing tests in `state-machine.test.ts`. All prior smoke tests still pass."}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:exists src/state/machine.ts"},{"workspaceId":"primary","command":"fs:contains src/state/machine.ts \"export enum WorkerState\""},{"workspaceId":"primary","command":"fs:contains src/state/machine.ts \"export async function detectState\""},{"workspaceId":"primary","command":"fs:contains src/state/machine.ts \"STATE_C_CONFIGURED\""},{"workspaceId":"primary","command":"fs:contains src/state/machine.ts \"STATE_D_EXPIRED\""},{"workspaceId":"primary","command":"fs:contains src/state/machine.ts \"setup_window_start\""},{"workspaceId":"primary","command":"fs:exists src/__tests__/state-machine/state-machine.test.ts"},{"workspaceId":"primary","command":"fs:contains src/__tests__/state-machine/state-machine.test.ts \"DW12\""}],"functional":[{"workspaceId":"primary","command":"pnpm typecheck"},{"workspaceId":"primary","command":"pnpm test"},{"workspaceId":"primary","command":"pnpm build"}]}}],"riskFlags":["RF1: createRemoteJWKSet builds an in-memory JWKS cache per instance; jwt.ts must create the JWKS client inside the try block (per-call) to prevent cross-request key cache leakage in the miniflare single-worker context — the existing worker.ts handleAdmin() already uses this pattern as precedent.","RF2: vitest.config.ts uses singleWorker: true, meaning all test files share one KV namespace instance; state-machine tests must delete both 'config' and 'setup_window_start' in beforeEach or prior test runs in the same suite can contaminate state and cause false failures."],"confidence":0.87}