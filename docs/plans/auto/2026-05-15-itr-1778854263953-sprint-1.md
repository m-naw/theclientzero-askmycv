{"sprintId":"ITR-1778854263953-sprint-1","sprintName":"GET / Chat Page Wiring","specPath":"docs/specs/auto/2026-05-15-public-chat-behavior-f4.md","phaseContext":"Goal G5: Wire visitor-facing chat behavior. Sprint 1 covers GET / optional profile fields (location, social links, suggested questions). Sprint 2 will cover POST /chat + SSE streaming.","tasks":[{"id":"T-1","title":"Extend StoredConfig with optional profile fields and update handleRoot to forward them","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/types/config.ts"},{"workspaceId":"primary","path":"src/routes/index.ts"}],"create":[]},"artifact_refs":[],"dependencies":[],"steps":[{"stepNumber":1,"instruction":"In src/types/config.ts, add five optional fields to the StoredConfig interface, immediately after the `setup_timestamp` field:\n```ts\n/** Owner's city/region shown on the chat page. Optional. */\nlocation?: string;\n/** Owner's LinkedIn profile URL. Optional. */\nlinkedin_url?: string;\n/** Owner's GitHub profile URL. Optional. */\ngithub_url?: string;\n/** Downloadable CV PDF URL. Optional. */\npdf_cv_url?: string;\n/** Suggested starter questions for the chat page. When absent, handleRoot falls back to DEFAULT_SUGGESTED_QUESTIONS. Optional. */\nsuggested_questions?: string[];\n```\nDo NOT change the parseStoredConfig function body — the runtime cast on line 82 (`r as unknown as StoredConfig`) already passes through any extra fields present in the raw JSON object. Only the TypeScript interface needs updating.","fileReference":"src/types/config.ts","verification":"grep -n 'location\\?\\|linkedin_url\\?\\|github_url\\?\\|pdf_cv_url\\?\\|suggested_questions\\?' src/types/config.ts | wc -l — expect 5"},{"stepNumber":2,"instruction":"In src/routes/index.ts, replace the ChatPageProps construction inside the `State.C_CONFIGURED` branch (lines 98-102) with the following, which forwards all optional fields and uses cfg.suggested_questions with a fallback to DEFAULT_SUGGESTED_QUESTIONS:\n```ts\nconst props: ChatPageProps = {\n  display_name: cfg.display_name,\n  headline: cfg.headline,\n  location: cfg.location,\n  linkedin_url: cfg.linkedin_url,\n  github_url: cfg.github_url,\n  pdf_cv_url: cfg.pdf_cv_url,\n  suggested_questions: cfg.suggested_questions ?? DEFAULT_SUGGESTED_QUESTIONS,\n};\n```","fileReference":"src/routes/index.ts","verification":"grep -n 'location: cfg.location\\|linkedin_url: cfg.linkedin_url\\|github_url: cfg.github_url\\|pdf_cv_url: cfg.pdf_cv_url\\|suggested_questions: cfg.suggested_questions' src/routes/index.ts | wc -l — expect 5"},{"stepNumber":3,"instruction":"Run the TypeScript compiler to confirm no type errors were introduced:\n```bash\npnpm typecheck\n```\nExpected: exits 0 with no output (or only pre-existing diagnostics). If there are new errors, they will be in config.ts or routes/index.ts — fix any TypeScript complaint about the new optional fields before proceeding.","fileReference":"src/types/config.ts","verification":"pnpm typecheck exits 0"},{"stepNumber":4,"instruction":"Run the build to confirm the Worker bundle compiles cleanly:\n```bash\npnpm build\n```\nExpected: exits 0. The dist/ output should be generated without error.","fileReference":"src/worker.ts","verification":"pnpm build exits 0"}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:contains src/types/config.ts \"location?: string\""},{"workspaceId":"primary","command":"fs:contains src/types/config.ts \"linkedin_url?: string\""},{"workspaceId":"primary","command":"fs:contains src/types/config.ts \"github_url?: string\""},{"workspaceId":"primary","command":"fs:contains src/types/config.ts \"pdf_cv_url?: string\""},{"workspaceId":"primary","command":"fs:contains src/types/config.ts \"suggested_questions?: string[]\""},{"workspaceId":"primary","command":"fs:contains src/routes/index.ts \"location: cfg.location\""},{"workspaceId":"primary","command":"fs:contains src/routes/index.ts \"linkedin_url: cfg.linkedin_url\""},{"workspaceId":"primary","command":"fs:contains src/routes/index.ts \"github_url: cfg.github_url\""},{"workspaceId":"primary","command":"fs:contains src/routes/index.ts \"pdf_cv_url: cfg.pdf_cv_url\""},{"workspaceId":"primary","command":"fs:contains src/routes/index.ts \"suggested_questions: cfg.suggested_questions ?? DEFAULT_SUGGESTED_QUESTIONS\""}],"functional":[{"workspaceId":"primary","command":"pnpm typecheck"},{"workspaceId":"primary","command":"pnpm build"}]}},{"id":"T-2","title":"Write tests for GET / rendering optional profile fields","type":"code","modelTier":2,"files":{"modify":[],"create":[{"workspaceId":"primary","path":"src/test/chat-page-wiring.test.ts"}]},"artifact_refs":[],"dependencies":["T-1"],"steps":[{"stepNumber":1,"instruction":"Create src/test/chat-page-wiring.test.ts with the following complete content. This covers all done_when criteria for Sprint 1: GET / renders optional fields, URLs become <a> tags, ≥3 suggested questions appear, and anthropic_api_key is absent from the HTML.\n\n```ts\nimport { beforeEach, describe, expect, it } from \"vitest\";\n// @ts-expect-error — provided by @cloudflare/vitest-pool-workers at runtime\nimport { env, createExecutionContext, waitOnExecutionContext } from \"cloudflare:test\";\nimport worker from \"../worker\";\nimport { putJson } from \"./harness/kv\";\n\nconst BASE_CONFIG = {\n  display_name: \"Ada Lovelace\",\n  headline: \"Pioneering computer scientist\",\n  cv_markdown: \"# Ada\\n\\nPioneered the first algorithm.\",\n  anthropic_api_key: \"sk-ant-test\",\n  daily_budget_usd: 5,\n  access_email: \"ada@example.com\",\n  access_aud: \"aud-test\",\n  access_team_domain: \"team.cloudflareaccess.com\",\n  setup_timestamp: Date.now(),\n};\n\ndescribe(\"GET / optional profile fields wiring\", () => {\n  let kv: KVNamespace;\n\n  beforeEach(async () => {\n    kv = (env as { STATE: KVNamespace }).STATE;\n    // Clear any previous state.\n    await kv.delete(\"config\");\n    await kv.delete(\"setup_window_start\");\n  });\n\n  it(\"renders display_name and headline without optional fields\", async () => {\n    await putJson(kv, \"config\", BASE_CONFIG);\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    expect(res.status).toBe(200);\n    const body = await res.text();\n    expect(body).toContain(\"Ada Lovelace\");\n    expect(body).toContain(\"Pioneering computer scientist\");\n  });\n\n  it(\"renders location as plain text when present\", async () => {\n    await putJson(kv, \"config\", { ...BASE_CONFIG, location: \"London, UK\" });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    expect(body).toContain(\"London, UK\");\n  });\n\n  it(\"renders linkedin_url as an <a> tag when present\", async () => {\n    await putJson(kv, \"config\", { ...BASE_CONFIG, linkedin_url: \"https://linkedin.com/in/ada\" });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    expect(body).toContain('<a href=\"https://linkedin.com/in/ada\"');\n  });\n\n  it(\"renders github_url as an <a> tag when present\", async () => {\n    await putJson(kv, \"config\", { ...BASE_CONFIG, github_url: \"https://github.com/ada\" });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    expect(body).toContain('<a href=\"https://github.com/ada\"');\n  });\n\n  it(\"renders pdf_cv_url as an <a> tag when present\", async () => {\n    await putJson(kv, \"config\", { ...BASE_CONFIG, pdf_cv_url: \"https://example.com/cv.pdf\" });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    expect(body).toContain('<a href=\"https://example.com/cv.pdf\"');\n  });\n\n  it(\"renders all four optional fields together in a single response\", async () => {\n    await putJson(kv, \"config\", {\n      ...BASE_CONFIG,\n      location: \"London, UK\",\n      linkedin_url: \"https://linkedin.com/in/ada\",\n      github_url: \"https://github.com/ada\",\n      pdf_cv_url: \"https://example.com/cv.pdf\",\n    });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    expect(res.status).toBe(200);\n    const body = await res.text();\n    expect(body).toContain(\"London, UK\");\n    expect(body).toContain('<a href=\"https://linkedin.com/in/ada\"');\n    expect(body).toContain('<a href=\"https://github.com/ada\"');\n    expect(body).toContain('<a href=\"https://example.com/cv.pdf\"');\n  });\n\n  it(\"shows ≥3 suggested questions from config when provided\", async () => {\n    const questions = [\"Q one?\", \"Q two?\", \"Q three?\", \"Q four?\"];\n    await putJson(kv, \"config\", { ...BASE_CONFIG, suggested_questions: questions });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    for (const q of questions) {\n      expect(body).toContain(q);\n    }\n  });\n\n  it(\"falls back to DEFAULT_SUGGESTED_QUESTIONS when suggested_questions absent\", async () => {\n    await putJson(kv, \"config\", BASE_CONFIG);\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    // Default questions contain these fragments.\n    expect(body).toContain(\"Tell me about a hard technical decision\");\n    expect(body).toContain(\"largest team\");\n    expect(body).toContain(\"salary\");\n  });\n\n  it(\"does not expose anthropic_api_key anywhere in the GET / HTML response\", async () => {\n    await putJson(kv, \"config\", { ...BASE_CONFIG, linkedin_url: \"https://linkedin.com/in/ada\" });\n    const req = new Request(\"https://example.test/\");\n    const ctx = createExecutionContext();\n    const res = await worker.fetch(req, env as never, ctx);\n    await waitOnExecutionContext(ctx);\n    const body = await res.text();\n    expect(body).not.toContain(\"anthropic_api_key\");\n    expect(body).not.toContain(\"sk-ant-test\");\n  });\n});\n```","fileReference":"src/test/chat-page-wiring.test.ts","verification":"fs:exists src/test/chat-page-wiring.test.ts"},{"stepNumber":2,"instruction":"Run only the new test file to confirm all 8 tests pass in isolation:\n```bash\npnpm vitest run src/test/chat-page-wiring.test.ts\n```\nExpected: 8 tests pass, 0 fail. If any test fails, inspect the failure message — the most likely cause is a mismatch between the KV key or the anchor rendering in chat-page.ts. The anchor() helper is at src/views/primitives/index.ts; verify it produces `<a href=\"{url}\">` syntax.","fileReference":"src/test/chat-page-wiring.test.ts","verification":"pnpm vitest run src/test/chat-page-wiring.test.ts exits 0 with 8 passed"},{"stepNumber":3,"instruction":"Run the full test suite to verify no regressions and that test count meets the guardrail (≥76 passing):\n```bash\npnpm vitest run 2>&1 | grep 'Tests'\n```\nExpected output contains something like: `Tests  84 passed (84)` — the exact number depends on pre-existing suite. It must be ≥76 and must include the 8 new tests from step 2. If the count is below 76, a pre-existing test was broken — diff the output against the baseline to find which file.","fileReference":"src/test/smoke.test.ts","verification":"pnpm vitest run exits 0 with ≥76 passing tests"}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:exists src/test/chat-page-wiring.test.ts"},{"workspaceId":"primary","command":"fs:contains src/test/chat-page-wiring.test.ts \"anthropic_api_key\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-page-wiring.test.ts \"location: \\\"London, UK\\\"\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-page-wiring.test.ts \"<a href=\\\\\\\"https://linkedin.com\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-page-wiring.test.ts \"suggested_questions\""}],"functional":[{"workspaceId":"primary","command":"pnpm vitest run src/test/chat-page-wiring.test.ts"},{"workspaceId":"primary","command":"pnpm vitest run 2>&1 | grep 'Tests'"}]}}],"riskFlags":["RF1: The anchor() helper in src/views/primitives/index.ts must produce href=\"{url}\" syntax — if it wraps differently (e.g., data-href or relative URL), the <a> tag assertions in T-2 tests will fail. Verify anchor() output format against src/views/chat-page.ts line 26 before writing test assertions.","RF2: KV state from other tests (setup_window_start, config) may bleed between test suites since they share the same miniflare KV namespace. The beforeEach in T-2 deletes both keys explicitly, but if other test suites add new keys that affect detectState, isolation may break. Confirm smoke.test.ts and setup tests clean up their KV state."],"confidence":0.9}