# Sprint Plan: Inline recovery content and JSON error response

**Sprint ID:** ITR-1779131965968-sprint-1
**Spec:** [2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md](docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md)
**Confidence:** 0.87

## Context

Goal G6: Inline recovery pages with verified Cloudflare dashboard navigation. Prior sprints (commits 1ca5bf3, b71529b, 9d058a6) implemented all structural changes. This sprint completes the only remaining gap: missing test assertions in error-pages.test.ts, then confirms all done-when criteria pass via pnpm test and pnpm build.

## Risk flags

- RF1: The renderExpiredSetup test fixture on line 9 passes setupWindowStart as ISO string '2026-05-15T10:00:00Z' which resolves to NaN via Number() — expiredAt timestamp will not render. This is a pre-existing issue; the dash.cloudflare.com anchor and <ol> steps are unconditional so both new assertions will still pass. No code change needed to fix the fixture.

## Task T-1: Add error-pages test assertions and verify all done-when criteria

**Type:** code (modelTier: 2)

**Files to modify:**
- src/test/views/error-pages.test.ts (workspace: primary)

### Step 1

Confirm structural done-when criterion 1 is met: run `grep 'recovery_summary' src/routes/setup.ts` (must find line 116) and `grep 'recovery_url' src/routes/setup.ts` (must find NO matches, i.e., exit non-zero). Both are already present from commit 1ca5bf3 — verify before proceeding.

**File reference:** src/routes/setup.ts
**Verification:** grep 'recovery_summary' src/routes/setup.ts exits 0; grep 'recovery_url' src/routes/setup.ts exits non-zero (no match)

### Step 2

Confirm structural done-when criterion 2 is met: run `grep 'dash.cloudflare.com' src/views/error-pages.ts` (must find match on line 90) and `grep -c '<li' src/views/error-pages.ts` (must return 7, which is ≥5). Both already satisfied.

**File reference:** src/views/error-pages.ts
**Verification:** grep 'dash.cloudflare.com' src/views/error-pages.ts exits 0; grep -c '<li' src/views/error-pages.ts outputs ≥5

### Step 3

Confirm structural done-when criterion 3 is met: run `grep -r 'admin_password_hash' src/views/` (must find matches in src/views/login.ts lines 59-60) and `grep -r '<details' src/views/` (must find match in src/views/login.ts line 51). Both already satisfied.

**File reference:** src/views/login.ts
**Verification:** grep -r 'admin_password_hash' src/views/ exits 0; grep -r '<details' src/views/ exits 0

### Step 4

Add two new test assertions to the `renderExpiredSetup` describe block in src/test/views/error-pages.test.ts (after line 27, before the closing `});` of the describe block). Insert:

  it("includes a link to dash.cloudflare.com", () => {
    expect(html).toContain("dash.cloudflare.com");
  });
  it("has at least 5 recovery steps", () => {
    const liCount = (html.match(/<li/g) ?? []).length;
    expect(liCount).toBeGreaterThanOrEqual(5);
  });

Note: the existing test fixture on line 9 passes setupWindowStart as an ISO string `"2026-05-15T10:00:00Z"` which resolves to NaN via Number() — the expiredAt timestamp will not render in html, but the <ol> steps and the dash.cloudflare.com anchor are unconditional and will still be present. Both assertions will pass.

**File reference:** src/test/views/error-pages.test.ts
**Verification:** grep 'dash.cloudflare.com' src/test/views/error-pages.test.ts exits 0; grep 'liCount' src/test/views/error-pages.test.ts exits 0

### Step 5

Run `pnpm test` and confirm exit code 0 with 0 failures. This satisfies done-when criterion 1's pnpm test requirement and validates the new error-pages assertions.

**File reference:** src/test/views/error-pages.test.ts
**Verification:** pnpm test exits 0

### Step 6

Run `pnpm build` and confirm exit code 0 with 0 TypeScript errors. This satisfies done-when criterion 3's pnpm build requirement.

**File reference:** src/views/error-pages.ts
**Verification:** pnpm build exits 0

### Task verification

**Structural:**
- `grep 'recovery_summary' src/routes/setup.ts` (ws: primary)
- `! grep 'recovery_url' src/routes/setup.ts` (ws: primary)
- `grep 'dash.cloudflare.com' src/views/error-pages.ts` (ws: primary)
- `grep -c '<li' src/views/error-pages.ts | awk '{exit ($1 < 5)}'` (ws: primary)
- `grep -r 'admin_password_hash' src/views/` (ws: primary)
- `grep -r '<details' src/views/` (ws: primary)
- `grep 'dash.cloudflare.com' src/test/views/error-pages.test.ts` (ws: primary)
- `grep 'liCount' src/test/views/error-pages.test.ts` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)
- `pnpm build` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779131965968-sprint-1",
  "sprintName": "Inline recovery content and JSON error response",
  "specPath": "docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md",
  "phaseContext": "Goal G6: Inline recovery pages with verified Cloudflare dashboard navigation. Prior sprints (commits 1ca5bf3, b71529b, 9d058a6) implemented all structural changes. This sprint completes the only remaining gap: missing test assertions in error-pages.test.ts, then confirms all done-when criteria pass via pnpm test and pnpm build.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Add error-pages test assertions and verify all done-when criteria",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/test/views/error-pages.test.ts"
          }
        ],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Confirm structural done-when criterion 1 is met: run `grep 'recovery_summary' src/routes/setup.ts` (must find line 116) and `grep 'recovery_url' src/routes/setup.ts` (must find NO matches, i.e., exit non-zero). Both are already present from commit 1ca5bf3 — verify before proceeding.",
          "fileReference": "src/routes/setup.ts",
          "verification": "grep 'recovery_summary' src/routes/setup.ts exits 0; grep 'recovery_url' src/routes/setup.ts exits non-zero (no match)"
        },
        {
          "stepNumber": 2,
          "instruction": "Confirm structural done-when criterion 2 is met: run `grep 'dash.cloudflare.com' src/views/error-pages.ts` (must find match on line 90) and `grep -c '<li' src/views/error-pages.ts` (must return 7, which is ≥5). Both already satisfied.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "grep 'dash.cloudflare.com' src/views/error-pages.ts exits 0; grep -c '<li' src/views/error-pages.ts outputs ≥5"
        },
        {
          "stepNumber": 3,
          "instruction": "Confirm structural done-when criterion 3 is met: run `grep -r 'admin_password_hash' src/views/` (must find matches in src/views/login.ts lines 59-60) and `grep -r '<details' src/views/` (must find match in src/views/login.ts line 51). Both already satisfied.",
          "fileReference": "src/views/login.ts",
          "verification": "grep -r 'admin_password_hash' src/views/ exits 0; grep -r '<details' src/views/ exits 0"
        },
        {
          "stepNumber": 4,
          "instruction": "Add two new test assertions to the `renderExpiredSetup` describe block in src/test/views/error-pages.test.ts (after line 27, before the closing `});` of the describe block). Insert:\n\n  it(\"includes a link to dash.cloudflare.com\", () => {\n    expect(html).toContain(\"dash.cloudflare.com\");\n  });\n  it(\"has at least 5 recovery steps\", () => {\n    const liCount = (html.match(/<li/g) ?? []).length;\n    expect(liCount).toBeGreaterThanOrEqual(5);\n  });\n\nNote: the existing test fixture on line 9 passes setupWindowStart as an ISO string `\"2026-05-15T10:00:00Z\"` which resolves to NaN via Number() — the expiredAt timestamp will not render in html, but the <ol> steps and the dash.cloudflare.com anchor are unconditional and will still be present. Both assertions will pass.",
          "fileReference": "src/test/views/error-pages.test.ts",
          "verification": "grep 'dash.cloudflare.com' src/test/views/error-pages.test.ts exits 0; grep 'liCount' src/test/views/error-pages.test.ts exits 0"
        },
        {
          "stepNumber": 5,
          "instruction": "Run `pnpm test` and confirm exit code 0 with 0 failures. This satisfies done-when criterion 1's pnpm test requirement and validates the new error-pages assertions.",
          "fileReference": "src/test/views/error-pages.test.ts",
          "verification": "pnpm test exits 0"
        },
        {
          "stepNumber": 6,
          "instruction": "Run `pnpm build` and confirm exit code 0 with 0 TypeScript errors. This satisfies done-when criterion 3's pnpm build requirement.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "pnpm build exits 0"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep 'recovery_summary' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "! grep 'recovery_url' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'dash.cloudflare.com' src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -c '<li' src/views/error-pages.ts | awk '{exit ($1 < 5)}'"
          },
          {
            "workspaceId": "primary",
            "command": "grep -r 'admin_password_hash' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -r '<details' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'dash.cloudflare.com' src/test/views/error-pages.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'liCount' src/test/views/error-pages.test.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: The renderExpiredSetup test fixture on line 9 passes setupWindowStart as ISO string '2026-05-15T10:00:00Z' which resolves to NaN via Number() — expiredAt timestamp will not render. This is a pre-existing issue; the dash.cloudflare.com anchor and <ol> steps are unconditional so both new assertions will still pass. No code change needed to fix the fixture."
  ],
  "confidence": 0.87
}
```
