{"sprintId":"ITR-1778861503812-followup-1","sprintName":"Follow-up: 4 unmet criteria","specPath":"docs/specs/auto/2026-05-15-admin-configuration-editing-behavior-f5.md","phaseContext":"G7 iteration follow-up sprint: all implementation is in place; only test coverage gaps and one TypeScript error remain. src/routes/admin.ts correctly implements the Anthropic test-call shape and cv_markdown KV persistence. src/test/smoke.test.ts has a stale assertion (expects JSON from /admin but route now returns HTML). src/__tests__/admin/admin.test.ts has a TypeScript void-chain error on line 247, Test 9 lacks body-shape capture, and Acceptance Tests 10 and 11 per spec §12 are absent.","tasks":[{"id":"T-1","title":"Fix TypeScript error, add request-body capture to Test 9, and add Acceptance Tests 10 and 11","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/__tests__/admin/admin.test.ts"},{"workspaceId":"primary","path":"src/test/smoke.test.ts"}],"create":[]},"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Read src/__tests__/admin/admin.test.ts in full. Locate line 247: `expect(html).not.toContain(\"anthropic_api_key\".replace(\"_\", \"\")).not.toContain(\"sk-ant\");`. Replace this single line with two separate expect() calls:\n```\nexpect(html).not.toContain(\"anthropicapi_key\");\nexpect(html).not.toContain(\"sk-ant\");\n```\nThis fixes the TypeScript error caused by chaining .not.toContain() on a void return value.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"Run: pnpm typecheck 2>&1 | grep -c 'error TS' — result must be 0."},{"stepNumber":2,"instruction":"In src/__tests__/admin/admin.test.ts, locate Test 9 ('invalid anthropic_api_key returns 400 and config is not modified'). The current fetchMock intercept uses a static .reply(). Replace it with a callback-form intercept that captures the outgoing request body:\n```ts\nlet capturedReqBody: Record<string, unknown> | null = null;\nfetchMock\n  .get(rejectHost)\n  .intercept({ path: /\\/v1\\/messages.*/, method: \"POST\" })\n  .reply((opts: { body?: string }) => {\n    try { capturedReqBody = JSON.parse(opts.body ?? \"{}\"); } catch { /* */ }\n    return {\n      statusCode: 401,\n      data: JSON.stringify({ error: { type: \"authentication_error\" } }),\n      responseOptions: { headers: { \"content-type\": \"application/json\" } },\n    };\n  });\n```\nThen, AFTER the expect(res.status).toBe(400) and KV-unchanged assertions, add body-shape assertions:\n```ts\n// Verify outgoing Anthropic test-call body shape (spec §9 F5)\nexpect(capturedReqBody).not.toBeNull();\nexpect(capturedReqBody).toHaveProperty(\"model\");\nexpect(capturedReqBody).toHaveProperty(\"system\");\nexpect(capturedReqBody).toHaveProperty(\"messages\");\nexpect(capturedReqBody).toHaveProperty(\"max_tokens\");\n```","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm vitest run src/__tests__/admin/admin.test.ts 2>&1 | grep -E 'passed|failed' — all existing tests must still pass and test count must not decrease."},{"stepNumber":3,"instruction":"Add a module-level SSE helper at the top of src/__tests__/admin/admin.test.ts (after imports) that will be used by Acceptance Test 11:\n```ts\nfunction fakeAnthropicSse(): string {\n  return [\n    `event: message_start\\ndata: ${JSON.stringify({ type: \"message_start\", message: { id: \"m1\", usage: { input_tokens: 10, output_tokens: 0 } } })}\\n\\n`,\n    `event: content_block_delta\\ndata: ${JSON.stringify({ type: \"content_block_delta\", index: 0, delta: { type: \"text_delta\", text: \"Hi\" } })}\\n\\n`,\n    `event: message_delta\\ndata: ${JSON.stringify({ type: \"message_delta\", delta: { stop_reason: \"end_turn\" }, usage: { output_tokens: 1 } })}\\n\\n`,\n    `event: message_stop\\ndata: ${JSON.stringify({ type: \"message_stop\" })}\\n\\n`,\n  ].join(\"\");\n}\n```\nAlso add a drainStream helper:\n```ts\nasync function drainStream(res: Response): Promise<void> {\n  const reader = res.body!.getReader();\n  for (;;) {\n    const { done } = await reader.read();\n    if (done) break;\n  }\n}\n```","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm typecheck 2>&1 | grep -c 'error TS' — must be 0."},{"stepNumber":4,"instruction":"Append a new describe block at the end of src/__tests__/admin/admin.test.ts implementing Acceptance Test 10 per spec §12:\n```ts\ndescribe(\"Acceptance Test 10: Admin requires matching identity (spec §12 Test 10)\", () => {\n  const AT10_HOST = \"https://anthropic-mock-at10.test\";\n\n  beforeEach(async () => {\n    fetchMock.activate();\n    fetchMock.disableNetConnect();\n    try { fetchMock.enableNetConnect(/localhost/); } catch { /* */ }\n    (env as Record<string, string>).ANTHROPIC_BASE_URL = AT10_HOST;\n    (env as Record<string, string>).ACCESS_JWKS_URL_OVERRIDE = \"\";\n    await clearKv();\n  });\n\n  afterEach(async () => {\n    fetchMock.deactivate();\n    await clearKv();\n  });\n\n  it(\"runs all 5 spec §12 acceptance steps in sequence\", async () => {\n    const kp = await createJwtHarness();\n    await getEnv().STATE.put(TEST_JWKS_KV_KEY, JSON.stringify(kp.jwksDocument));\n    const cfg = baseConfig({\n      access_email: \"owner@test\",\n      access_aud: \"test-aud-1\",\n      access_team_domain: \"test.cloudflareaccess.com\",\n    });\n    await seedConfig(cfg);\n\n    // Step 1: no JWT → 403\n    const res1 = await runFetch(adminGetRequest(null));\n    expect(res1.status).toBe(403);\n\n    // Step 2: wrong email → 403 body contains 'email'\n    const jwtWrongEmail = await mintAccessJwt({\n      privateKey: kp.privateKey,\n      aud: \"test-aud-1\",\n      iss: \"https://test.cloudflareaccess.com\",\n      email: \"attacker@test\",\n    });\n    const res2 = await runFetch(adminGetRequest(jwtWrongEmail));\n    expect(res2.status).toBe(403);\n    expect(await res2.text()).toContain(\"email\");\n\n    // Step 3: wrong aud → 403 body contains 'aud'\n    const jwtWrongAud = await mintAccessJwt({\n      privateKey: kp.privateKey,\n      aud: \"different-aud\",\n      iss: \"https://test.cloudflareaccess.com\",\n      email: \"owner@test\",\n    });\n    const res3 = await runFetch(adminGetRequest(jwtWrongAud));\n    expect(res3.status).toBe(403);\n    expect(await res3.text()).toContain(\"aud\");\n\n    // Step 4: wrong iss → 403\n    const jwtWrongIss = await mintAccessJwt({\n      privateKey: kp.privateKey,\n      aud: \"test-aud-1\",\n      iss: \"https://other.cloudflareaccess.com\",\n      email: \"owner@test\",\n    });\n    const res4 = await runFetch(adminGetRequest(jwtWrongIss));\n    expect(res4.status).toBe(403);\n\n    // Step 5: fully matching JWT → 200 HTML with pre-filled input fields\n    const jwtValid = await mintAccessJwt({\n      privateKey: kp.privateKey,\n      aud: \"test-aud-1\",\n      iss: \"https://test.cloudflareaccess.com\",\n      email: \"owner@test\",\n    });\n    const res5 = await runFetch(adminGetRequest(jwtValid));\n    expect(res5.status).toBe(200);\n    const html5 = await res5.text();\n    expect(html5).toContain(\"input\");\n    expect(html5).toContain(cfg.display_name);\n  });\n});\n```","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm vitest run src/__tests__/admin/admin.test.ts 2>&1 | grep -E 'Acceptance Test 10' — must show a passing test."},{"stepNumber":5,"instruction":"Append another describe block at the end of src/__tests__/admin/admin.test.ts implementing Acceptance Test 11 per spec §12. IMPORTANT: cv_markdown must be >= 200 chars (CV_MIN_LENGTH) for admin/save to accept it:\n```ts\ndescribe(\"Acceptance Test 11: Admin edit reflects in chat (spec §12 Test 11)\", () => {\n  const AT11_HOST = \"https://anthropic-mock-at11.test\";\n  const OLD_CV = \"# Old CV content\\n\\n\" + \"This is the old content from before. \".repeat(6);\n  const NEW_CV = \"# Brand new role at Acme\\n\\n\" + \"Working at Acme Corp on exciting projects. \".repeat(5);\n\n  beforeEach(async () => {\n    fetchMock.activate();\n    fetchMock.disableNetConnect();\n    try { fetchMock.enableNetConnect(/localhost/); } catch { /* */ }\n    (env as Record<string, string>).ANTHROPIC_BASE_URL = AT11_HOST;\n    (env as Record<string, string>).ACCESS_JWKS_URL_OVERRIDE = \"\";\n    await clearKv();\n  });\n\n  afterEach(async () => {\n    fetchMock.deactivate();\n    await clearKv();\n  });\n\n  it(\"runs all 3 spec §12 acceptance steps in sequence\", async () => {\n    const kp = await createJwtHarness();\n    await getEnv().STATE.put(TEST_JWKS_KV_KEY, JSON.stringify(kp.jwksDocument));\n    const cfg = baseConfig({ cv_markdown: OLD_CV });\n    await seedConfig(cfg);\n\n    // Step 1: POST /admin/save with new cv_markdown → 200\n    const jwt = await mintAccessJwt({\n      privateKey: kp.privateKey,\n      aud: cfg.access_aud,\n      iss: `https://${cfg.access_team_domain}`,\n      email: cfg.access_email,\n    });\n    const saveBody = validSaveBody({ cv_markdown: NEW_CV });\n    const saveRes = await runFetch(adminSaveRequest(jwt, saveBody));\n    expect(saveRes.status).toBe(200);\n\n    // Step 2: Inspect KV — config.cv_markdown is updated\n    const storedRaw = await getEnv().STATE.get(\"config\");\n    const stored = JSON.parse(storedRaw as string) as StoredConfig;\n    expect(stored.cv_markdown).toBe(NEW_CV);\n\n    // Step 3: POST /chat — Anthropic request system field contains new CV, not old\n    const captured: { body: Record<string, unknown> | null } = { body: null };\n    fetchMock\n      .get(AT11_HOST)\n      .intercept({ path: /\\/v1\\/messages.*/, method: \"POST\" })\n      .reply((opts: { body?: string }) => {\n        try { captured.body = JSON.parse(opts.body ?? \"{}\"); } catch { /* */ }\n        return {\n          statusCode: 200,\n          data: fakeAnthropicSse(),\n          responseOptions: { headers: { \"content-type\": \"text/event-stream\" } },\n        };\n      });\n\n    const chatRes = await runFetch(new Request(\"https://example.test/chat\", {\n      method: \"POST\",\n      headers: {\n        \"content-type\": \"application/json\",\n        \"user-agent\": \"Mozilla/5.0 (compatible)\",\n      },\n      body: JSON.stringify({ messages: [{ role: \"user\", content: \"Tell me about your current role\" }] }),\n    }));\n    await drainStream(chatRes);\n\n    expect(captured.body).not.toBeNull();\n    const system = captured.body?.system as string | undefined;\n    expect(system).toContain(\"Brand new role at Acme\");\n    expect(system).not.toContain(\"old content from before\");\n  });\n});\n```","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm vitest run src/__tests__/admin/admin.test.ts 2>&1 | grep -E 'Acceptance Test 11' — must show a passing test."},{"stepNumber":6,"instruction":"Fix src/test/smoke.test.ts: the describe block 'JWKS injection wired through Worker /admin route' has a test at line 103 that expects a JSON response with { email: string } from GET /admin, but the route now returns HTML. Fix this by:\n1. Add `import { TEST_JWKS_KV_KEY } from \"../routes/jwks-source\";` to imports.\n2. Add an async afterEach inside the describe block that cleans up `config` and `TEST_JWKS_KV_KEY` from KV.\n3. Inside the 'accepts a valid JWT' it block, before the two admin requests, seed KV with a config matching the JWT identity (email: 'owner@example.com', aud: 'aud-123', access_team_domain: 'team.cloudflareaccess.com') AND seed `TEST_JWKS_KV_KEY` with the JWKS document from `jwks.getJwks()`. With TEST_JWKS_KV_KEY in KV, resolveJwksSource returns the in-memory JWKS so the JWKS URL interceptors are no longer needed (leave them or remove them — they won't be consumed, which is fine with .persist() not set).\n4. Replace `const validBody = (await validRes.json()) as { email: string }; expect(validBody.email).toBe('owner@example.com');` with `const validHtml = await validRes.text(); expect(validHtml).toContain('name=\"cv_markdown\"');` to verify the admin form is rendered.","fileReference":"src/test/smoke.test.ts","verification":"pnpm vitest run src/test/smoke.test.ts 2>&1 | grep -E 'passed|failed' — all smoke tests must pass."},{"stepNumber":7,"instruction":"Run the full test suite to verify all criteria. Check test count is >= baseline (180 from G6) plus new tests:\n```\npnpm test 2>&1 | tail -20\npnpm typecheck 2>&1 | tail -5\npnpm build 2>&1 | tail -5\n```\nIf any test fails:\n- For admin.test.ts failures: re-read the file and check that the fetchMock callback form matches the pattern in src/__tests__/chat/chat-route.test.ts (opts: { body?: string }) => { ... }\n- For smoke.test.ts failures: verify that seeding TEST_JWKS_KV_KEY in KV causes resolveJwksSource to return jwksOverride (confirmed in src/routes/jwks-source.ts:26-33)\n- For typecheck failures: ensure no new void-chain expressions were introduced","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm test 2>&1 | grep 'Tests' | grep -E '[0-9]+ passed' — count must be >= 186 (180 baseline + 6 new: Test 9 body assertions, AT10 step sequence, AT11 step sequence, smoke fix)."}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"Acceptance Test 10\""},{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"Acceptance Test 11\""},{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"capturedReqBody\""},{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"toHaveProperty(\\\"model\\\")\""},{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"Brand new role at Acme\""}],"functional":[{"workspaceId":"primary","command":"pnpm typecheck 2>&1 | grep -c 'error TS'"},{"workspaceId":"primary","command":"pnpm vitest run src/__tests__/admin/admin.test.ts 2>&1 | grep -E 'passed|failed'"},{"workspaceId":"primary","command":"pnpm vitest run src/test/smoke.test.ts 2>&1 | grep -E 'passed|failed'"},{"workspaceId":"primary","command":"pnpm test 2>&1 | grep 'Tests'"},{"workspaceId":"primary","command":"pnpm build 2>&1 | tail -3"}]}}],"riskFlags":["RF1: fetchMock callback-form body capture for Anthropic SDK calls — the SDK may encode the request body differently from raw fetch; the opts.body pattern is confirmed to work for raw fetch in chat-route.test.ts but must be verified to also work when the SDK is the caller in admin.ts (same underlying fetch mechanism, but worth confirming by running the test).","RF2: smoke.test.ts JWKS interceptors become unused after seeding TEST_JWKS_KV_KEY — the two .intercept() calls will not be consumed; if fetchMock is configured to fail on unused interceptors this could break the test. Mitigation: add .persist() to the interceptors or remove them after seeding TEST_JWKS_KV_KEY in KV."],"confidence":0.9}