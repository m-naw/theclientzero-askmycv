# Sprint Plan: All G6 changes: atomic setup.ts fix, error-pages expansion, login details, verify

**Sprint ID:** ITR-1779132817608-sprint-1
**Spec:** [2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md](docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md)
**Confidence:** 0.8

## Context

G6 final attempt (attempt 4): all structural done-when criteria appear met from prior commits (4abaed4, b71529b, f06d11c); this sprint verifies and completes — adds admin_password_hash to renderExpiredSetup per objective, then runs pnpm test + pnpm build to confirm all done-when criteria pass.

## Risk flags

- RF1: pnpm test may fail for a reason not visible in static file analysis — 3 prior sprint failures suggest hidden fragility; executor must read test output carefully and fix root cause, not symptoms
- RF2: Adding admin_password_hash paragraph to renderExpiredSetup must not break existing literals (setup_window_start, STATE, Delete, dash.cloudflare.com must all remain present) — insert after </ol> inside same <section> element

## Task T-1: Add admin_password_hash to renderExpiredSetup, then run pnpm test + pnpm build

**Type:** code (modelTier: 2)

**Files to modify:**
- src/views/error-pages.ts (workspace: primary)
- src/routes/setup.ts (workspace: primary)
- src/__tests__/setup/setup-acceptance.test.ts (workspace: primary)

### Step 1

Read src/routes/setup.ts lines 105–130. Verify the window-expired gate (step 2 in the comment block) returns HTTP 403 with JSON body containing 'error' (string), 'expired_at' (ISO8601 string), 'recovery_summary' (non-empty string), and NO 'recovery_url' field. The existing code at line ~111 should already return 403 JSON. If it still returns 410 or HTML, replace that block with: return new Response(JSON.stringify({ error: 'setup_window_expired', expired_at: new Date(startMs + SETUP_WINDOW_MS).toISOString(), recovery_summary: 'Delete the setup_window_start key from the STATE KV namespace at https://dash.cloudflare.com to open a new 10-minute setup window.' }), { status: 403, headers: { 'content-type': 'application/json' } });

**File reference:** src/routes/setup.ts
**Verification:** grep -n 'recovery_summary' src/routes/setup.ts must return at least one match

### Step 2

Read src/__tests__/setup/setup-acceptance.test.ts lines 358–400 (Test 5). Verify assertions match the 403 JSON contract: expect(r.status).toBe(403); expect(r.headers.get('content-type')).toContain('application/json'); const json = await r.json(); expect(typeof json.error).toBe('string'); expect(typeof json.expired_at).toBe('string'); expect(typeof json.recovery_summary).toBe('string'); expect((json.recovery_summary as string).length).toBeGreaterThan(0); expect(json.recovery_url).toBeUndefined(). If the test still has toBe(410), replace it with toBe(403) and add the JSON assertions listed above. Do NOT add expect(json.error).toBe('...') with a specific string value — the test must only check typeof to allow flexibility in the error string.

**File reference:** src/__tests__/setup/setup-acceptance.test.ts
**Verification:** grep -n 'toBe(410)' src/__tests__/setup/setup-acceptance.test.ts must return zero lines

### Step 3

Read src/views/error-pages.ts. Verify renderExpiredSetup already has: (a) dash.cloudflare.com link in the recovery steps, (b) 6+ ordered <li> steps, (c) '10-minute' (not '30-minute'), (d) import of SETUP_WINDOW_MS at line 1. Then add a mention of admin_password_hash in the renderExpiredSetup recovery section — the objective requires it. After the existing ordered list (7 recovery steps for setup_window_start), add a note paragraph: '<p class="muted">If you also need to reset admin credentials, delete the <code>admin_password_hash</code> key from the same <strong>STATE</strong> namespace, then re-run setup at <code>/setup</code> to set a new admin password.</p>'. Insert it inside the same <section class="card"> element, after the closing </ol> tag and before the closing </section> tag.

**File reference:** src/views/error-pages.ts
**Verification:** grep -n 'admin_password_hash' src/views/error-pages.ts must return at least one match

### Step 4

Read src/views/login.ts and verify renderLoginForm already has: (a) <details class="forgot-password"> element, (b) the text 'admin_password_hash' inside it, (c) 'STATE' inside it, (d) a link to dash.cloudflare.com. Also verify the form still has name='admin_password' and action='/login'. No changes should be needed — this was completed in a prior commit. If any element is missing, add it per the spec OBJECTIVE 3 template: a <details><summary>Forgot password</summary> block with an <ol> of steps referencing dash.cloudflare.com, admin_password_hash, STATE, and Delete.

**File reference:** src/views/login.ts
**Verification:** grep -n '<details' src/views/login.ts must return at least one match; grep -n 'admin_password_hash' src/views/login.ts must return at least one match

### Step 5

Run pnpm test. If all tests pass, proceed to step 6. If tests fail: (1) Read the full failure output including file names, test names, and assertion errors. (2) Identify whether the failure is in a test assertion (wrong expected value) or in the implementation (wrong response returned). (3) Fix the ROOT CAUSE — do not patch a test to hide an implementation bug. Key invariants to preserve: GET / and POST /chat are unauthenticated; POST /setup returns 403 JSON (not 410, not HTML) when setup_window_start is expired; the JSON has 'error', 'expired_at', 'recovery_summary', and NO 'recovery_url'; bcrypt cost >=12; session cookie is HttpOnly+Secure+SameSite=Lax.

**File reference:** src/__tests__/setup/setup-acceptance.test.ts
**Verification:** pnpm test exit code must be 0

### Step 6

Run pnpm build. If build succeeds with exit code 0, all done-when criteria are met. If build fails with TypeScript errors: read each error message, identify the file:line, and fix the type issue. Common causes in this sprint: (1) escapeHtml called with possibly-undefined value — add null guard; (2) import path typo; (3) return type mismatch in a modified function. After fixing, re-run pnpm test to confirm tests still pass before declaring done.

**File reference:** src/views/error-pages.ts
**Verification:** pnpm build exit code must be 0

### Task verification

**Structural:**
- `grep -n 'recovery_summary' src/routes/setup.ts` (ws: primary)
- `grep -n 'dash.cloudflare.com' src/views/error-pages.ts` (ws: primary)
- `grep -rn 'admin_password_hash' src/views/` (ws: primary)
- `grep -rn '<details' src/views/` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)
- `pnpm build` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779132817608-sprint-1",
  "sprintName": "All G6 changes: atomic setup.ts fix, error-pages expansion, login details, verify",
  "specPath": "docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md",
  "phaseContext": "G6 final attempt (attempt 4): all structural done-when criteria appear met from prior commits (4abaed4, b71529b, f06d11c); this sprint verifies and completes — adds admin_password_hash to renderExpiredSetup per objective, then runs pnpm test + pnpm build to confirm all done-when criteria pass.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Add admin_password_hash to renderExpiredSetup, then run pnpm test + pnpm build",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/__tests__/setup/setup-acceptance.test.ts"
          }
        ],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read src/routes/setup.ts lines 105–130. Verify the window-expired gate (step 2 in the comment block) returns HTTP 403 with JSON body containing 'error' (string), 'expired_at' (ISO8601 string), 'recovery_summary' (non-empty string), and NO 'recovery_url' field. The existing code at line ~111 should already return 403 JSON. If it still returns 410 or HTML, replace that block with: return new Response(JSON.stringify({ error: 'setup_window_expired', expired_at: new Date(startMs + SETUP_WINDOW_MS).toISOString(), recovery_summary: 'Delete the setup_window_start key from the STATE KV namespace at https://dash.cloudflare.com to open a new 10-minute setup window.' }), { status: 403, headers: { 'content-type': 'application/json' } });",
          "fileReference": "src/routes/setup.ts",
          "verification": "grep -n 'recovery_summary' src/routes/setup.ts must return at least one match"
        },
        {
          "stepNumber": 2,
          "instruction": "Read src/__tests__/setup/setup-acceptance.test.ts lines 358–400 (Test 5). Verify assertions match the 403 JSON contract: expect(r.status).toBe(403); expect(r.headers.get('content-type')).toContain('application/json'); const json = await r.json(); expect(typeof json.error).toBe('string'); expect(typeof json.expired_at).toBe('string'); expect(typeof json.recovery_summary).toBe('string'); expect((json.recovery_summary as string).length).toBeGreaterThan(0); expect(json.recovery_url).toBeUndefined(). If the test still has toBe(410), replace it with toBe(403) and add the JSON assertions listed above. Do NOT add expect(json.error).toBe('...') with a specific string value — the test must only check typeof to allow flexibility in the error string.",
          "fileReference": "src/__tests__/setup/setup-acceptance.test.ts",
          "verification": "grep -n 'toBe(410)' src/__tests__/setup/setup-acceptance.test.ts must return zero lines"
        },
        {
          "stepNumber": 3,
          "instruction": "Read src/views/error-pages.ts. Verify renderExpiredSetup already has: (a) dash.cloudflare.com link in the recovery steps, (b) 6+ ordered <li> steps, (c) '10-minute' (not '30-minute'), (d) import of SETUP_WINDOW_MS at line 1. Then add a mention of admin_password_hash in the renderExpiredSetup recovery section — the objective requires it. After the existing ordered list (7 recovery steps for setup_window_start), add a note paragraph: '<p class=\"muted\">If you also need to reset admin credentials, delete the <code>admin_password_hash</code> key from the same <strong>STATE</strong> namespace, then re-run setup at <code>/setup</code> to set a new admin password.</p>'. Insert it inside the same <section class=\"card\"> element, after the closing </ol> tag and before the closing </section> tag.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "grep -n 'admin_password_hash' src/views/error-pages.ts must return at least one match"
        },
        {
          "stepNumber": 4,
          "instruction": "Read src/views/login.ts and verify renderLoginForm already has: (a) <details class=\"forgot-password\"> element, (b) the text 'admin_password_hash' inside it, (c) 'STATE' inside it, (d) a link to dash.cloudflare.com. Also verify the form still has name='admin_password' and action='/login'. No changes should be needed — this was completed in a prior commit. If any element is missing, add it per the spec OBJECTIVE 3 template: a <details><summary>Forgot password</summary> block with an <ol> of steps referencing dash.cloudflare.com, admin_password_hash, STATE, and Delete.",
          "fileReference": "src/views/login.ts",
          "verification": "grep -n '<details' src/views/login.ts must return at least one match; grep -n 'admin_password_hash' src/views/login.ts must return at least one match"
        },
        {
          "stepNumber": 5,
          "instruction": "Run pnpm test. If all tests pass, proceed to step 6. If tests fail: (1) Read the full failure output including file names, test names, and assertion errors. (2) Identify whether the failure is in a test assertion (wrong expected value) or in the implementation (wrong response returned). (3) Fix the ROOT CAUSE — do not patch a test to hide an implementation bug. Key invariants to preserve: GET / and POST /chat are unauthenticated; POST /setup returns 403 JSON (not 410, not HTML) when setup_window_start is expired; the JSON has 'error', 'expired_at', 'recovery_summary', and NO 'recovery_url'; bcrypt cost >=12; session cookie is HttpOnly+Secure+SameSite=Lax.",
          "fileReference": "src/__tests__/setup/setup-acceptance.test.ts",
          "verification": "pnpm test exit code must be 0"
        },
        {
          "stepNumber": 6,
          "instruction": "Run pnpm build. If build succeeds with exit code 0, all done-when criteria are met. If build fails with TypeScript errors: read each error message, identify the file:line, and fix the type issue. Common causes in this sprint: (1) escapeHtml called with possibly-undefined value — add null guard; (2) import path typo; (3) return type mismatch in a modified function. After fixing, re-run pnpm test to confirm tests still pass before declaring done.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "pnpm build exit code must be 0"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -n 'recovery_summary' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -n 'dash.cloudflare.com' src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rn 'admin_password_hash' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rn '<details' src/views/"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: pnpm test may fail for a reason not visible in static file analysis — 3 prior sprint failures suggest hidden fragility; executor must read test output carefully and fix root cause, not symptoms",
    "RF2: Adding admin_password_hash paragraph to renderExpiredSetup must not break existing literals (setup_window_start, STATE, Delete, dash.cloudflare.com must all remain present) — insert after </ol> inside same <section> element"
  ],
  "confidence": 0.8
}
```
