# Sprint Plan: End-to-End Verification and Final Fixes

**Sprint ID:** ITR-1779191604774-sprint-1
**Spec:** [2026-05-19-full-v2-acceptance-test-suite-and-regression-sweep.md](docs/specs/auto/2026-05-19-full-v2-acceptance-test-suite-and-regression-sweep.md)
**Confidence:** 0.95

## Context

G8 final verification sprint — all 20 spec-v2.md acceptance tests were implemented in prior sprints (commits 7b774a9, c350447, 3f8a3f3). This sprint runs the full quality gate, fixes any residual failures in-place, and updates the acceptance report with the live test count. The COVERAGE-MANDATE applies because done_when includes pnpm test/build/typecheck/lint — the task is code-class even when no source changes are expected, because in-place fixes are required if any gate fails.

## Risk flags

- RF1: Duplicate test files exist (test-chat-not-configured.test.ts + chat-503-not-configured.test.ts; test-kv-failure.test.ts + kv-and-spend-failure.test.ts). Both pairs currently pass (verified: 292/0, exit 0). Duplicate coverage is harmless but adds KV-state-bleed risk in future miniflare singleWorker upgrades. Not a blocker for this sprint.
- RF2: Acceptance report aggregate count was authored as partial sections (25 acceptance + 149 changed-suite) that do not match the 292 live total from pnpm test. Step 4 corrects this. If the report already shows 292, step 4 is a no-op — the executor must verify the actual number from step 1 before editing.

## Task T-1: Run full quality gate, fix any in-place failures, and update acceptance report with live test count

**Type:** code (modelTier: 2)

**Files to modify:**
- docs/checkpoints/v2-acceptance-report.md (workspace: primary)

### Step 1

Run pnpm test and capture the live count. Command: `pnpm test 2>&1 | tail -30`. Confirm exit 0 and record the exact passing count (expected: 292, minimum acceptable: 214). If exit code is non-zero or any test fails, read the failing test's error, identify whether the failure is a compile error, assertion mismatch, or type error, fix it in-place in the relevant source or test file, then re-run pnpm test until exit 0. Do NOT skip or delete failing tests — fix them.

**File reference:** vitest.config.ts
**Verification:** pnpm test exits 0 with >=214 passing and 0 failures

### Step 2

Run `pnpm build && pnpm typecheck && pnpm lint`. Confirm all three exit 0. If build fails, read the TypeScript error (file:line), fix the type error in the indicated source file, and re-run until exit 0. If lint fails, read the ESLint violation, fix it in the flagged file, and re-run. Do NOT add eslint-disable comments unless the rule genuinely does not apply — prefer fixing the code.

**File reference:** package.json
**Verification:** pnpm build && pnpm typecheck && pnpm lint all exit 0

### Step 3

Run all 7 structural verification commands verbatim and confirm each exits 0:

```
test -f docs/SPEC_CORRECTIONS.md
test -f docs/checkpoints/v2-acceptance-report.md
grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q .
grep -rPl '/chat.{0,200}503.{0,200}not.configured' src/__tests__ | grep -q .
grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q .
grep -rPl '/admin.{0,200}404.{0,200}unconfigured' src/__tests__ | grep -q .
! grep -E '\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\b' wrangler.toml
```

If any command exits non-zero, diagnose and fix the broken criterion before continuing. For the wrangler.toml check: if it fails, identify which mock variable was introduced and remove it — never add mock vars to wrangler.toml [vars] under any circumstance.

**File reference:** wrangler.toml
**Verification:** All 7 commands exit 0

### Step 4

Open docs/checkpoints/v2-acceptance-report.md. Find the line(s) that state the total passing test count (the report was authored with partial counts that sum to fewer than the live 292). Replace the authored aggregate count with the exact number from step 1's pnpm test output. Do NOT change per-test pass/fail entries, the human-smoke-test callout for F9 (iOS/Android keyboard), or any other narrative content — only the aggregate count line(s). If the report already contains the correct live count from step 1, no edit is needed.

**File reference:** docs/checkpoints/v2-acceptance-report.md
**Verification:** docs/checkpoints/v2-acceptance-report.md contains the live test count number that matches step 1's pnpm test output

### Step 5

Run pnpm test one final time to confirm no regression from step 4's edit (a .md file edit cannot affect tests, but this confirms the workspace is in a clean, reproducible state). Confirm exit 0 with the same passing count as step 1.

**File reference:** vitest.config.ts
**Verification:** pnpm test exits 0 with the same passing count as step 1, 0 failures

### Task verification

**Structural:**
- `test -f docs/SPEC_CORRECTIONS.md` (ws: primary)
- `test -f docs/checkpoints/v2-acceptance-report.md` (ws: primary)
- `grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q .` (ws: primary)
- `grep -rPl '/chat.{0,200}503.{0,200}not.configured' src/__tests__ | grep -q .` (ws: primary)
- `grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q .` (ws: primary)
- `grep -rPl '/admin.{0,200}404.{0,200}unconfigured' src/__tests__ | grep -q .` (ws: primary)
- `! grep -E '\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\b' wrangler.toml` (ws: primary)

**Functional:**
- `pnpm test && pnpm build && pnpm typecheck && pnpm lint` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779191604774-sprint-1",
  "sprintName": "End-to-End Verification and Final Fixes",
  "specPath": "docs/specs/auto/2026-05-19-full-v2-acceptance-test-suite-and-regression-sweep.md",
  "phaseContext": "G8 final verification sprint — all 20 spec-v2.md acceptance tests were implemented in prior sprints (commits 7b774a9, c350447, 3f8a3f3). This sprint runs the full quality gate, fixes any residual failures in-place, and updates the acceptance report with the live test count. The COVERAGE-MANDATE applies because done_when includes pnpm test/build/typecheck/lint — the task is code-class even when no source changes are expected, because in-place fixes are required if any gate fails.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Run full quality gate, fix any in-place failures, and update acceptance report with live test count",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "docs/checkpoints/v2-acceptance-report.md"
          }
        ],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run pnpm test and capture the live count. Command: `pnpm test 2>&1 | tail -30`. Confirm exit 0 and record the exact passing count (expected: 292, minimum acceptable: 214). If exit code is non-zero or any test fails, read the failing test's error, identify whether the failure is a compile error, assertion mismatch, or type error, fix it in-place in the relevant source or test file, then re-run pnpm test until exit 0. Do NOT skip or delete failing tests — fix them.",
          "fileReference": "vitest.config.ts",
          "verification": "pnpm test exits 0 with >=214 passing and 0 failures"
        },
        {
          "stepNumber": 2,
          "instruction": "Run `pnpm build && pnpm typecheck && pnpm lint`. Confirm all three exit 0. If build fails, read the TypeScript error (file:line), fix the type error in the indicated source file, and re-run until exit 0. If lint fails, read the ESLint violation, fix it in the flagged file, and re-run. Do NOT add eslint-disable comments unless the rule genuinely does not apply — prefer fixing the code.",
          "fileReference": "package.json",
          "verification": "pnpm build && pnpm typecheck && pnpm lint all exit 0"
        },
        {
          "stepNumber": 3,
          "instruction": "Run all 7 structural verification commands verbatim and confirm each exits 0:\n\n```\ntest -f docs/SPEC_CORRECTIONS.md\ntest -f docs/checkpoints/v2-acceptance-report.md\ngrep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q .\ngrep -rPl '/chat.{0,200}503.{0,200}not.configured' src/__tests__ | grep -q .\ngrep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q .\ngrep -rPl '/admin.{0,200}404.{0,200}unconfigured' src/__tests__ | grep -q .\n! grep -E '\\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\\b' wrangler.toml\n```\n\nIf any command exits non-zero, diagnose and fix the broken criterion before continuing. For the wrangler.toml check: if it fails, identify which mock variable was introduced and remove it — never add mock vars to wrangler.toml [vars] under any circumstance.",
          "fileReference": "wrangler.toml",
          "verification": "All 7 commands exit 0"
        },
        {
          "stepNumber": 4,
          "instruction": "Open docs/checkpoints/v2-acceptance-report.md. Find the line(s) that state the total passing test count (the report was authored with partial counts that sum to fewer than the live 292). Replace the authored aggregate count with the exact number from step 1's pnpm test output. Do NOT change per-test pass/fail entries, the human-smoke-test callout for F9 (iOS/Android keyboard), or any other narrative content — only the aggregate count line(s). If the report already contains the correct live count from step 1, no edit is needed.",
          "fileReference": "docs/checkpoints/v2-acceptance-report.md",
          "verification": "docs/checkpoints/v2-acceptance-report.md contains the live test count number that matches step 1's pnpm test output"
        },
        {
          "stepNumber": 5,
          "instruction": "Run pnpm test one final time to confirm no regression from step 4's edit (a .md file edit cannot affect tests, but this confirms the workspace is in a clean, reproducible state). Confirm exit 0 with the same passing count as step 1.",
          "fileReference": "vitest.config.ts",
          "verification": "pnpm test exits 0 with the same passing count as step 1, 0 failures"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "test -f docs/SPEC_CORRECTIONS.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/checkpoints/v2-acceptance-report.md"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q ."
          },
          {
            "workspaceId": "primary",
            "command": "grep -rPl '/chat.{0,200}503.{0,200}not.configured' src/__tests__ | grep -q ."
          },
          {
            "workspaceId": "primary",
            "command": "grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q ."
          },
          {
            "workspaceId": "primary",
            "command": "grep -rPl '/admin.{0,200}404.{0,200}unconfigured' src/__tests__ | grep -q ."
          },
          {
            "workspaceId": "primary",
            "command": "! grep -E '\\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\\b' wrangler.toml"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test && pnpm build && pnpm typecheck && pnpm lint"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: Duplicate test files exist (test-chat-not-configured.test.ts + chat-503-not-configured.test.ts; test-kv-failure.test.ts + kv-and-spend-failure.test.ts). Both pairs currently pass (verified: 292/0, exit 0). Duplicate coverage is harmless but adds KV-state-bleed risk in future miniflare singleWorker upgrades. Not a blocker for this sprint.",
    "RF2: Acceptance report aggregate count was authored as partial sections (25 acceptance + 149 changed-suite) that do not match the 292 live total from pnpm test. Step 4 corrects this. If the report already shows 292, step 4 is a no-op — the executor must verify the actual number from step 1 before editing."
  ],
  "confidence": 0.95
}
```
