{"sprintId":"ITR-1778857568357-sprint-1","sprintName":"Anti-abuse guards + budget pre-flight","specPath":"docs/specs/auto/2026-05-15-anti-abuse-daily-budget-cap-f6-f7.md","phaseContext":"G6: Anti-abuse and daily budget cap — F6/F7 from docs/spec.md. Completes post-G5 hardening of the POST /chat endpoint with UA rejection, per-IP rate limiting, budget pre-flight, payload size guard, and ctx.waitUntil decoupling for spend writes.","tasks":[{"id":"T-1","title":"Add max_msgs_per_hour to StoredConfig and thread ctx through worker.ts","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/types/config.ts"},{"workspaceId":"primary","path":"src/worker.ts"}],"create":[]},"dependencies":[],"steps":[{"stepNumber":1,"instruction":"In src/types/config.ts, add the optional field `max_msgs_per_hour?: number` to the StoredConfig interface, after the existing optional profile fields block. No change to parseStoredConfig is needed — the existing cast at line 94 (`r as unknown as StoredConfig`) will pass the field through when present. The field is optional and defaults to 30 at the call-site in chat.ts.","fileReference":"src/types/config.ts","verification":"grep 'max_msgs_per_hour' src/types/config.ts"},{"stepNumber":2,"instruction":"In src/worker.ts line 46, rename the parameter from `_ctx: ExecutionContext` to `ctx: ExecutionContext`. On line 58, change the call from `handlePostChat(request, env)` to `handlePostChat(request, env, ctx)`. This threads the ExecutionContext through so chat.ts can use ctx.waitUntil for spend writes.","fileReference":"src/worker.ts","verification":"grep 'ctx: ExecutionContext' src/worker.ts && grep 'handlePostChat(request, env, ctx)' src/worker.ts"}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:contains src/types/config.ts \"max_msgs_per_hour\""},{"workspaceId":"primary","command":"fs:contains src/worker.ts \"handlePostChat(request, env, ctx)\""}],"functional":[{"workspaceId":"primary","command":"pnpm typecheck 2>&1 | tail -5"}]}},{"id":"T-2","title":"Implement anti-abuse guards and budget pre-flight in chat.ts + acceptance tests 7/8/9","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/routes/chat.ts"}],"create":[{"workspaceId":"primary","path":"src/__tests__/chat/chat-abuse.test.ts"}]},"dependencies":["T-1"],"steps":[{"stepNumber":1,"instruction":"In src/routes/chat.ts, update the function signature from `handlePostChat(request: Request, env: Env)` to `handlePostChat(request: Request, env: Env, ctx: ExecutionContext)`. This matches the updated call-site in worker.ts.","fileReference":"src/routes/chat.ts","verification":"grep 'ctx: ExecutionContext' src/routes/chat.ts"},{"stepNumber":2,"instruction":"In src/routes/chat.ts, add a 100KB payload size guard as the FIRST check in handlePostChat (before body parsing). Read the Content-Length header: if it is present and greater than 100_000, return errorJson(413, 'payload too large'). If Content-Length is absent, read the body as text first, check text.length > 100_000 and return 413 if so, then parse JSON from the text string. This handles both cases without double-reading the body.","fileReference":"src/routes/chat.ts","verification":"grep '413' src/routes/chat.ts"},{"stepNumber":3,"instruction":"In src/routes/chat.ts, add a User-Agent bot rejection guard immediately after body parsing and validation (after the garbage-input check, before `readConfig`). Read `request.headers.get('user-agent') ?? ''`. If: (a) length < 10, OR (b) matches `/^(curl|wget|python-requests|httpie|go-http)/i`, return `new Response(JSON.stringify({ error: 'forbidden' }), { status: 403, headers: JSON_HEADERS })`. This must fire before any KV or Anthropic access.","fileReference":"src/routes/chat.ts","verification":"grep '403' src/routes/chat.ts"},{"stepNumber":4,"instruction":"In src/routes/chat.ts, add a budget pre-flight check immediately after `readConfig` (after `cfg === null` guard). Read the KV key `spend:<todayUtcDate()>`. If `Number(current ?? '0') >= cfg.daily_budget_usd`, return `new Response(JSON.stringify({ error: 'daily budget exceeded' }), { status: 503, headers: JSON_HEADERS })`. Zero Anthropic calls must be made when this fires.","fileReference":"src/routes/chat.ts","verification":"grep 'daily budget exceeded' src/routes/chat.ts"},{"stepNumber":5,"instruction":"In src/routes/chat.ts, add a per-IP hourly rate limit check immediately after the budget pre-flight. Extract IP: `const ip = request.headers.get('cf-connecting-ip') ?? request.headers.get('x-forwarded-for') ?? 'unknown'`. Compute hour bucket: `const hour = Math.floor(Date.now() / 3_600_000)`. KV key: `ratelimit:${ip}:${hour}`. Read the key; parse count as `Number(existing ?? '0')`. Limit: `cfg.max_msgs_per_hour ?? 30`. If count >= limit, return `new Response(JSON.stringify({ error: 'rate limit exceeded' }), { status: 429, headers: JSON_HEADERS })`. Otherwise, increment: `await env.STATE.put(ratelimitKey, String(count + 1), { expirationTtl: 3600 })` BEFORE proceeding to the Anthropic call.","fileReference":"src/routes/chat.ts","verification":"grep 'ratelimit:' src/routes/chat.ts && grep '429' src/routes/chat.ts"},{"stepNumber":6,"instruction":"In src/routes/chat.ts, change the `flush()` method in the TransformStream to use `ctx.waitUntil` instead of `await` for the KV spend write. The flush body should build a spend-write promise and call `ctx.waitUntil(promise.catch(() => {}))` then return synchronously (no await). This decouples the visitor's streamed response from KV write success. The existing try/catch inside flush can be removed since the .catch(() => {}) handles errors.","fileReference":"src/routes/chat.ts","verification":"grep 'ctx.waitUntil' src/routes/chat.ts"},{"stepNumber":7,"instruction":"Create src/__tests__/chat/chat-abuse.test.ts implementing Acceptance Tests 7, 8, 9 from spec §12. Structure: (a) Test 7 — three sub-cases with bot UAs (curl/7.0, empty string padded to <10 chars, python-requests/2.0) → assert 403 each time, anthropic mock receives 0 calls; (b) Test 8 — seed config with max_msgs_per_hour=3, send 3 requests from a fake cf-connecting-ip → assert 200 each, send 4th → assert 429, verify mock received exactly 3 calls, delete the ratelimit KV key, send again → assert 200; (c) Test 9 — seed daily_budget_usd=1, pre-seed spend:<today>=1.05 in KV, POST /chat → assert 503 and mock 0 calls, set spend:<today>=0, POST /chat → assert 200; (d) KV failure test — mock STATE.put to throw inside flush, POST /chat → assert 200 with SSE body. Use same harness pattern as src/__tests__/chat/acceptance-test-6.test.ts: import from cloudflare:test, use env, createExecutionContext, fetchMock, waitOnExecutionContext, seed config via STATE.put.","fileReference":"src/__tests__/chat/chat-abuse.test.ts","verification":"pnpm vitest run src/__tests__/chat/chat-abuse.test.ts 2>&1 | grep -E 'passed|failed'"}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:contains src/routes/chat.ts \"ctx.waitUntil\""},{"workspaceId":"primary","command":"fs:contains src/routes/chat.ts \"413\""},{"workspaceId":"primary","command":"fs:contains src/routes/chat.ts \"403\""},{"workspaceId":"primary","command":"fs:contains src/routes/chat.ts \"503\""},{"workspaceId":"primary","command":"fs:contains src/routes/chat.ts \"429\""},{"workspaceId":"primary","command":"fs:exists src/__tests__/chat/chat-abuse.test.ts"}],"functional":[{"workspaceId":"primary","command":"pnpm build 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm typecheck 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm vitest run src/__tests__/chat/chat-abuse.test.ts 2>&1 | grep -E 'passed|failed'"},{"workspaceId":"primary","command":"pnpm vitest run 2>&1 | grep 'Tests' | grep -E '[0-9]+ passed'"}]}}],"riskFlags":["RF1: ctx.waitUntil inside TransformStream flush — the flush callback receives no arguments and ctx must be captured in closure from the outer handlePostChat scope. Verify ctx is in scope when flush() is defined, not a stale reference.","RF2: 100KB body size guard without Content-Length header — Workers may not always set Content-Length for streamed POSTs. The implementation must handle the fallback path (read body as text, check length, re-parse JSON) without double-consuming a ReadableStream; use text() once and JSON.parse the string."],"confidence":0.91}