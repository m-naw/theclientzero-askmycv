# Sprint Plan: Follow-up: 1 unmet criteria

**Sprint ID:** ITR-1779184809824-followup-1
**Spec:** [2026-05-18-documentation-suite-and-license-attribution-clause.md](docs/specs/auto/2026-05-18-documentation-suite-and-license-attribution-clause.md)
**Confidence:** 0.93

## Context

G6 documentation suite — follow-up sprint to fix 4 URLs with trailing commas in PREFLIGHT.md that cause the grep-based cross-reference check to miss them in SOURCES.md

## Risk flags

- RF1: The `grep -hoE 'https?://[^ )"<>]+` regex stops at `)` so markdown link syntax `[text](URL)` correctly terminates URL capture — verified by regex analysis. Residual risk: if any other bare URL+comma patterns exist in the three files that were not detected by the Haiku scan, they would remain missing. The functional verification (verbatim done-when command) catches any such residual cases.

## Task T-1: Fix trailing-comma URL extraction in PREFLIGHT.md

**Type:** docs (modelTier: 2)

**Files to modify:**
- docs/PREFLIGHT.md (workspace: primary)

### Step 1

Read docs/PREFLIGHT.md to see the exact current content of lines 39-65 (the section containing the four problematic bare URLs).

**File reference:** docs/PREFLIGHT.md
**Verification:** Confirm the four lines are visible: line ~41 containing `https://github.com/m-naw/theclientzero-askmycv,`, line ~49 containing `https://console.anthropic.com/settings/keys,`, line ~53 containing `https://console.anthropic.com/settings/billing,`, line ~61 containing `https://one.cloudflare.com/,`.

### Step 2

On PREFLIGHT.md line ~41, change the bare URL to markdown link syntax. Replace `https://github.com/m-naw/theclientzero-askmycv,` (mid-sentence) with `[theclientzero-askmycv](https://github.com/m-naw/theclientzero-askmycv),`. The full line should read: `  On [theclientzero-askmycv](https://github.com/m-naw/theclientzero-askmycv), click **Fork**, then select your account.`

**File reference:** docs/PREFLIGHT.md
**Verification:** grep -n 'theclientzero-askmycv' docs/PREFLIGHT.md should show the markdown link form with no bare trailing comma after the URL.

### Step 3

On PREFLIGHT.md line ~49, change the bare URL to markdown link syntax. Replace `In https://console.anthropic.com/settings/keys,` with `In [API Keys](https://console.anthropic.com/settings/keys),`. The full line should read: `  In [API Keys](https://console.anthropic.com/settings/keys), click **Create key**.`

**File reference:** docs/PREFLIGHT.md
**Verification:** grep -n 'settings/keys' docs/PREFLIGHT.md shows the markdown link form.

### Step 4

On PREFLIGHT.md line ~53, change the bare URL to markdown link syntax. Replace `In https://console.anthropic.com/settings/billing,` with `In [Billing](https://console.anthropic.com/settings/billing),`. The full line should read: `  In [Billing](https://console.anthropic.com/settings/billing), confirm a payment method is attached and has available credit.`

**File reference:** docs/PREFLIGHT.md
**Verification:** grep -n 'settings/billing' docs/PREFLIGHT.md shows the markdown link form.

### Step 5

On PREFLIGHT.md line ~61, change the bare URL to markdown link syntax. Replace `In https://one.cloudflare.com/,` with `In [one.cloudflare.com](https://one.cloudflare.com/),`. The full line should read: `  In [one.cloudflare.com](https://one.cloudflare.com/), complete the Zero Trust onboarding to set a team subdomain.`

**File reference:** docs/PREFLIGHT.md
**Verification:** grep -n 'one.cloudflare.com' docs/PREFLIGHT.md shows the markdown link form.

### Step 6

Run the verbatim done-when check to confirm miss=0: `miss=0; for u in $(grep -hoE 'https?://[^ )"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF "$u" docs/SOURCES.md || miss=$((miss+1)); done; echo "miss=$miss"; test $miss -eq 0`

**File reference:** docs/PREFLIGHT.md
**Verification:** Command exits 0 and prints miss=0.

### Task verification

**Structural:**
- `grep -c 'https://github.com/m-naw/theclientzero-askmycv,' docs/PREFLIGHT.md | xargs test 0 -eq` (ws: primary)
- `grep -c 'https://console.anthropic.com/settings/keys,' docs/PREFLIGHT.md | xargs test 0 -eq` (ws: primary)
- `grep -c 'https://console.anthropic.com/settings/billing,' docs/PREFLIGHT.md | xargs test 0 -eq` (ws: primary)
- `grep -c 'https://one.cloudflare.com/,' docs/PREFLIGHT.md | xargs test 0 -eq` (ws: primary)

**Functional:**
- `miss=0; for u in $(grep -hoE 'https?://[^ )"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF "$u" docs/SOURCES.md || miss=$((miss+1)); done; test $miss -eq 0` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779184809824-followup-1",
  "sprintName": "Follow-up: 1 unmet criteria",
  "specPath": "docs/specs/auto/2026-05-18-documentation-suite-and-license-attribution-clause.md",
  "phaseContext": "G6 documentation suite — follow-up sprint to fix 4 URLs with trailing commas in PREFLIGHT.md that cause the grep-based cross-reference check to miss them in SOURCES.md",
  "tasks": [
    {
      "id": "T-1",
      "title": "Fix trailing-comma URL extraction in PREFLIGHT.md",
      "type": "docs",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "docs/PREFLIGHT.md"
          }
        ],
        "create": []
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read docs/PREFLIGHT.md to see the exact current content of lines 39-65 (the section containing the four problematic bare URLs).",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "Confirm the four lines are visible: line ~41 containing `https://github.com/m-naw/theclientzero-askmycv,`, line ~49 containing `https://console.anthropic.com/settings/keys,`, line ~53 containing `https://console.anthropic.com/settings/billing,`, line ~61 containing `https://one.cloudflare.com/,`."
        },
        {
          "stepNumber": 2,
          "instruction": "On PREFLIGHT.md line ~41, change the bare URL to markdown link syntax. Replace `https://github.com/m-naw/theclientzero-askmycv,` (mid-sentence) with `[theclientzero-askmycv](https://github.com/m-naw/theclientzero-askmycv),`. The full line should read: `  On [theclientzero-askmycv](https://github.com/m-naw/theclientzero-askmycv), click **Fork**, then select your account.`",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "grep -n 'theclientzero-askmycv' docs/PREFLIGHT.md should show the markdown link form with no bare trailing comma after the URL."
        },
        {
          "stepNumber": 3,
          "instruction": "On PREFLIGHT.md line ~49, change the bare URL to markdown link syntax. Replace `In https://console.anthropic.com/settings/keys,` with `In [API Keys](https://console.anthropic.com/settings/keys),`. The full line should read: `  In [API Keys](https://console.anthropic.com/settings/keys), click **Create key**.`",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "grep -n 'settings/keys' docs/PREFLIGHT.md shows the markdown link form."
        },
        {
          "stepNumber": 4,
          "instruction": "On PREFLIGHT.md line ~53, change the bare URL to markdown link syntax. Replace `In https://console.anthropic.com/settings/billing,` with `In [Billing](https://console.anthropic.com/settings/billing),`. The full line should read: `  In [Billing](https://console.anthropic.com/settings/billing), confirm a payment method is attached and has available credit.`",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "grep -n 'settings/billing' docs/PREFLIGHT.md shows the markdown link form."
        },
        {
          "stepNumber": 5,
          "instruction": "On PREFLIGHT.md line ~61, change the bare URL to markdown link syntax. Replace `In https://one.cloudflare.com/,` with `In [one.cloudflare.com](https://one.cloudflare.com/),`. The full line should read: `  In [one.cloudflare.com](https://one.cloudflare.com/), complete the Zero Trust onboarding to set a team subdomain.`",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "grep -n 'one.cloudflare.com' docs/PREFLIGHT.md shows the markdown link form."
        },
        {
          "stepNumber": 6,
          "instruction": "Run the verbatim done-when check to confirm miss=0: `miss=0; for u in $(grep -hoE 'https?://[^ )\"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF \"$u\" docs/SOURCES.md || miss=$((miss+1)); done; echo \"miss=$miss\"; test $miss -eq 0`",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "Command exits 0 and prints miss=0."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -c 'https://github.com/m-naw/theclientzero-askmycv,' docs/PREFLIGHT.md | xargs test 0 -eq"
          },
          {
            "workspaceId": "primary",
            "command": "grep -c 'https://console.anthropic.com/settings/keys,' docs/PREFLIGHT.md | xargs test 0 -eq"
          },
          {
            "workspaceId": "primary",
            "command": "grep -c 'https://console.anthropic.com/settings/billing,' docs/PREFLIGHT.md | xargs test 0 -eq"
          },
          {
            "workspaceId": "primary",
            "command": "grep -c 'https://one.cloudflare.com/,' docs/PREFLIGHT.md | xargs test 0 -eq"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "miss=0; for u in $(grep -hoE 'https?://[^ )\"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF \"$u\" docs/SOURCES.md || miss=$((miss+1)); done; test $miss -eq 0"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: The `grep -hoE 'https?://[^ )\"<>]+` regex stops at `)` so markdown link syntax `[text](URL)` correctly terminates URL capture — verified by regex analysis. Residual risk: if any other bare URL+comma patterns exist in the three files that were not detected by the Haiku scan, they would remain missing. The functional verification (verbatim done-when command) catches any such residual cases."
  ],
  "confidence": 0.93
}
```
