# Sprint Plan: Fix Case Mismatch, Literal Proximity, and Test Alignment

**Sprint ID:** ITR-1779071166852-sprint-1
**Spec:** [2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md](docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md)
**Confidence:** 0.9

## Context

Goal G3 — Admin password-primary auth, panel additions, setup window tightening. This sprint closes 3 remaining UNMET criteria (C17, C20, C24) by fixing precise source-level defects identified in the previous iteration.

## Risk flags

- RF1: Spec notes 'possible additional smoke test failure (expects 403, gets 404)' beyond the 4 identified — executor must run pnpm test before any edits to get the full failure list, then fix all failures
- RF2: The 50_000 proximity regex /(50_?000|50000)[\s\S]{0,80}cv_?markdown/ requires 50_000 to appear BEFORE cv_markdown in source. The planned const CV_MAX = 50_000; // cv_markdown comment approach satisfies this, but the executor must verify with the regex tool before moving on
- RF3: Rewriting admin.test.ts to use access_email='' removes two-layer JWT denial coverage from this file — acceptable only because src/__tests__/admin/admin.test.ts already tests 'access_email set + only session → 403'; if that test file does NOT cover JWT denial reasons (signature_invalid, aud_mismatch, etc.), add a note in the PR that coverage gap exists

## Task T-1: Fix Danger zone case, 50_000 literal proximity, and SETUP_WINDOW_MS test value

**Type:** code (modelTier: 2)

**Files to modify:**
- src/views/admin-form.ts (workspace: primary)
- src/routes/setup.ts (workspace: primary)
- src/__tests__/state-machine/machine.test.ts (workspace: primary)

### Step 1

In src/views/admin-form.ts line 31, change 'Danger Zone' to 'Danger zone' (lowercase z). The line currently reads: `<h2 style="color: var(--color-error);">Danger Zone</h2>`. Change only the Z to z. Do not alter any other attribute, text, or surrounding structure.

**File reference:** src/views/admin-form.ts
**Verification:** grep 'Danger zone' src/views/admin-form.ts exits 0; grep 'Danger Zone' src/views/admin-form.ts exits 1 (no uppercase Z remains)

### Step 2

In src/routes/setup.ts, replace the cv_markdown length validation so the numeric literal 50_000 appears BEFORE the string 'cv_markdown' within 80 source characters. The current code (lines 130-137) is:
```
const cv = parsed.cv_markdown ?? "";
if (cv.length < CV_MIN_LENGTH || cv.length > CV_MAX_LENGTH) {
  return errorResponse(400, `cv_markdown must be between ${CV_MIN_LENGTH} and ${CV_MAX_LENGTH} characters`, "cv_markdown");
}
```
Replace it with:
```
const CV_MAX = 50_000; // cv_markdown maximum character limit
const cv = parsed.cv_markdown ?? "";
if (cv.length < CV_MIN_LENGTH || cv.length > CV_MAX) {
  return errorResponse(400, "cv_markdown must be between 200 and 50,000 characters", "cv_markdown");
}
```
This puts '50_000' at column ~14 and 'cv_markdown' at column ~23 of the same comment line — distance ~9 chars, well within 80. After this change, check whether CV_MAX_LENGTH is still referenced elsewhere in the file. If the only remaining use is the import declaration, remove CV_MAX_LENGTH from the import list at the top of the file to avoid an unused-import TypeScript error.

**File reference:** src/routes/setup.ts
**Verification:** grep -P '50_000' src/routes/setup.ts exits 0; node -e "const src=require('fs').readFileSync('src/routes/setup.ts','utf8'); const m=src.match(/50_?000([\/\s\S]{0,80})cv_?markdown/); process.exit(m?0:1)" exits 0 confirming 50_000 before cv_markdown within 80 chars; pnpm typecheck exits 0

### Step 3

In src/__tests__/state-machine/machine.test.ts lines 303-306, update the SETUP_WINDOW_MS test. The current code is:
```
describe("SETUP_WINDOW_MS constant", () => {
  it("equals 30 minutes in milliseconds", () => {
    expect(SETUP_WINDOW_MS).toBe(30 * 60 * 1000);
  });
});
```
Change it to:
```
describe("SETUP_WINDOW_MS constant", () => {
  it("equals 600 seconds in milliseconds", () => {
    expect(SETUP_WINDOW_MS).toBe(600_000);
  });
});
```

**File reference:** src/__tests__/state-machine/machine.test.ts
**Verification:** grep -P '600_000' src/__tests__/state-machine/machine.test.ts exits 0; grep -P '30 \* 60 \* 1000' src/__tests__/state-machine/machine.test.ts exits 1

### Task verification

**Structural:**
- `grep 'Danger zone' src/views/admin-form.ts` (ws: primary)
- `grep -P '50_000' src/routes/setup.ts` (ws: primary)
- `grep -P '600_000|600000' src/__tests__/state-machine/machine.test.ts` (ws: primary)

**Functional:**
- `pnpm typecheck` (ws: primary)

## Task T-2: Fix admin.test.ts: seed session cookie, rewrite JWT-era tests for session-primary auth model

**Type:** code (modelTier: 2)

**Depends on:** T-1

**Files to modify:**
- src/test/admin.test.ts (workspace: primary)

### Step 1

Run `pnpm test 2>&1 | grep -E 'FAIL|✗|×|Error' | head -40` to confirm the exact set of failing tests before making any changes. Record all failures, not just the expected 6. If additional failures exist outside admin.test.ts and machine.test.ts, investigate and plan to fix them too before proceeding.

**File reference:** src/test/admin.test.ts
**Verification:** Output is reviewed; no unexpected failures exist beyond C17/C20/C24 defects

### Step 2

Read src/auth/session.ts to confirm the exports: signSession(payload, secret), SESSION_COOKIE_NAME, and the AdminSessionPayload type shape {sub: string, iat: number, exp: number}.

**File reference:** src/auth/session.ts
**Verification:** signSession, SESSION_COOKIE_NAME, AdminSessionPayload are all exported from src/auth/session.ts

### Step 3

Rewrite src/test/admin.test.ts to use session-primary auth. The complete set of changes:

(A) Add imports at the top:
```typescript
import { signSession, SESSION_COOKIE_NAME } from "../auth/session";
```

(B) Update clearKv() to also delete cookie_signing_secret:
```typescript
async function clearKv(): Promise<void> {
  const kv = getEnv().STATE;
  for (const key of ["config", "setup_window_start", TEST_JWKS_KV_KEY, "cookie_signing_secret"]) {
    await kv.delete(key);
  }
}
```

(C) Add a session cookie seeding helper AFTER the clearKv function:
```typescript
const TEST_SIGNING_SECRET = "test-signing-secret-for-admin-tests-32b";

async function seedSessionCookie(): Promise<string> {
  await getEnv().STATE.put("cookie_signing_secret", TEST_SIGNING_SECRET);
  const nowSeconds = Math.floor(Date.now() / 1000);
  const token = await signSession(
    { sub: "admin", iat: nowSeconds, exp: nowSeconds + 86400 },
    TEST_SIGNING_SECRET,
  );
  return `${SESSION_COOKIE_NAME}=${token}`;
}
```

(D) Update baseConfig() to use access_email: "" so the JWT layer is skipped (JWT-specific denial tests in this file would always check session-layer only; two-layer JWT denial is covered in src/__tests__/admin/admin.test.ts):
```typescript
function baseConfig(overrides: Partial<StoredConfig> = {}): StoredConfig {
  return {
    display_name: "Test Owner",
    headline: "Full-stack engineer · Remote",
    cv_markdown: "# Test Owner\n\n## Experience\n\n" + "Worked on many projects. ".repeat(12),
    anthropic_api_key: "sk-ant-admin-test-secret-key",
    daily_budget_usd: 3,
    access_email: "",
    access_aud: "admin-test-aud-123",
    access_team_domain: "admintest.cloudflareaccess.com",
    setup_timestamp: Date.now() - 5_000,
    ...overrides,
  };
}
```

(E) Update adminGetRequest to accept an optional session cookie:
```typescript
function adminGetRequest(jwt: string | null, sessionCookie?: string): Request {
  const headers: Record<string, string> = {};
  if (jwt) headers["cf-access-jwt-assertion"] = jwt;
  if (sessionCookie) headers["Cookie"] = sessionCookie;
  return new Request("https://example.test/admin", { method: "GET", headers });
}
```

(F) Rewrite the test suite body. Replace all 6 existing tests with the following 6 tests that exercise the session-primary auth model:

1. Success test — valid session cookie returns 200 with admin form:
```typescript
it("valid session cookie returns 200 with admin form pre-filled from config and no API key exposed", async () => {
  const cfg = baseConfig();
  await seedConfig(cfg);
  const cookie = await seedSessionCookie();

  const res = await runFetch(adminGetRequest(null, cookie));
  expect(res.status).toBe(200);
  const html = await res.text();
  expect(html).toContain('name="cv_markdown"');
  expect(html).toContain(cfg.display_name);
  expect(html).toContain(cfg.headline);
  expect(html).not.toContain("sk-ant-admin-test-secret-key");
  expect(html).not.toContain("sk-ant");
});
```

2. No session cookie returns 401:
```typescript
it("missing session cookie returns 401", async () => {
  await seedConfig(baseConfig());

  const res = await runFetch(adminGetRequest(null));
  expect(res.status).toBe(401);
});
```

3. Invalid session cookie (bad signature) returns 401:
```typescript
it("tampered session cookie returns 401", async () => {
  await seedConfig(baseConfig());
  await seedSessionCookie(); // seeds cookie_signing_secret in KV
  const fakeSession = `${SESSION_COOKIE_NAME}=notavalidtoken.invalidsig`;

  const res = await runFetch(adminGetRequest(null, fakeSession));
  expect(res.status).toBe(401);
});
```

4. Session cookie present but no config (404 — not yet configured):
```typescript
it("returns 404 when not yet configured even with valid session", async () => {
  const cookie = await seedSessionCookie();
  // no seedConfig call
  const res = await runFetch(adminGetRequest(null, cookie));
  expect(res.status).toBe(404);
});
```

5. Valid session + access_email set but no JWT returns 403:
```typescript
it("valid session but missing JWT returns 403 when access_email is configured", async () => {
  const cfg = baseConfig({ access_email: "owner@admin-test.example" });
  await seedConfig(cfg);
  const cookie = await seedSessionCookie();

  const res = await runFetch(adminGetRequest(null, cookie));
  expect(res.status).toBe(403);
});
```

6. API key never appears in admin HTML (extra hardening check):
```typescript
it("API key is never exposed in the admin HTML response", async () => {
  const cfg = baseConfig();
  await seedConfig(cfg);
  const cookie = await seedSessionCookie();

  const res = await runFetch(adminGetRequest(null, cookie));
  expect(res.status).toBe(200);
  const html = await res.text();
  expect(html).not.toContain(cfg.anthropic_api_key);
});
```

**File reference:** src/test/admin.test.ts
**Verification:** File compiles without TypeScript errors; all 6 tests exist and cover the scenarios described

### Step 4

Run `pnpm typecheck` and fix any TypeScript errors in src/test/admin.test.ts before running the full test suite. Common issues: unused imports (remove them), type mismatches in seedConfig if StoredConfig requires fields not in baseConfig overrides.

**File reference:** src/test/admin.test.ts
**Verification:** pnpm typecheck exits 0

### Step 5

Run the full test suite: `pnpm test`. All tests must pass. If any test outside src/test/admin.test.ts fails, investigate and fix before claiming this task done. Do not commit until pnpm test exits 0.

**File reference:** src/test/admin.test.ts
**Verification:** pnpm test exits 0

### Step 6

Run pnpm build to confirm the production bundle compiles without errors.

**File reference:** src/worker.ts
**Verification:** pnpm build exits 0

### Task verification

**Structural:**
- `grep 'seedSessionCookie' src/test/admin.test.ts` (ws: primary)
- `grep 'SESSION_COOKIE_NAME' src/test/admin.test.ts` (ws: primary)
- `grep "access_email: \"\"" src/test/admin.test.ts` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)
- `pnpm build` (ws: primary)
- `pnpm typecheck` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779071166852-sprint-1",
  "sprintName": "Fix Case Mismatch, Literal Proximity, and Test Alignment",
  "specPath": "docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md",
  "phaseContext": "Goal G3 — Admin password-primary auth, panel additions, setup window tightening. This sprint closes 3 remaining UNMET criteria (C17, C20, C24) by fixing precise source-level defects identified in the previous iteration.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Fix Danger zone case, 50_000 literal proximity, and SETUP_WINDOW_MS test value",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/views/admin-form.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/__tests__/state-machine/machine.test.ts"
          }
        ],
        "create": []
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "In src/views/admin-form.ts line 31, change 'Danger Zone' to 'Danger zone' (lowercase z). The line currently reads: `<h2 style=\"color: var(--color-error);\">Danger Zone</h2>`. Change only the Z to z. Do not alter any other attribute, text, or surrounding structure.",
          "fileReference": "src/views/admin-form.ts",
          "verification": "grep 'Danger zone' src/views/admin-form.ts exits 0; grep 'Danger Zone' src/views/admin-form.ts exits 1 (no uppercase Z remains)"
        },
        {
          "stepNumber": 2,
          "instruction": "In src/routes/setup.ts, replace the cv_markdown length validation so the numeric literal 50_000 appears BEFORE the string 'cv_markdown' within 80 source characters. The current code (lines 130-137) is:\n```\nconst cv = parsed.cv_markdown ?? \"\";\nif (cv.length < CV_MIN_LENGTH || cv.length > CV_MAX_LENGTH) {\n  return errorResponse(400, `cv_markdown must be between ${CV_MIN_LENGTH} and ${CV_MAX_LENGTH} characters`, \"cv_markdown\");\n}\n```\nReplace it with:\n```\nconst CV_MAX = 50_000; // cv_markdown maximum character limit\nconst cv = parsed.cv_markdown ?? \"\";\nif (cv.length < CV_MIN_LENGTH || cv.length > CV_MAX) {\n  return errorResponse(400, \"cv_markdown must be between 200 and 50,000 characters\", \"cv_markdown\");\n}\n```\nThis puts '50_000' at column ~14 and 'cv_markdown' at column ~23 of the same comment line — distance ~9 chars, well within 80. After this change, check whether CV_MAX_LENGTH is still referenced elsewhere in the file. If the only remaining use is the import declaration, remove CV_MAX_LENGTH from the import list at the top of the file to avoid an unused-import TypeScript error.",
          "fileReference": "src/routes/setup.ts",
          "verification": "grep -P '50_000' src/routes/setup.ts exits 0; node -e \"const src=require('fs').readFileSync('src/routes/setup.ts','utf8'); const m=src.match(/50_?000([\\/\\s\\S]{0,80})cv_?markdown/); process.exit(m?0:1)\" exits 0 confirming 50_000 before cv_markdown within 80 chars; pnpm typecheck exits 0"
        },
        {
          "stepNumber": 3,
          "instruction": "In src/__tests__/state-machine/machine.test.ts lines 303-306, update the SETUP_WINDOW_MS test. The current code is:\n```\ndescribe(\"SETUP_WINDOW_MS constant\", () => {\n  it(\"equals 30 minutes in milliseconds\", () => {\n    expect(SETUP_WINDOW_MS).toBe(30 * 60 * 1000);\n  });\n});\n```\nChange it to:\n```\ndescribe(\"SETUP_WINDOW_MS constant\", () => {\n  it(\"equals 600 seconds in milliseconds\", () => {\n    expect(SETUP_WINDOW_MS).toBe(600_000);\n  });\n});\n```",
          "fileReference": "src/__tests__/state-machine/machine.test.ts",
          "verification": "grep -P '600_000' src/__tests__/state-machine/machine.test.ts exits 0; grep -P '30 \\* 60 \\* 1000' src/__tests__/state-machine/machine.test.ts exits 1"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep 'Danger zone' src/views/admin-form.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -P '50_000' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -P '600_000|600000' src/__tests__/state-machine/machine.test.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm typecheck"
          }
        ]
      }
    },
    {
      "id": "T-2",
      "title": "Fix admin.test.ts: seed session cookie, rewrite JWT-era tests for session-primary auth model",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/test/admin.test.ts"
          }
        ],
        "create": []
      },
      "dependencies": [
        "T-1"
      ],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run `pnpm test 2>&1 | grep -E 'FAIL|✗|×|Error' | head -40` to confirm the exact set of failing tests before making any changes. Record all failures, not just the expected 6. If additional failures exist outside admin.test.ts and machine.test.ts, investigate and plan to fix them too before proceeding.",
          "fileReference": "src/test/admin.test.ts",
          "verification": "Output is reviewed; no unexpected failures exist beyond C17/C20/C24 defects"
        },
        {
          "stepNumber": 2,
          "instruction": "Read src/auth/session.ts to confirm the exports: signSession(payload, secret), SESSION_COOKIE_NAME, and the AdminSessionPayload type shape {sub: string, iat: number, exp: number}.",
          "fileReference": "src/auth/session.ts",
          "verification": "signSession, SESSION_COOKIE_NAME, AdminSessionPayload are all exported from src/auth/session.ts"
        },
        {
          "stepNumber": 3,
          "instruction": "Rewrite src/test/admin.test.ts to use session-primary auth. The complete set of changes:\n\n(A) Add imports at the top:\n```typescript\nimport { signSession, SESSION_COOKIE_NAME } from \"../auth/session\";\n```\n\n(B) Update clearKv() to also delete cookie_signing_secret:\n```typescript\nasync function clearKv(): Promise<void> {\n  const kv = getEnv().STATE;\n  for (const key of [\"config\", \"setup_window_start\", TEST_JWKS_KV_KEY, \"cookie_signing_secret\"]) {\n    await kv.delete(key);\n  }\n}\n```\n\n(C) Add a session cookie seeding helper AFTER the clearKv function:\n```typescript\nconst TEST_SIGNING_SECRET = \"test-signing-secret-for-admin-tests-32b\";\n\nasync function seedSessionCookie(): Promise<string> {\n  await getEnv().STATE.put(\"cookie_signing_secret\", TEST_SIGNING_SECRET);\n  const nowSeconds = Math.floor(Date.now() / 1000);\n  const token = await signSession(\n    { sub: \"admin\", iat: nowSeconds, exp: nowSeconds + 86400 },\n    TEST_SIGNING_SECRET,\n  );\n  return `${SESSION_COOKIE_NAME}=${token}`;\n}\n```\n\n(D) Update baseConfig() to use access_email: \"\" so the JWT layer is skipped (JWT-specific denial tests in this file would always check session-layer only; two-layer JWT denial is covered in src/__tests__/admin/admin.test.ts):\n```typescript\nfunction baseConfig(overrides: Partial<StoredConfig> = {}): StoredConfig {\n  return {\n    display_name: \"Test Owner\",\n    headline: \"Full-stack engineer · Remote\",\n    cv_markdown: \"# Test Owner\\n\\n## Experience\\n\\n\" + \"Worked on many projects. \".repeat(12),\n    anthropic_api_key: \"sk-ant-admin-test-secret-key\",\n    daily_budget_usd: 3,\n    access_email: \"\",\n    access_aud: \"admin-test-aud-123\",\n    access_team_domain: \"admintest.cloudflareaccess.com\",\n    setup_timestamp: Date.now() - 5_000,\n    ...overrides,\n  };\n}\n```\n\n(E) Update adminGetRequest to accept an optional session cookie:\n```typescript\nfunction adminGetRequest(jwt: string | null, sessionCookie?: string): Request {\n  const headers: Record<string, string> = {};\n  if (jwt) headers[\"cf-access-jwt-assertion\"] = jwt;\n  if (sessionCookie) headers[\"Cookie\"] = sessionCookie;\n  return new Request(\"https://example.test/admin\", { method: \"GET\", headers });\n}\n```\n\n(F) Rewrite the test suite body. Replace all 6 existing tests with the following 6 tests that exercise the session-primary auth model:\n\n1. Success test — valid session cookie returns 200 with admin form:\n```typescript\nit(\"valid session cookie returns 200 with admin form pre-filled from config and no API key exposed\", async () => {\n  const cfg = baseConfig();\n  await seedConfig(cfg);\n  const cookie = await seedSessionCookie();\n\n  const res = await runFetch(adminGetRequest(null, cookie));\n  expect(res.status).toBe(200);\n  const html = await res.text();\n  expect(html).toContain('name=\"cv_markdown\"');\n  expect(html).toContain(cfg.display_name);\n  expect(html).toContain(cfg.headline);\n  expect(html).not.toContain(\"sk-ant-admin-test-secret-key\");\n  expect(html).not.toContain(\"sk-ant\");\n});\n```\n\n2. No session cookie returns 401:\n```typescript\nit(\"missing session cookie returns 401\", async () => {\n  await seedConfig(baseConfig());\n\n  const res = await runFetch(adminGetRequest(null));\n  expect(res.status).toBe(401);\n});\n```\n\n3. Invalid session cookie (bad signature) returns 401:\n```typescript\nit(\"tampered session cookie returns 401\", async () => {\n  await seedConfig(baseConfig());\n  await seedSessionCookie(); // seeds cookie_signing_secret in KV\n  const fakeSession = `${SESSION_COOKIE_NAME}=notavalidtoken.invalidsig`;\n\n  const res = await runFetch(adminGetRequest(null, fakeSession));\n  expect(res.status).toBe(401);\n});\n```\n\n4. Session cookie present but no config (404 — not yet configured):\n```typescript\nit(\"returns 404 when not yet configured even with valid session\", async () => {\n  const cookie = await seedSessionCookie();\n  // no seedConfig call\n  const res = await runFetch(adminGetRequest(null, cookie));\n  expect(res.status).toBe(404);\n});\n```\n\n5. Valid session + access_email set but no JWT returns 403:\n```typescript\nit(\"valid session but missing JWT returns 403 when access_email is configured\", async () => {\n  const cfg = baseConfig({ access_email: \"owner@admin-test.example\" });\n  await seedConfig(cfg);\n  const cookie = await seedSessionCookie();\n\n  const res = await runFetch(adminGetRequest(null, cookie));\n  expect(res.status).toBe(403);\n});\n```\n\n6. API key never appears in admin HTML (extra hardening check):\n```typescript\nit(\"API key is never exposed in the admin HTML response\", async () => {\n  const cfg = baseConfig();\n  await seedConfig(cfg);\n  const cookie = await seedSessionCookie();\n\n  const res = await runFetch(adminGetRequest(null, cookie));\n  expect(res.status).toBe(200);\n  const html = await res.text();\n  expect(html).not.toContain(cfg.anthropic_api_key);\n});\n```",
          "fileReference": "src/test/admin.test.ts",
          "verification": "File compiles without TypeScript errors; all 6 tests exist and cover the scenarios described"
        },
        {
          "stepNumber": 4,
          "instruction": "Run `pnpm typecheck` and fix any TypeScript errors in src/test/admin.test.ts before running the full test suite. Common issues: unused imports (remove them), type mismatches in seedConfig if StoredConfig requires fields not in baseConfig overrides.",
          "fileReference": "src/test/admin.test.ts",
          "verification": "pnpm typecheck exits 0"
        },
        {
          "stepNumber": 5,
          "instruction": "Run the full test suite: `pnpm test`. All tests must pass. If any test outside src/test/admin.test.ts fails, investigate and fix before claiming this task done. Do not commit until pnpm test exits 0.",
          "fileReference": "src/test/admin.test.ts",
          "verification": "pnpm test exits 0"
        },
        {
          "stepNumber": 6,
          "instruction": "Run pnpm build to confirm the production bundle compiles without errors.",
          "fileReference": "src/worker.ts",
          "verification": "pnpm build exits 0"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep 'seedSessionCookie' src/test/admin.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'SESSION_COOKIE_NAME' src/test/admin.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep \"access_email: \\\"\\\"\" src/test/admin.test.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm typecheck"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: Spec notes 'possible additional smoke test failure (expects 403, gets 404)' beyond the 4 identified — executor must run pnpm test before any edits to get the full failure list, then fix all failures",
    "RF2: The 50_000 proximity regex /(50_?000|50000)[\\s\\S]{0,80}cv_?markdown/ requires 50_000 to appear BEFORE cv_markdown in source. The planned const CV_MAX = 50_000; // cv_markdown comment approach satisfies this, but the executor must verify with the regex tool before moving on",
    "RF3: Rewriting admin.test.ts to use access_email='' removes two-layer JWT denial coverage from this file — acceptable only because src/__tests__/admin/admin.test.ts already tests 'access_email set + only session → 403'; if that test file does NOT cover JWT denial reasons (signature_invalid, aud_mismatch, etc.), add a note in the PR that coverage gap exists"
  ],
  "confidence": 0.9
}
```
