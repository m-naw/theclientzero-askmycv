# Sprint Plan: Documentation Suite

**Sprint ID:** ITR-1779184809824-sprint-1
**Spec:** [2026-05-18-documentation-suite-and-license-attribution-clause.md](docs/specs/auto/2026-05-18-documentation-suite-and-license-attribution-clause.md)
**Confidence:** 0.88

## Context

G6 final sprint: authoring the full operational documentation set (F20/F21). All code goals complete; this sprint delivers prose docs, README preflight section, and citation index only.

## Risk flags

- RF1: URL-mirror check may fail if operational docs use URL fragments or trailing punctuation that alter the exact URL string — executor must run the mirror-check script (step 9) before declaring done and patch SOURCES.md if any miss is found.

## Task T-1: Author all operational docs, SOURCES.md, SPEC_CORRECTIONS.md, and README 'Before you start' section

**Type:** docs (modelTier: 2)

**Files to create:**
- docs/UNINSTALL.md (workspace: primary)
- docs/SECURITY.md (workspace: primary)
- docs/ACCESS_FALLBACK.md (workspace: primary)
- docs/TROUBLESHOOTING.md (workspace: primary)
- docs/PREFLIGHT.md (workspace: primary)
- docs/SPEC_CORRECTIONS.md (workspace: primary)
- docs/SOURCES.md (workspace: primary)

**Files to modify:**
- README.md (workspace: primary)

### Step 1

Read README.md to identify the exact insert point for the 'Before you start' section. The section must be inserted before the '## Deploy to Cloudflare' heading (currently line 5). Read the file first to confirm current structure.

**File reference:** README.md
**Verification:** grep -n 'Deploy to Cloudflare' README.md | head -3

### Step 2

Create docs/UNINSTALL.md with three scenarios (Pause, Reset, Permanent Delete). Each scenario must have exact navigation steps with no vague phrases. Forbidden phrases: 'go to the dashboard', 'find the setting', 'click somewhere', 'refer to the documentation'. Required URLs: https://dash.cloudflare.com/ (Pause/Reset), https://developers.cloudflare.com/workers/configuration/delete-a-worker/ (Permanent Delete), https://github.com/settings/repositories (Permanent Delete), https://console.anthropic.com/settings/keys (Permanent Delete). End the file with: 'Last verified: 2026-05-18'. Exact scenario steps from spec:
- Pause: navigate to https://dash.cloudflare.com/ → Workers & Pages → select the worker → Settings → toggle "Enabled" off.
- Reset: navigate to https://dash.cloudflare.com/ → Workers & Pages → KV → delete the CHAT_KV binding, then run `wrangler kv:namespace delete`.
- Permanent delete: delete Cloudflare Worker at https://developers.cloudflare.com/workers/configuration/delete-a-worker/, delete GitHub fork at https://github.com/settings/repositories, revoke Anthropic API key at https://console.anthropic.com/settings/keys.

**File reference:** docs/UNINSTALL.md
**Verification:** test -f docs/UNINSTALL.md && grep -q 'Last verified:' docs/UNINSTALL.md && ! grep -iE 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md

### Step 3

Create docs/SECURITY.md covering: (1) Password-primary auth: bcrypt cost ≥12 hash stored in KV, plaintext never persisted. (2) Optional Cloudflare Access progressive enhancement: how JWT verification is auto-detected (non-hardcoded), configured only when CF-Access-Jwt-Assertion header is present. (3) Threat model: in-scope (admin credential theft, rate-limit bypass); out-of-scope (Cloudflare infrastructure, Anthropic API key leakage via Cloudflare platform). (4) Recovery procedures: reset admin password via `wrangler kv:key delete STATE admin_password_hash` then re-run `/setup`. End the file with: 'Last verified: 2026-05-18'.

**File reference:** docs/SECURITY.md
**Verification:** test -f docs/SECURITY.md && grep -q 'Last verified:' docs/SECURITY.md && grep -q 'bcrypt' docs/SECURITY.md

### Step 4

Create docs/ACCESS_FALLBACK.md covering: (1) When CF Access is useful: shared-network deployments, team environments requiring SSO. (2) Step-by-step setup: navigate to https://one.cloudflare.com/ → Zero Trust → Access → Applications → Add an application → Self-hosted. Configure the policy to protect /admin routes. Cite https://developers.cloudflare.com/cloudflare-one/applications/configure-apps/self-hosted-apps/ as the authoritative source. (3) Verification procedure: check worker logs for CF-Access-Jwt-Assertion header presence using `wrangler tail`. End with: 'Last verified: 2026-05-18'. The URL https://developers.cloudflare.com must appear in this file.

**File reference:** docs/ACCESS_FALLBACK.md
**Verification:** test -f docs/ACCESS_FALLBACK.md && grep -q 'Last verified:' docs/ACCESS_FALLBACK.md && grep -q 'developers.cloudflare.com' docs/ACCESS_FALLBACK.md

### Step 5

Create docs/TROUBLESHOOTING.md covering exactly these 5 failure modes with exact remediation steps: (1) wrangler deploy authentication error: run `wrangler login` to re-authenticate. (2) wrangler.toml vars issue: test-only variables must not appear in [vars] block; keep test mocks in vitest config only. (3) Anthropic key rejection: verify key exists and is active at https://console.anthropic.com/settings/keys; confirm billing is active. (4) Budget cap: DAILY_SPEND_LIMIT_USD env var controls spend; default $1.00; increase via `wrangler secret put DAILY_SPEND_LIMIT_USD`. (5) 503 errors: Cloudflare Worker CPU limit exceeded; check `wrangler tail` output at https://dash.cloudflare.com/ → Workers & Pages → worker name → Logs. End with: 'Last verified: 2026-05-18'.

**File reference:** docs/TROUBLESHOOTING.md
**Verification:** test -f docs/TROUBLESHOOTING.md && grep -q 'Last verified:' docs/TROUBLESHOOTING.md && grep -q 'wrangler login' docs/TROUBLESHOOTING.md

### Step 6

Create docs/PREFLIGHT.md as a pre-deployment checklist with these exact items and URLs:
- [ ] GitHub account: https://github.com/signup
- [ ] Cloudflare account: https://dash.cloudflare.com/sign-up
- [ ] Anthropic account: https://console.anthropic.com/
- [ ] Wrangler CLI authenticated: run `wrangler login`
- [ ] Node 20+ installed
- [ ] pnpm 9+ installed
- [ ] Fork of this repo available in your GitHub account
End with: 'Last verified: 2026-05-18'. The strings 'github.com/signup' and 'dash.cloudflare.com/sign-up' must appear verbatim.

**File reference:** docs/PREFLIGHT.md
**Verification:** test -f docs/PREFLIGHT.md && grep -q 'github.com/signup' docs/PREFLIGHT.md && grep -q 'dash.cloudflare.com/sign-up' docs/PREFLIGHT.md && grep -q 'Last verified:' docs/PREFLIGHT.md

### Step 7

Create docs/SPEC_CORRECTIONS.md as a stub template. Include these template fields only (no operational content): Title, Date, Affected Spec, Correction, Reason. Add a one-line header explaining this file records corrections to spec documents. No Last verified stamp needed.

**File reference:** docs/SPEC_CORRECTIONS.md
**Verification:** test -f docs/SPEC_CORRECTIONS.md

### Step 8

Build docs/SOURCES.md as the master citation index. First, run this command to extract all URLs from operational docs: `grep -hoE 'https?://[^ )"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md | sort -u`. Then create docs/SOURCES.md with a markdown table: columns URL, Purpose, Last verified. Include every URL found by the grep. The file must contain ≥5 URLs. Every URL that appears in docs/UNINSTALL.md, docs/ACCESS_FALLBACK.md, and docs/PREFLIGHT.md must appear in SOURCES.md (URL-mirror requirement). Each entry carries 'Last verified: 2026-05-18'. Known URLs to include at minimum: https://dash.cloudflare.com/, https://developers.cloudflare.com/workers/configuration/delete-a-worker/, https://github.com/settings/repositories, https://console.anthropic.com/settings/keys, https://one.cloudflare.com/, https://developers.cloudflare.com/cloudflare-one/applications/configure-apps/self-hosted-apps/, https://github.com/signup, https://dash.cloudflare.com/sign-up, https://console.anthropic.com/.

**File reference:** docs/SOURCES.md
**Verification:** test -f docs/SOURCES.md && test $(grep -cE 'https?://' docs/SOURCES.md) -ge 5

### Step 9

Run the URL-mirror check to verify all URLs in UNINSTALL.md, ACCESS_FALLBACK.md, PREFLIGHT.md appear in SOURCES.md: `miss=0; for u in $(grep -hoE 'https?://[^ )"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF "$u" docs/SOURCES.md || { echo "MISSING: $u"; miss=$((miss+1)); }; done; echo "miss=$miss"; test $miss -eq 0`. If any URL is missing, add it to SOURCES.md before continuing.

**File reference:** docs/SOURCES.md
**Verification:** miss=0; for u in $(grep -hoE 'https?://[^ )"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF "$u" docs/SOURCES.md || miss=$((miss+1)); done; test $miss -eq 0

### Step 10

Update README.md: insert a '## Before you start' section immediately before the '## Deploy to Cloudflare' line. The section must contain an intro sentence about three required accounts, followed by these exact hyperlinks:
- [Create a GitHub account](https://github.com/signup)
- [Create a Cloudflare account](https://dash.cloudflare.com/sign-up)
- [Create an Anthropic account](https://console.anthropic.com/)
The strings 'github.com/signup', 'dash.cloudflare.com/sign-up', and 'console.anthropic.com' must all appear verbatim in README.md. The heading 'Before you start' must appear verbatim.

**File reference:** README.md
**Verification:** grep -q 'Before you start' README.md && grep -q 'github.com/signup' README.md && grep -q 'dash.cloudflare.com/sign-up' README.md && grep -q 'console.anthropic.com' README.md

### Step 11

Run the vague-phrase check to confirm no forbidden phrases appear in any authored doc or in README.md: `grep -rEi 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md README.md`. The command must return no matches (exit 1 from grep means clean). If any match is found, rewrite the offending sentence with exact navigation steps instead.

**File reference:** docs/UNINSTALL.md
**Verification:** ! grep -rEi 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md README.md

### Step 12

Confirm LICENSE already contains Section 7(b) — do NOT modify LICENSE. Run: `grep -q 'Section 7(b)' LICENSE && echo 'LICENSE OK'`. This is a read-only verification step; the criterion is pre-satisfied per codebase investigation.

**File reference:** LICENSE
**Verification:** grep -q 'Section 7(b)' LICENSE

### Task verification

**Structural:**
- `test -f docs/UNINSTALL.md && grep -q 'Last verified:' docs/UNINSTALL.md && ! grep -iE 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md` (ws: primary)
- `test -f docs/SECURITY.md && grep -q 'Last verified:' docs/SECURITY.md` (ws: primary)
- `test -f docs/ACCESS_FALLBACK.md && grep -q 'Last verified:' docs/ACCESS_FALLBACK.md && grep -q 'developers.cloudflare.com' docs/ACCESS_FALLBACK.md` (ws: primary)
- `test -f docs/TROUBLESHOOTING.md && grep -q 'Last verified:' docs/TROUBLESHOOTING.md` (ws: primary)
- `test -f docs/PREFLIGHT.md && grep -q 'github.com/signup' docs/PREFLIGHT.md && grep -q 'dash.cloudflare.com/sign-up' docs/PREFLIGHT.md && grep -q 'Last verified:' docs/PREFLIGHT.md` (ws: primary)
- `test -f docs/SPEC_CORRECTIONS.md` (ws: primary)
- `test -f docs/SOURCES.md && test $(grep -cE 'https?://' docs/SOURCES.md) -ge 5` (ws: primary)
- `miss=0; for u in $(grep -hoE 'https?://[^ )"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF "$u" docs/SOURCES.md || miss=$((miss+1)); done; test $miss -eq 0` (ws: primary)
- `grep -q 'Before you start' README.md && grep -q 'github.com/signup' README.md && grep -q 'dash.cloudflare.com/sign-up' README.md && grep -q 'console.anthropic.com' README.md` (ws: primary)
- `grep -q 'Section 7(b)' LICENSE && grep -rEi 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md README.md && exit 1 || exit 0` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779184809824-sprint-1",
  "sprintName": "Documentation Suite",
  "specPath": "docs/specs/auto/2026-05-18-documentation-suite-and-license-attribution-clause.md",
  "phaseContext": "G6 final sprint: authoring the full operational documentation set (F20/F21). All code goals complete; this sprint delivers prose docs, README preflight section, and citation index only.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Author all operational docs, SOURCES.md, SPEC_CORRECTIONS.md, and README 'Before you start' section",
      "type": "docs",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "README.md"
          }
        ],
        "create": [
          {
            "workspaceId": "primary",
            "path": "docs/UNINSTALL.md"
          },
          {
            "workspaceId": "primary",
            "path": "docs/SECURITY.md"
          },
          {
            "workspaceId": "primary",
            "path": "docs/ACCESS_FALLBACK.md"
          },
          {
            "workspaceId": "primary",
            "path": "docs/TROUBLESHOOTING.md"
          },
          {
            "workspaceId": "primary",
            "path": "docs/PREFLIGHT.md"
          },
          {
            "workspaceId": "primary",
            "path": "docs/SPEC_CORRECTIONS.md"
          },
          {
            "workspaceId": "primary",
            "path": "docs/SOURCES.md"
          }
        ]
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read README.md to identify the exact insert point for the 'Before you start' section. The section must be inserted before the '## Deploy to Cloudflare' heading (currently line 5). Read the file first to confirm current structure.",
          "fileReference": "README.md",
          "verification": "grep -n 'Deploy to Cloudflare' README.md | head -3"
        },
        {
          "stepNumber": 2,
          "instruction": "Create docs/UNINSTALL.md with three scenarios (Pause, Reset, Permanent Delete). Each scenario must have exact navigation steps with no vague phrases. Forbidden phrases: 'go to the dashboard', 'find the setting', 'click somewhere', 'refer to the documentation'. Required URLs: https://dash.cloudflare.com/ (Pause/Reset), https://developers.cloudflare.com/workers/configuration/delete-a-worker/ (Permanent Delete), https://github.com/settings/repositories (Permanent Delete), https://console.anthropic.com/settings/keys (Permanent Delete). End the file with: 'Last verified: 2026-05-18'. Exact scenario steps from spec:\n- Pause: navigate to https://dash.cloudflare.com/ → Workers & Pages → select the worker → Settings → toggle \"Enabled\" off.\n- Reset: navigate to https://dash.cloudflare.com/ → Workers & Pages → KV → delete the CHAT_KV binding, then run `wrangler kv:namespace delete`.\n- Permanent delete: delete Cloudflare Worker at https://developers.cloudflare.com/workers/configuration/delete-a-worker/, delete GitHub fork at https://github.com/settings/repositories, revoke Anthropic API key at https://console.anthropic.com/settings/keys.",
          "fileReference": "docs/UNINSTALL.md",
          "verification": "test -f docs/UNINSTALL.md && grep -q 'Last verified:' docs/UNINSTALL.md && ! grep -iE 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md"
        },
        {
          "stepNumber": 3,
          "instruction": "Create docs/SECURITY.md covering: (1) Password-primary auth: bcrypt cost ≥12 hash stored in KV, plaintext never persisted. (2) Optional Cloudflare Access progressive enhancement: how JWT verification is auto-detected (non-hardcoded), configured only when CF-Access-Jwt-Assertion header is present. (3) Threat model: in-scope (admin credential theft, rate-limit bypass); out-of-scope (Cloudflare infrastructure, Anthropic API key leakage via Cloudflare platform). (4) Recovery procedures: reset admin password via `wrangler kv:key delete STATE admin_password_hash` then re-run `/setup`. End the file with: 'Last verified: 2026-05-18'.",
          "fileReference": "docs/SECURITY.md",
          "verification": "test -f docs/SECURITY.md && grep -q 'Last verified:' docs/SECURITY.md && grep -q 'bcrypt' docs/SECURITY.md"
        },
        {
          "stepNumber": 4,
          "instruction": "Create docs/ACCESS_FALLBACK.md covering: (1) When CF Access is useful: shared-network deployments, team environments requiring SSO. (2) Step-by-step setup: navigate to https://one.cloudflare.com/ → Zero Trust → Access → Applications → Add an application → Self-hosted. Configure the policy to protect /admin routes. Cite https://developers.cloudflare.com/cloudflare-one/applications/configure-apps/self-hosted-apps/ as the authoritative source. (3) Verification procedure: check worker logs for CF-Access-Jwt-Assertion header presence using `wrangler tail`. End with: 'Last verified: 2026-05-18'. The URL https://developers.cloudflare.com must appear in this file.",
          "fileReference": "docs/ACCESS_FALLBACK.md",
          "verification": "test -f docs/ACCESS_FALLBACK.md && grep -q 'Last verified:' docs/ACCESS_FALLBACK.md && grep -q 'developers.cloudflare.com' docs/ACCESS_FALLBACK.md"
        },
        {
          "stepNumber": 5,
          "instruction": "Create docs/TROUBLESHOOTING.md covering exactly these 5 failure modes with exact remediation steps: (1) wrangler deploy authentication error: run `wrangler login` to re-authenticate. (2) wrangler.toml vars issue: test-only variables must not appear in [vars] block; keep test mocks in vitest config only. (3) Anthropic key rejection: verify key exists and is active at https://console.anthropic.com/settings/keys; confirm billing is active. (4) Budget cap: DAILY_SPEND_LIMIT_USD env var controls spend; default $1.00; increase via `wrangler secret put DAILY_SPEND_LIMIT_USD`. (5) 503 errors: Cloudflare Worker CPU limit exceeded; check `wrangler tail` output at https://dash.cloudflare.com/ → Workers & Pages → worker name → Logs. End with: 'Last verified: 2026-05-18'.",
          "fileReference": "docs/TROUBLESHOOTING.md",
          "verification": "test -f docs/TROUBLESHOOTING.md && grep -q 'Last verified:' docs/TROUBLESHOOTING.md && grep -q 'wrangler login' docs/TROUBLESHOOTING.md"
        },
        {
          "stepNumber": 6,
          "instruction": "Create docs/PREFLIGHT.md as a pre-deployment checklist with these exact items and URLs:\n- [ ] GitHub account: https://github.com/signup\n- [ ] Cloudflare account: https://dash.cloudflare.com/sign-up\n- [ ] Anthropic account: https://console.anthropic.com/\n- [ ] Wrangler CLI authenticated: run `wrangler login`\n- [ ] Node 20+ installed\n- [ ] pnpm 9+ installed\n- [ ] Fork of this repo available in your GitHub account\nEnd with: 'Last verified: 2026-05-18'. The strings 'github.com/signup' and 'dash.cloudflare.com/sign-up' must appear verbatim.",
          "fileReference": "docs/PREFLIGHT.md",
          "verification": "test -f docs/PREFLIGHT.md && grep -q 'github.com/signup' docs/PREFLIGHT.md && grep -q 'dash.cloudflare.com/sign-up' docs/PREFLIGHT.md && grep -q 'Last verified:' docs/PREFLIGHT.md"
        },
        {
          "stepNumber": 7,
          "instruction": "Create docs/SPEC_CORRECTIONS.md as a stub template. Include these template fields only (no operational content): Title, Date, Affected Spec, Correction, Reason. Add a one-line header explaining this file records corrections to spec documents. No Last verified stamp needed.",
          "fileReference": "docs/SPEC_CORRECTIONS.md",
          "verification": "test -f docs/SPEC_CORRECTIONS.md"
        },
        {
          "stepNumber": 8,
          "instruction": "Build docs/SOURCES.md as the master citation index. First, run this command to extract all URLs from operational docs: `grep -hoE 'https?://[^ )\"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md | sort -u`. Then create docs/SOURCES.md with a markdown table: columns URL, Purpose, Last verified. Include every URL found by the grep. The file must contain ≥5 URLs. Every URL that appears in docs/UNINSTALL.md, docs/ACCESS_FALLBACK.md, and docs/PREFLIGHT.md must appear in SOURCES.md (URL-mirror requirement). Each entry carries 'Last verified: 2026-05-18'. Known URLs to include at minimum: https://dash.cloudflare.com/, https://developers.cloudflare.com/workers/configuration/delete-a-worker/, https://github.com/settings/repositories, https://console.anthropic.com/settings/keys, https://one.cloudflare.com/, https://developers.cloudflare.com/cloudflare-one/applications/configure-apps/self-hosted-apps/, https://github.com/signup, https://dash.cloudflare.com/sign-up, https://console.anthropic.com/.",
          "fileReference": "docs/SOURCES.md",
          "verification": "test -f docs/SOURCES.md && test $(grep -cE 'https?://' docs/SOURCES.md) -ge 5"
        },
        {
          "stepNumber": 9,
          "instruction": "Run the URL-mirror check to verify all URLs in UNINSTALL.md, ACCESS_FALLBACK.md, PREFLIGHT.md appear in SOURCES.md: `miss=0; for u in $(grep -hoE 'https?://[^ )\"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF \"$u\" docs/SOURCES.md || { echo \"MISSING: $u\"; miss=$((miss+1)); }; done; echo \"miss=$miss\"; test $miss -eq 0`. If any URL is missing, add it to SOURCES.md before continuing.",
          "fileReference": "docs/SOURCES.md",
          "verification": "miss=0; for u in $(grep -hoE 'https?://[^ )\"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF \"$u\" docs/SOURCES.md || miss=$((miss+1)); done; test $miss -eq 0"
        },
        {
          "stepNumber": 10,
          "instruction": "Update README.md: insert a '## Before you start' section immediately before the '## Deploy to Cloudflare' line. The section must contain an intro sentence about three required accounts, followed by these exact hyperlinks:\n- [Create a GitHub account](https://github.com/signup)\n- [Create a Cloudflare account](https://dash.cloudflare.com/sign-up)\n- [Create an Anthropic account](https://console.anthropic.com/)\nThe strings 'github.com/signup', 'dash.cloudflare.com/sign-up', and 'console.anthropic.com' must all appear verbatim in README.md. The heading 'Before you start' must appear verbatim.",
          "fileReference": "README.md",
          "verification": "grep -q 'Before you start' README.md && grep -q 'github.com/signup' README.md && grep -q 'dash.cloudflare.com/sign-up' README.md && grep -q 'console.anthropic.com' README.md"
        },
        {
          "stepNumber": 11,
          "instruction": "Run the vague-phrase check to confirm no forbidden phrases appear in any authored doc or in README.md: `grep -rEi 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md README.md`. The command must return no matches (exit 1 from grep means clean). If any match is found, rewrite the offending sentence with exact navigation steps instead.",
          "fileReference": "docs/UNINSTALL.md",
          "verification": "! grep -rEi 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md README.md"
        },
        {
          "stepNumber": 12,
          "instruction": "Confirm LICENSE already contains Section 7(b) — do NOT modify LICENSE. Run: `grep -q 'Section 7(b)' LICENSE && echo 'LICENSE OK'`. This is a read-only verification step; the criterion is pre-satisfied per codebase investigation.",
          "fileReference": "LICENSE",
          "verification": "grep -q 'Section 7(b)' LICENSE"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "test -f docs/UNINSTALL.md && grep -q 'Last verified:' docs/UNINSTALL.md && ! grep -iE 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/SECURITY.md && grep -q 'Last verified:' docs/SECURITY.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/ACCESS_FALLBACK.md && grep -q 'Last verified:' docs/ACCESS_FALLBACK.md && grep -q 'developers.cloudflare.com' docs/ACCESS_FALLBACK.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/TROUBLESHOOTING.md && grep -q 'Last verified:' docs/TROUBLESHOOTING.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/PREFLIGHT.md && grep -q 'github.com/signup' docs/PREFLIGHT.md && grep -q 'dash.cloudflare.com/sign-up' docs/PREFLIGHT.md && grep -q 'Last verified:' docs/PREFLIGHT.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/SPEC_CORRECTIONS.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/SOURCES.md && test $(grep -cE 'https?://' docs/SOURCES.md) -ge 5"
          },
          {
            "workspaceId": "primary",
            "command": "miss=0; for u in $(grep -hoE 'https?://[^ )\"<>]+' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md | sort -u); do grep -qF \"$u\" docs/SOURCES.md || miss=$((miss+1)); done; test $miss -eq 0"
          },
          {
            "workspaceId": "primary",
            "command": "grep -q 'Before you start' README.md && grep -q 'github.com/signup' README.md && grep -q 'dash.cloudflare.com/sign-up' README.md && grep -q 'console.anthropic.com' README.md"
          },
          {
            "workspaceId": "primary",
            "command": "grep -q 'Section 7(b)' LICENSE && grep -rEi 'go to the dashboard|find the setting|click somewhere|refer to the documentation' docs/UNINSTALL.md docs/ACCESS_FALLBACK.md docs/PREFLIGHT.md docs/SECURITY.md docs/TROUBLESHOOTING.md README.md && exit 1 || exit 0"
          }
        ],
        "functional": []
      }
    }
  ],
  "riskFlags": [
    "RF1: URL-mirror check may fail if operational docs use URL fragments or trailing punctuation that alter the exact URL string — executor must run the mirror-check script (step 9) before declaring done and patch SOURCES.md if any miss is found."
  ],
  "confidence": 0.88
}
```
