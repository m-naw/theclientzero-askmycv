# Sprint Plan: UI Foundation

**Sprint ID:** ITR-1779062230299-sprint-1
**Spec:** [2026-05-17-foundation-config-schema-theme-system-system-prompt-seo-accessibility-baseline.md](docs/specs/auto/2026-05-17-foundation-config-schema-theme-system-system-prompt-seo-accessibility-baseline.md)
**Confidence:** 0.9

## Context

Sprint 1 of G2: establishes the design system layer (dark theme tokens, web fonts, data-theme attribute, SEO meta tags, and design context documentation) before Sprint 2 adds form/schema/system-prompt changes.

## Risk flags

- RF1: docs/artifacts/ directory does not exist yet — T-1 must create it before writing design-context.md. The task steps include this but executors must not skip mkdir -p.
- RF2: Token-discipline test enforces hex-only-in-design-tokens.ts. If any hex literal is accidentally placed in layout.ts during T-3 (e.g. from a copy-paste error), the test suite will fail. T-3 step 7 includes an adversarial grep check to catch this.

## Task T-1: Design Context

**Type:** ui-analysis (modelTier: 2)

**Files to create:**
- docs/artifacts/design-context.md (workspace: primary)

### Step 1

Read src/views/design-tokens.ts in full to understand the existing token system: CSS custom property names, current light palette hex values (#fafaf7 bg, #1a1a1f text, etc.), font-stack strings, spacing/radius/motion tokens, and the toCssVars()/baseStyles() function signatures.

**File reference:** src/views/design-tokens.ts
**Verification:** You can identify: (a) the :root block produced by toCssVars(), (b) the --font-mono value on line 70, (c) the body font-family in baseStyles() on line 108, (d) the h1/h2 rules on lines 114-115.

### Step 2

Read src/views/layout.ts to understand the current LayoutProps interface (title, accentColor?, body, inlineScript?) and the renderLayout() HTML template structure.

**File reference:** src/views/layout.ts
**Verification:** You can confirm: (a) <html lang="en"> has no data-theme attribute, (b) only charset and viewport meta tags are present, (c) escapeHtml is imported from './escape'.

### Step 3

Read src/test/views/token-discipline.test.ts to understand the enforcement rule: hex literals matching /#[0-9a-fA-F]{3,8}\b/ are only permitted in design-tokens.ts. This constraint means ALL dark-theme hex values must land in design-tokens.ts exclusively.

**File reference:** src/test/views/token-discipline.test.ts
**Verification:** Confirm the regex pattern and which files are excluded from the check.

### Step 4

Create the directory docs/artifacts/ (it does not exist yet) and write docs/artifacts/design-context.md with the following content documenting the design system:

```markdown
# Design Context

## Design System Overview

This project uses a custom CSS custom-property (CSS variable) token system with no Tailwind or CSS-in-JS framework. All design tokens are declared in `src/views/design-tokens.ts`, which is the ONLY file in `src/views/` permitted to contain raw hex, px, or rem literals (enforced by `src/test/views/token-discipline.test.ts`).

## Token Architecture

- **Single source of truth:** `src/views/design-tokens.ts` exports `TOKENS` (JS references to `var(--name)` strings), `toCssVars(accentColor?)` (produces the `:root {}` and `[data-theme="dark"] {}` CSS blocks), and `baseStyles()` (shared layout/typography/component CSS).
- **No Figma, no Tailwind, no tailwind.config.ts.** All design decisions are encoded as CSS custom properties.
- **Dual-theme system:** `:root {}` defines the light palette; `[data-theme="dark"] {}` overrides a subset of color tokens for dark mode. The theme is applied via `data-theme` attribute on `<html>`.

## Color Palette

### Light Theme (`:root`)
| Token | Value | Role |
|---|---|---|
| `--color-bg` | `#fafaf7` | Page background |
| `--color-surface` | `#ffffff` | Card/input background |
| `--color-text` | `#1a1a1f` | Body text |
| `--color-text-muted` | `#6b6b78` | Secondary text |
| `--color-accent` | `#3b5bdb` (default) | Interactive/brand color |
| `--color-accent-fg` | `#ffffff` | Text on accent |
| `--color-error` | `#c92a2a` | Error states |
| `--color-border` | `#e3e3e8` | Dividers/borders |

### Dark Theme (`[data-theme="dark"]`)
| Token | Value | Role |
|---|---|---|
| `--color-bg` | `#181613` | Page background |
| `--color-surface` | `#242220` | Card/input background |
| `--color-text` | `#ededeb` | Body text |
| `--color-text-muted` | `#9b9b94` | Secondary text |
| `--color-border` | `#3a3834` | Dividers/borders |

## Typography

### Web Fonts (Google Fonts)
Three font families are loaded via Google Fonts stylesheet in `src/views/layout.ts`:
- **Fraunces** — serif display font for `h1`, `h2` headings
- **Instrument Sans** — humanist sans-serif for `body` text
- **JetBrains Mono** — monospaced font for `--font-mono` (code, textarea)

Font-family declarations in `baseStyles()` use the unencoded names with spaces (e.g., `'Instrument Sans'`, `'JetBrains Mono'`). The Google Fonts URL uses URL-encoded `+` separators (`Instrument+Sans`, `JetBrains+Mono`) which is a different encoding and does not satisfy the `grep -rP 'Instrument\s+Sans'` criterion — only the CSS declarations in `design-tokens.ts` do.

### Font Stack
- `body font-family`: `'Instrument Sans', system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`
- `h1, h2 font-family`: `'Fraunces', Georgia, serif`
- `--font-mono`: `'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace`

## Spacing Scale
| Token | Value |
|---|---|
| `--space-xs` | `0.25rem` |
| `--space-sm` | `0.5rem` |
| `--space-md` | `1rem` |
| `--space-lg` | `1.5rem` |
| `--space-xl` | `2.5rem` |

## Border Radius
| Token | Value |
|---|---|
| `--radius-sm` | `4px` |
| `--radius-md` | `8px` |
| `--radius-lg` | `16px` |

## Motion
| Token | Value |
|---|---|
| `--motion-fast` | `120ms ease-out` |
| `--motion-normal` | `220ms ease-out` |

## Layout Entrypoint

`src/views/layout.ts` exports `renderLayout(props: LayoutProps)` which wraps every page. `LayoutProps` includes optional `theme` (`'light' | 'dark'`) and `description` fields added in Sprint 1. The `data-theme` attribute on `<html>` is set from `props.theme ?? 'light'`.

## SEO Meta Tags

All pages include (via `renderLayout`):
- `<meta property="og:title" content="...">` — HTML-escaped title
- `<meta property="og:type" content="website">`
- `<meta name="twitter:card" content="summary">`
- `<meta name="twitter:title" content="...">` — HTML-escaped title

## Token Discipline Constraint

The test at `src/test/views/token-discipline.test.ts` sweeps all `src/views/**/*.ts` files with regex `/#[0-9a-fA-F]{3,8}\b/` and fails if any file other than `design-tokens.ts` contains a hex color literal. Hex values must NEVER be placed in `layout.ts`, `chat-page.ts`, or any other view file.
```

**File reference:** docs/artifacts/design-context.md
**Verification:** File exists at docs/artifacts/design-context.md and contains sections for color palette, typography, and token discipline constraint.

### Task verification

**Structural:**
- `fs:exists docs/artifacts/design-context.md` (ws: primary)
- `fs:contains docs/artifacts/design-context.md "#181613"` (ws: primary)
- `fs:contains docs/artifacts/design-context.md "Instrument Sans"` (ws: primary)
- `fs:contains docs/artifacts/design-context.md "JetBrains Mono"` (ws: primary)
- `fs:contains docs/artifacts/design-context.md "Fraunces"` (ws: primary)

## Task T-2: Update design-tokens.ts: dark theme + web fonts

**Type:** code (modelTier: 2)

**Depends on:** T-1

**Files to modify:**
- src/views/design-tokens.ts (workspace: primary)

### Step 1

Read docs/artifacts/design-context.md before starting work — its findings are authoritative and supersede any assumptions derived from the goal description or sprint objectives. This artifact documents the exact hex values, font stacks, and token discipline constraint that govern this task.

**File reference:** docs/artifacts/design-context.md
**Verification:** You have read and understood: dark theme hex values, font stacks, and the token-discipline rule (hex only in design-tokens.ts).

### Step 2

In src/views/design-tokens.ts, locate the closing `}` of the `:root` block (currently at line 93 after `--motion-normal: 220ms ease-out;`). Append the dark-theme override block immediately after that closing brace (inside the return string of toCssVars()):

```
[data-theme="dark"] {
  --color-bg: #181613;
  --color-surface: #242220;
  --color-text: #ededeb;
  --color-text-muted: #9b9b94;
  --color-border: #3a3834;
}```

The full end of toCssVars() return value should be:
```ts
  --motion-fast: 120ms ease-out;
  --motion-normal: 220ms ease-out;
}
[data-theme="dark"] {
  --color-bg: #181613;
  --color-surface: #242220;
  --color-text: #ededeb;
  --color-text-muted: #9b9b94;
  --color-border: #3a3834;
}`;
}```

**File reference:** src/views/design-tokens.ts:63-94
**Verification:** grep -r '#181613' src/ exits 0

### Step 3

In src/views/design-tokens.ts, inside toCssVars(), update the --font-mono line (currently line 70: `--font-mono: ui-monospace, SFMono-Regular, Menlo, monospace;`) to:
```
  --font-mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
```
This adds 'JetBrains Mono' (with a space) as the first entry, which satisfies `grep -rP 'JetBrains\s+Mono' src/views/`.

**File reference:** src/views/design-tokens.ts:70
**Verification:** grep -rP 'JetBrains\s+Mono' src/views/ exits 0

### Step 4

In src/views/design-tokens.ts, inside baseStyles(), update the body `font-family` rule (currently line 108: `font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;`) to:
```
  font-family: 'Instrument Sans', system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
```
This adds 'Instrument Sans' (with a space) as the first font, satisfying `grep -rP 'Instrument\s+Sans' src/views/`.

**File reference:** src/views/design-tokens.ts:108
**Verification:** grep -rP 'Instrument\s+Sans' src/views/ exits 0

### Step 5

In src/views/design-tokens.ts, inside baseStyles(), update the h1 and h2 rules to add `font-family: 'Fraunces', Georgia, serif;`. The current h1 rule (line 114) is:
```css
h1 { font-size: var(--font-xl); margin: 0 0 var(--space-sm); }
```
Change it to:
```css
h1 { font-size: var(--font-xl); margin: 0 0 var(--space-sm); font-family: 'Fraunces', Georgia, serif; }
```
And the current h2 rule (line 115):
```css
h2 { font-size: var(--font-lg); margin: 0 0 var(--space-sm); }
```
Change it to:
```css
h2 { font-size: var(--font-lg); margin: 0 0 var(--space-sm); font-family: 'Fraunces', Georgia, serif; }
```
This places 'Fraunces' in baseStyles(), satisfying `grep -rP 'Fraunces' src/views/`.

**File reference:** src/views/design-tokens.ts:114-115
**Verification:** grep -rP 'Fraunces' src/views/ exits 0

### Step 6

Run all structural grep checks to confirm all four done-when criteria for this file are satisfied:
- grep -r '#181613' src/
- grep -rP 'Fraunces' src/views/
- grep -rP 'Instrument\s+Sans' src/views/
- grep -rP 'JetBrains\s+Mono' src/views/
All four must exit 0.

**File reference:** src/views/design-tokens.ts
**Verification:** All four grep commands exit 0 with matches shown in design-tokens.ts.

### Step 7

Run pnpm build and pnpm typecheck to confirm no TypeScript or build errors were introduced. The changes are purely CSS string modifications inside template literals, so no type errors are expected.

**File reference:** src/views/design-tokens.ts
**Verification:** pnpm build exits 0 and pnpm typecheck exits 0.

### Step 8

Run pnpm test to confirm all existing tests still pass (≥214 tests, no regression). Pay particular attention to src/test/views/token-discipline.test.ts — it must still pass because all new hex values (#181613, #242220, #ededeb, #9b9b94, #3a3834) are in design-tokens.ts only.

**File reference:** src/test/views/token-discipline.test.ts
**Verification:** pnpm test exits 0 with all tests passing including token-discipline.

### Task verification

**Structural:**
- `grep -r '#181613' src/` (ws: primary)
- `grep -rP 'Fraunces' src/views/` (ws: primary)
- `grep -rP 'Instrument\s+Sans' src/views/` (ws: primary)
- `grep -rP 'JetBrains\s+Mono' src/views/` (ws: primary)

**Functional:**
- `pnpm build` (ws: primary)
- `pnpm typecheck` (ws: primary)
- `pnpm test` (ws: primary)

## Task T-3: Update layout.ts: data-theme, SEO meta tags, Google Fonts link

**Type:** code (modelTier: 2)

**Depends on:** T-1

**Files to modify:**
- src/views/layout.ts (workspace: primary)

### Step 1

Read docs/artifacts/design-context.md before starting work — its findings are authoritative and supersede any assumptions derived from the goal description or sprint objectives. This artifact documents the SEO meta tag structure, Google Fonts URL, and the token-discipline constraint (no hex literals in layout.ts).

**File reference:** docs/artifacts/design-context.md
**Verification:** You have confirmed: (a) Google Fonts URL format with URL-encoded + separators, (b) which meta tags are required, (c) that layout.ts must contain no hex literals.

### Step 2

In src/views/layout.ts, extend the LayoutProps interface by adding two optional fields. Change:
```ts
export interface LayoutProps {
  title: string;
  accentColor?: string;
  body: string;
  inlineScript?: string;
}
```
To:
```ts
export interface LayoutProps {
  title: string;
  accentColor?: string;
  body: string;
  inlineScript?: string;
  theme?: 'light' | 'dark';
  description?: string;
}
```
These are optional fields so all existing callers remain valid without changes.

**File reference:** src/views/layout.ts:6-11
**Verification:** pnpm typecheck exits 0 (no callers break).

### Step 3

In src/views/layout.ts, inside renderLayout(), change the opening `<html>` tag from:
```html
<html lang="en">
```
To:
```html
<html lang="en" data-theme="${props.theme ?? 'light'}">
```
This satisfies `grep -r 'data-theme' src/` using the props.theme optional field with a 'light' fallback.

**File reference:** src/views/layout.ts:16
**Verification:** grep -r 'data-theme' src/ exits 0 with a match in layout.ts.

### Step 4

In src/views/layout.ts, inside renderLayout(), add OG and Twitter meta tags after the `<title>` line. The template section should become:
```html
<title>${escapeHtml(props.title)}</title>
<meta property="og:title" content="${escapeHtml(props.title)}" />
<meta property="og:type" content="website" />
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content="${escapeHtml(props.title)}" />
```
All dynamic values use escapeHtml() (already imported at line 2) to prevent XSS. The static content values ('website', 'summary') need no escaping.

**File reference:** src/views/layout.ts:20
**Verification:** grep -r 'og:title' src/views/ exits 0 AND grep -r 'twitter:card' src/views/ exits 0.

### Step 5

In src/views/layout.ts, add Google Fonts preconnect and stylesheet links after the meta tags block (before the `<style>` tag). Add:
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;1,9..144,400&family=Instrument+Sans:wght@400;600&family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet" />
```
IMPORTANT: The Google Fonts URL uses URL-encoded `+` (e.g., `Instrument+Sans`, `JetBrains+Mono`) — this does NOT satisfy the `grep -rP 'Instrument\s+Sans'` criterion, which is why design-tokens.ts:baseStyles() carries the space-encoded CSS font-family declarations. Do NOT add hex literals to this file.

**File reference:** src/views/layout.ts
**Verification:** The file contains the Google Fonts stylesheet link; no hex literals exist in layout.ts.

### Step 6

Verify the final shape of renderLayout() return value matches this structure:
```html
<!doctype html>
<html lang="en" data-theme="${props.theme ?? 'light'}">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>${escapeHtml(props.title)}</title>
<meta property="og:title" content="${escapeHtml(props.title)}" />
<meta property="og:type" content="website" />
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content="${escapeHtml(props.title)}" />
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;1,9..144,400&family=Instrument+Sans:wght@400;600&family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet" />
<style>${toCssVars(props.accentColor)}${baseStyles()}</style>
</head>
<body>
<main class="page">${props.body}</main>
${script}
</body>
</html>
```

**File reference:** src/views/layout.ts
**Verification:** Visual inspection of the file matches the expected structure.

### Step 7

Run all structural grep checks for layout.ts done-when criteria:
- grep -r 'data-theme' src/ (must exit 0)
- grep -r 'og:title' src/views/ (must exit 0)
- grep -r 'twitter:card' src/views/ (must exit 0)
Also confirm no hex literals leaked into layout.ts: grep -P '#[0-9a-fA-F]{3,8}' src/views/layout.ts should exit non-zero (no matches).

**File reference:** src/views/layout.ts
**Verification:** First three greps exit 0; hex literal grep exits non-zero (no hex in layout.ts).

### Step 8

Run pnpm build, pnpm typecheck, and pnpm test to confirm all toolchain checks pass. The optional LayoutProps fields are non-breaking; existing callers provide only title/accentColor/body/inlineScript and will get data-theme="light" by default.

**File reference:** src/views/layout.ts
**Verification:** pnpm build exits 0, pnpm typecheck exits 0, pnpm test exits 0 with ≥214 tests passing.

### Task verification

**Structural:**
- `grep -r 'data-theme' src/` (ws: primary)
- `grep -r 'og:title' src/views/` (ws: primary)
- `grep -r 'twitter:card' src/views/` (ws: primary)

**Functional:**
- `pnpm build` (ws: primary)
- `pnpm typecheck` (ws: primary)
- `pnpm test` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779062230299-sprint-1",
  "sprintName": "UI Foundation",
  "specPath": "docs/specs/auto/2026-05-17-foundation-config-schema-theme-system-system-prompt-seo-accessibility-baseline.md",
  "phaseContext": "Sprint 1 of G2: establishes the design system layer (dark theme tokens, web fonts, data-theme attribute, SEO meta tags, and design context documentation) before Sprint 2 adds form/schema/system-prompt changes.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Design Context",
      "type": "ui-analysis",
      "modelTier": 2,
      "files": {
        "modify": [],
        "create": [
          {
            "workspaceId": "primary",
            "path": "docs/artifacts/design-context.md"
          }
        ]
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read src/views/design-tokens.ts in full to understand the existing token system: CSS custom property names, current light palette hex values (#fafaf7 bg, #1a1a1f text, etc.), font-stack strings, spacing/radius/motion tokens, and the toCssVars()/baseStyles() function signatures.",
          "fileReference": "src/views/design-tokens.ts",
          "verification": "You can identify: (a) the :root block produced by toCssVars(), (b) the --font-mono value on line 70, (c) the body font-family in baseStyles() on line 108, (d) the h1/h2 rules on lines 114-115."
        },
        {
          "stepNumber": 2,
          "instruction": "Read src/views/layout.ts to understand the current LayoutProps interface (title, accentColor?, body, inlineScript?) and the renderLayout() HTML template structure.",
          "fileReference": "src/views/layout.ts",
          "verification": "You can confirm: (a) <html lang=\"en\"> has no data-theme attribute, (b) only charset and viewport meta tags are present, (c) escapeHtml is imported from './escape'."
        },
        {
          "stepNumber": 3,
          "instruction": "Read src/test/views/token-discipline.test.ts to understand the enforcement rule: hex literals matching /#[0-9a-fA-F]{3,8}\\b/ are only permitted in design-tokens.ts. This constraint means ALL dark-theme hex values must land in design-tokens.ts exclusively.",
          "fileReference": "src/test/views/token-discipline.test.ts",
          "verification": "Confirm the regex pattern and which files are excluded from the check."
        },
        {
          "stepNumber": 4,
          "instruction": "Create the directory docs/artifacts/ (it does not exist yet) and write docs/artifacts/design-context.md with the following content documenting the design system:\n\n```markdown\n# Design Context\n\n## Design System Overview\n\nThis project uses a custom CSS custom-property (CSS variable) token system with no Tailwind or CSS-in-JS framework. All design tokens are declared in `src/views/design-tokens.ts`, which is the ONLY file in `src/views/` permitted to contain raw hex, px, or rem literals (enforced by `src/test/views/token-discipline.test.ts`).\n\n## Token Architecture\n\n- **Single source of truth:** `src/views/design-tokens.ts` exports `TOKENS` (JS references to `var(--name)` strings), `toCssVars(accentColor?)` (produces the `:root {}` and `[data-theme=\"dark\"] {}` CSS blocks), and `baseStyles()` (shared layout/typography/component CSS).\n- **No Figma, no Tailwind, no tailwind.config.ts.** All design decisions are encoded as CSS custom properties.\n- **Dual-theme system:** `:root {}` defines the light palette; `[data-theme=\"dark\"] {}` overrides a subset of color tokens for dark mode. The theme is applied via `data-theme` attribute on `<html>`.\n\n## Color Palette\n\n### Light Theme (`:root`)\n| Token | Value | Role |\n|---|---|---|\n| `--color-bg` | `#fafaf7` | Page background |\n| `--color-surface` | `#ffffff` | Card/input background |\n| `--color-text` | `#1a1a1f` | Body text |\n| `--color-text-muted` | `#6b6b78` | Secondary text |\n| `--color-accent` | `#3b5bdb` (default) | Interactive/brand color |\n| `--color-accent-fg` | `#ffffff` | Text on accent |\n| `--color-error` | `#c92a2a` | Error states |\n| `--color-border` | `#e3e3e8` | Dividers/borders |\n\n### Dark Theme (`[data-theme=\"dark\"]`)\n| Token | Value | Role |\n|---|---|---|\n| `--color-bg` | `#181613` | Page background |\n| `--color-surface` | `#242220` | Card/input background |\n| `--color-text` | `#ededeb` | Body text |\n| `--color-text-muted` | `#9b9b94` | Secondary text |\n| `--color-border` | `#3a3834` | Dividers/borders |\n\n## Typography\n\n### Web Fonts (Google Fonts)\nThree font families are loaded via Google Fonts stylesheet in `src/views/layout.ts`:\n- **Fraunces** — serif display font for `h1`, `h2` headings\n- **Instrument Sans** — humanist sans-serif for `body` text\n- **JetBrains Mono** — monospaced font for `--font-mono` (code, textarea)\n\nFont-family declarations in `baseStyles()` use the unencoded names with spaces (e.g., `'Instrument Sans'`, `'JetBrains Mono'`). The Google Fonts URL uses URL-encoded `+` separators (`Instrument+Sans`, `JetBrains+Mono`) which is a different encoding and does not satisfy the `grep -rP 'Instrument\\s+Sans'` criterion — only the CSS declarations in `design-tokens.ts` do.\n\n### Font Stack\n- `body font-family`: `'Instrument Sans', system-ui, -apple-system, \"Segoe UI\", Roboto, sans-serif`\n- `h1, h2 font-family`: `'Fraunces', Georgia, serif`\n- `--font-mono`: `'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace`\n\n## Spacing Scale\n| Token | Value |\n|---|---|\n| `--space-xs` | `0.25rem` |\n| `--space-sm` | `0.5rem` |\n| `--space-md` | `1rem` |\n| `--space-lg` | `1.5rem` |\n| `--space-xl` | `2.5rem` |\n\n## Border Radius\n| Token | Value |\n|---|---|\n| `--radius-sm` | `4px` |\n| `--radius-md` | `8px` |\n| `--radius-lg` | `16px` |\n\n## Motion\n| Token | Value |\n|---|---|\n| `--motion-fast` | `120ms ease-out` |\n| `--motion-normal` | `220ms ease-out` |\n\n## Layout Entrypoint\n\n`src/views/layout.ts` exports `renderLayout(props: LayoutProps)` which wraps every page. `LayoutProps` includes optional `theme` (`'light' | 'dark'`) and `description` fields added in Sprint 1. The `data-theme` attribute on `<html>` is set from `props.theme ?? 'light'`.\n\n## SEO Meta Tags\n\nAll pages include (via `renderLayout`):\n- `<meta property=\"og:title\" content=\"...\">` — HTML-escaped title\n- `<meta property=\"og:type\" content=\"website\">`\n- `<meta name=\"twitter:card\" content=\"summary\">`\n- `<meta name=\"twitter:title\" content=\"...\">` — HTML-escaped title\n\n## Token Discipline Constraint\n\nThe test at `src/test/views/token-discipline.test.ts` sweeps all `src/views/**/*.ts` files with regex `/#[0-9a-fA-F]{3,8}\\b/` and fails if any file other than `design-tokens.ts` contains a hex color literal. Hex values must NEVER be placed in `layout.ts`, `chat-page.ts`, or any other view file.\n```",
          "fileReference": "docs/artifacts/design-context.md",
          "verification": "File exists at docs/artifacts/design-context.md and contains sections for color palette, typography, and token discipline constraint."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "fs:exists docs/artifacts/design-context.md"
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains docs/artifacts/design-context.md \"#181613\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains docs/artifacts/design-context.md \"Instrument Sans\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains docs/artifacts/design-context.md \"JetBrains Mono\""
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains docs/artifacts/design-context.md \"Fraunces\""
          }
        ],
        "functional": []
      }
    },
    {
      "id": "T-2",
      "title": "Update design-tokens.ts: dark theme + web fonts",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/views/design-tokens.ts"
          }
        ],
        "create": []
      },
      "artifact_refs": [
        "docs/artifacts/design-context.md"
      ],
      "dependencies": [
        "T-1"
      ],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read docs/artifacts/design-context.md before starting work — its findings are authoritative and supersede any assumptions derived from the goal description or sprint objectives. This artifact documents the exact hex values, font stacks, and token discipline constraint that govern this task.",
          "fileReference": "docs/artifacts/design-context.md",
          "verification": "You have read and understood: dark theme hex values, font stacks, and the token-discipline rule (hex only in design-tokens.ts)."
        },
        {
          "stepNumber": 2,
          "instruction": "In src/views/design-tokens.ts, locate the closing `}` of the `:root` block (currently at line 93 after `--motion-normal: 220ms ease-out;`). Append the dark-theme override block immediately after that closing brace (inside the return string of toCssVars()):\n\n```\n[data-theme=\"dark\"] {\n  --color-bg: #181613;\n  --color-surface: #242220;\n  --color-text: #ededeb;\n  --color-text-muted: #9b9b94;\n  --color-border: #3a3834;\n}```\n\nThe full end of toCssVars() return value should be:\n```ts\n  --motion-fast: 120ms ease-out;\n  --motion-normal: 220ms ease-out;\n}\n[data-theme=\"dark\"] {\n  --color-bg: #181613;\n  --color-surface: #242220;\n  --color-text: #ededeb;\n  --color-text-muted: #9b9b94;\n  --color-border: #3a3834;\n}`;\n}```",
          "fileReference": "src/views/design-tokens.ts:63-94",
          "verification": "grep -r '#181613' src/ exits 0"
        },
        {
          "stepNumber": 3,
          "instruction": "In src/views/design-tokens.ts, inside toCssVars(), update the --font-mono line (currently line 70: `--font-mono: ui-monospace, SFMono-Regular, Menlo, monospace;`) to:\n```\n  --font-mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace;\n```\nThis adds 'JetBrains Mono' (with a space) as the first entry, which satisfies `grep -rP 'JetBrains\\s+Mono' src/views/`.",
          "fileReference": "src/views/design-tokens.ts:70",
          "verification": "grep -rP 'JetBrains\\s+Mono' src/views/ exits 0"
        },
        {
          "stepNumber": 4,
          "instruction": "In src/views/design-tokens.ts, inside baseStyles(), update the body `font-family` rule (currently line 108: `font-family: system-ui, -apple-system, \"Segoe UI\", Roboto, sans-serif;`) to:\n```\n  font-family: 'Instrument Sans', system-ui, -apple-system, \"Segoe UI\", Roboto, sans-serif;\n```\nThis adds 'Instrument Sans' (with a space) as the first font, satisfying `grep -rP 'Instrument\\s+Sans' src/views/`.",
          "fileReference": "src/views/design-tokens.ts:108",
          "verification": "grep -rP 'Instrument\\s+Sans' src/views/ exits 0"
        },
        {
          "stepNumber": 5,
          "instruction": "In src/views/design-tokens.ts, inside baseStyles(), update the h1 and h2 rules to add `font-family: 'Fraunces', Georgia, serif;`. The current h1 rule (line 114) is:\n```css\nh1 { font-size: var(--font-xl); margin: 0 0 var(--space-sm); }\n```\nChange it to:\n```css\nh1 { font-size: var(--font-xl); margin: 0 0 var(--space-sm); font-family: 'Fraunces', Georgia, serif; }\n```\nAnd the current h2 rule (line 115):\n```css\nh2 { font-size: var(--font-lg); margin: 0 0 var(--space-sm); }\n```\nChange it to:\n```css\nh2 { font-size: var(--font-lg); margin: 0 0 var(--space-sm); font-family: 'Fraunces', Georgia, serif; }\n```\nThis places 'Fraunces' in baseStyles(), satisfying `grep -rP 'Fraunces' src/views/`.",
          "fileReference": "src/views/design-tokens.ts:114-115",
          "verification": "grep -rP 'Fraunces' src/views/ exits 0"
        },
        {
          "stepNumber": 6,
          "instruction": "Run all structural grep checks to confirm all four done-when criteria for this file are satisfied:\n- grep -r '#181613' src/\n- grep -rP 'Fraunces' src/views/\n- grep -rP 'Instrument\\s+Sans' src/views/\n- grep -rP 'JetBrains\\s+Mono' src/views/\nAll four must exit 0.",
          "fileReference": "src/views/design-tokens.ts",
          "verification": "All four grep commands exit 0 with matches shown in design-tokens.ts."
        },
        {
          "stepNumber": 7,
          "instruction": "Run pnpm build and pnpm typecheck to confirm no TypeScript or build errors were introduced. The changes are purely CSS string modifications inside template literals, so no type errors are expected.",
          "fileReference": "src/views/design-tokens.ts",
          "verification": "pnpm build exits 0 and pnpm typecheck exits 0."
        },
        {
          "stepNumber": 8,
          "instruction": "Run pnpm test to confirm all existing tests still pass (≥214 tests, no regression). Pay particular attention to src/test/views/token-discipline.test.ts — it must still pass because all new hex values (#181613, #242220, #ededeb, #9b9b94, #3a3834) are in design-tokens.ts only.",
          "fileReference": "src/test/views/token-discipline.test.ts",
          "verification": "pnpm test exits 0 with all tests passing including token-discipline."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -r '#181613' src/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rP 'Fraunces' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rP 'Instrument\\s+Sans' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rP 'JetBrains\\s+Mono' src/views/"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm typecheck"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    },
    {
      "id": "T-3",
      "title": "Update layout.ts: data-theme, SEO meta tags, Google Fonts link",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/views/layout.ts"
          }
        ],
        "create": []
      },
      "artifact_refs": [
        "docs/artifacts/design-context.md"
      ],
      "dependencies": [
        "T-1"
      ],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read docs/artifacts/design-context.md before starting work — its findings are authoritative and supersede any assumptions derived from the goal description or sprint objectives. This artifact documents the SEO meta tag structure, Google Fonts URL, and the token-discipline constraint (no hex literals in layout.ts).",
          "fileReference": "docs/artifacts/design-context.md",
          "verification": "You have confirmed: (a) Google Fonts URL format with URL-encoded + separators, (b) which meta tags are required, (c) that layout.ts must contain no hex literals."
        },
        {
          "stepNumber": 2,
          "instruction": "In src/views/layout.ts, extend the LayoutProps interface by adding two optional fields. Change:\n```ts\nexport interface LayoutProps {\n  title: string;\n  accentColor?: string;\n  body: string;\n  inlineScript?: string;\n}\n```\nTo:\n```ts\nexport interface LayoutProps {\n  title: string;\n  accentColor?: string;\n  body: string;\n  inlineScript?: string;\n  theme?: 'light' | 'dark';\n  description?: string;\n}\n```\nThese are optional fields so all existing callers remain valid without changes.",
          "fileReference": "src/views/layout.ts:6-11",
          "verification": "pnpm typecheck exits 0 (no callers break)."
        },
        {
          "stepNumber": 3,
          "instruction": "In src/views/layout.ts, inside renderLayout(), change the opening `<html>` tag from:\n```html\n<html lang=\"en\">\n```\nTo:\n```html\n<html lang=\"en\" data-theme=\"${props.theme ?? 'light'}\">\n```\nThis satisfies `grep -r 'data-theme' src/` using the props.theme optional field with a 'light' fallback.",
          "fileReference": "src/views/layout.ts:16",
          "verification": "grep -r 'data-theme' src/ exits 0 with a match in layout.ts."
        },
        {
          "stepNumber": 4,
          "instruction": "In src/views/layout.ts, inside renderLayout(), add OG and Twitter meta tags after the `<title>` line. The template section should become:\n```html\n<title>${escapeHtml(props.title)}</title>\n<meta property=\"og:title\" content=\"${escapeHtml(props.title)}\" />\n<meta property=\"og:type\" content=\"website\" />\n<meta name=\"twitter:card\" content=\"summary\" />\n<meta name=\"twitter:title\" content=\"${escapeHtml(props.title)}\" />\n```\nAll dynamic values use escapeHtml() (already imported at line 2) to prevent XSS. The static content values ('website', 'summary') need no escaping.",
          "fileReference": "src/views/layout.ts:20",
          "verification": "grep -r 'og:title' src/views/ exits 0 AND grep -r 'twitter:card' src/views/ exits 0."
        },
        {
          "stepNumber": 5,
          "instruction": "In src/views/layout.ts, add Google Fonts preconnect and stylesheet links after the meta tags block (before the `<style>` tag). Add:\n```html\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\" />\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin />\n<link href=\"https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;1,9..144,400&family=Instrument+Sans:wght@400;600&family=JetBrains+Mono:wght@400&display=swap\" rel=\"stylesheet\" />\n```\nIMPORTANT: The Google Fonts URL uses URL-encoded `+` (e.g., `Instrument+Sans`, `JetBrains+Mono`) — this does NOT satisfy the `grep -rP 'Instrument\\s+Sans'` criterion, which is why design-tokens.ts:baseStyles() carries the space-encoded CSS font-family declarations. Do NOT add hex literals to this file.",
          "fileReference": "src/views/layout.ts",
          "verification": "The file contains the Google Fonts stylesheet link; no hex literals exist in layout.ts."
        },
        {
          "stepNumber": 6,
          "instruction": "Verify the final shape of renderLayout() return value matches this structure:\n```html\n<!doctype html>\n<html lang=\"en\" data-theme=\"${props.theme ?? 'light'}\">\n<head>\n<meta charset=\"utf-8\" />\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\" />\n<title>${escapeHtml(props.title)}</title>\n<meta property=\"og:title\" content=\"${escapeHtml(props.title)}\" />\n<meta property=\"og:type\" content=\"website\" />\n<meta name=\"twitter:card\" content=\"summary\" />\n<meta name=\"twitter:title\" content=\"${escapeHtml(props.title)}\" />\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\" />\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin />\n<link href=\"https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;1,9..144,400&family=Instrument+Sans:wght@400;600&family=JetBrains+Mono:wght@400&display=swap\" rel=\"stylesheet\" />\n<style>${toCssVars(props.accentColor)}${baseStyles()}</style>\n</head>\n<body>\n<main class=\"page\">${props.body}</main>\n${script}\n</body>\n</html>\n```",
          "fileReference": "src/views/layout.ts",
          "verification": "Visual inspection of the file matches the expected structure."
        },
        {
          "stepNumber": 7,
          "instruction": "Run all structural grep checks for layout.ts done-when criteria:\n- grep -r 'data-theme' src/ (must exit 0)\n- grep -r 'og:title' src/views/ (must exit 0)\n- grep -r 'twitter:card' src/views/ (must exit 0)\nAlso confirm no hex literals leaked into layout.ts: grep -P '#[0-9a-fA-F]{3,8}' src/views/layout.ts should exit non-zero (no matches).",
          "fileReference": "src/views/layout.ts",
          "verification": "First three greps exit 0; hex literal grep exits non-zero (no hex in layout.ts)."
        },
        {
          "stepNumber": 8,
          "instruction": "Run pnpm build, pnpm typecheck, and pnpm test to confirm all toolchain checks pass. The optional LayoutProps fields are non-breaking; existing callers provide only title/accentColor/body/inlineScript and will get data-theme=\"light\" by default.",
          "fileReference": "src/views/layout.ts",
          "verification": "pnpm build exits 0, pnpm typecheck exits 0, pnpm test exits 0 with ≥214 tests passing."
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -r 'data-theme' src/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -r 'og:title' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -r 'twitter:card' src/views/"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm typecheck"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: docs/artifacts/ directory does not exist yet — T-1 must create it before writing design-context.md. The task steps include this but executors must not skip mkdir -p.",
    "RF2: Token-discipline test enforces hex-only-in-design-tokens.ts. If any hex literal is accidentally placed in layout.ts during T-3 (e.g. from a copy-paste error), the test suite will fail. T-3 step 7 includes an adversarial grep check to catch this."
  ],
  "confidence": 0.9
}
```
