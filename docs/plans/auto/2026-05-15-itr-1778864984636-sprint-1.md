{"sprintId":"ITR-1778864984636-sprint-1","sprintName":"Green CI: diagnose and fix all verification failures","specPath":"docs/specs/auto/2026-05-15-admin-configuration-editing-behavior-f5.md","phaseContext":"Goal G7: Wire GET /admin and POST /admin/save behavior. This sprint fixes CI failures after sprint-1 implementation by closing 3 documented assertion gaps in the admin test suite and ensuring pnpm build/typecheck/test all exit 0.","tasks":[{"id":"T-1","title":"Fix admin test assertion gaps, KV isolation, and green CI","type":"code","modelTier":2,"files":{"modify":[{"workspaceId":"primary","path":"src/__tests__/admin/admin.test.ts"}],"create":[]},"artifact_refs":["docs/specs/auto/2026-05-15-admin-configuration-editing-behavior-f5.md"],"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Run `pnpm build` and record the exit code and any error output. If errors exist, fix them before continuing. Common risk: TypeScript compilation errors introduced by the admin route or test files.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm build exits 0 with no error lines."},{"stepNumber":2,"instruction":"Run `pnpm typecheck` and record the exit code and any diagnostic output. If type errors exist, fix them in the relevant source files. Known risk area: `opts: { body?: string }` callback typing in the undici fetchMock `.reply()` call — if TypeScript rejects the inline type annotation, remove the explicit annotation and let it infer, or use `(opts: Record<string, unknown>)` and cast `opts.body` to `string | undefined`.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm typecheck exits 0 with zero diagnostic lines."},{"stepNumber":3,"instruction":"In `src/__tests__/admin/admin.test.ts`, add `spend:<date>` key deletion to the shared `clearKv()` function at the top of the file (line ~52). The fix is:\n```ts\nasync function clearKv(): Promise<void> {\n  const kv = getEnv().STATE;\n  const today = new Date().toISOString().slice(0, 10);\n  for (const key of [\"config\", \"setup_window_start\", TEST_JWKS_KV_KEY, `spend:${today}`]) {\n    await kv.delete(key);\n  }\n}\n```\nThis prevents AT11's POST /chat from hitting the daily budget cap due to spend accumulation from earlier chat-abuse or budget tests running in the same pool.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"clearKv() now deletes the spend key; grep confirms `spend:${today}` appears in clearKv."},{"stepNumber":4,"instruction":"In `src/__tests__/admin/admin.test.ts` Test 7 ('blank anthropic_api_key preserves the existing key'), after the assertion `expect(stored.anthropic_api_key).toBe(\"sk-ant-existing-key\")` (line ~304), add the missing headline assertion:\n```ts\nexpect(stored.headline).toBe(\"Updated headline · Berlin\");\n```\nThis satisfies done_when criterion 8: 'POST save match + headline → 200 + KV updated'.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"grep confirms `expect(stored.headline).toBe(\"Updated headline \\xb7 Berlin\")` is present in Test 7."},{"stepNumber":5,"instruction":"In `src/__tests__/admin/admin.test.ts` Test 10 ('POST /admin/save with no JWT returns 403'), add a KV snapshot before and after the request. The test currently checks status + body; add:\n```ts\n// KV snapshot: config must remain unchanged on auth failure\nconst kvBefore = await getEnv().STATE.get(\"config\");\nconst res = await runFetch(adminSaveRequest(null, validSaveBody()));\nexpect(res.status).toBe(403);\nconst html = await res.text();\nexpect(html).toContain(\"no_jwt\");\nexpect(await getEnv().STATE.get(\"config\")).toBe(kvBefore);\n```\nReplace the existing Test 10 body with this version that includes the kvBefore snapshot. This satisfies done_when criterion 7: 'POST save no JWT → 403 + KV unchanged'.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"grep confirms `kvBefore` and `STATE.get(\"config\")` appear in Test 10."},{"stepNumber":6,"instruction":"Run `pnpm test` (full suite). Examine output for: (a) any FAIL lines, (b) test count reported. If AT11 still returns 503 for POST /chat despite the spend key fix, verify the AT11_HOST mock is registered AFTER step 1 (POST /admin/save) so the Anthropic mock is active when /chat is called — the mock registration at line ~599 inside the test body is correct because it is set up between step 2 and step 3. If any other test fails, diagnose and fix the root cause without touching tests that were passing before this sprint.","fileReference":"src/__tests__/admin/admin.test.ts","verification":"pnpm test exits 0; output line containing 'Tests' shows all passing; no FAIL lines present; test count >= 180 (G6 baseline) + 14 (admin suite) + 5 (src/test/admin.test.ts) or the actual count seen in the output."}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"spend:${today}\""},{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"stored.headline"},{"workspaceId":"primary","command":"fs:contains src/__tests__/admin/admin.test.ts \"kvBefore\""}],"functional":[{"workspaceId":"primary","command":"pnpm build"},{"workspaceId":"primary","command":"pnpm typecheck"},{"workspaceId":"primary","command":"pnpm test"}]}}],"riskFlags":["RF1: Unknown TypeScript errors beyond the 3 documented assertion gaps — the `opts: { body?: string }` inline type annotation in the fetchMock reply callback (Test 9, line ~349) may be rejected by strict TypeScript; if so, remove the annotation or cast to `Record<string, unknown>`. Pre-registered falsifier: `pnpm typecheck` exits non-zero pointing at admin.test.ts line ~349."],"confidence":0.87}