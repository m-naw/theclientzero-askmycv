# Sprint Plan: Deduplication and Final Verification

**Sprint ID:** ITR-1779192703496-sprint-1
**Spec:** [2026-05-19-full-v2-acceptance-test-suite-and-regression-sweep.md](docs/specs/auto/2026-05-19-full-v2-acceptance-test-suite-and-regression-sweep.md)
**Confidence:** 0.92

## Context

G8 final sprint: remove 3 duplicate acceptance test files that cause fetchMock singleton interference, port the unique spend-tracking it() block into the canonical file, then run full quality gate and update the acceptance report with the live test count.

## Risk flags

- RF1: fetchMock intercept drainage between the two it() blocks in test-kv-failure.test.ts — afterEach calls fetchMock.deactivate() which resets the pool, so inter-test contamination is not possible; beforeEach re-activates for each test. Confirmed safe by reading lines 116-131 of test-kv-failure.test.ts.
- RF2: Live test count after deduplication is unknown until pnpm test runs; the acceptance report must be updated with the exact number from that run, not an estimate. T-2 step 1 captures this.

## Task T-1: Port spend-tracking it() block into test-kv-failure.test.ts then delete 3 duplicate acceptance test files

**Type:** code (modelTier: 2)

**Files to modify:**
- src/__tests__/acceptance/test-kv-failure.test.ts (workspace: primary)

### Step 1

Run the criterion-9 pre-deletion grep to confirm test-kv-failure.test.ts does NOT independently cover the spend-failure pattern. The command to run: `grep -Pi 'spend[\s\S]{0,80}(fail|reject|throw)' src/__tests__/acceptance/test-kv-failure.test.ts`. Expected: no output (exit 1), confirming the spend it() block must be ported. If the grep returns a match, skip step 2 (port not needed) and proceed to step 3.

**File reference:** src/__tests__/acceptance/test-kv-failure.test.ts
**Verification:** grep -Pi 'spend[\s\S]{0,80}(fail|reject|throw)' src/__tests__/acceptance/test-kv-failure.test.ts; echo exit=$?

### Step 2

Port the spend-tracking failure it() block into src/__tests__/acceptance/test-kv-failure.test.ts. Insert the following it() block INSIDE the existing describe() block, immediately after the closing brace of the first it() block (before the closing `});` of the describe). Do NOT add a new fetchMock.activate() call — the describe's beforeEach already handles activation. The block to insert:

```typescript
  it("POST /chat with KV.put rejecting on spend-key write returns well-formed response (spend-tracking fail-open)", async () => {
    // Mock Anthropic upstream — intercept for this spend-tracking test
    const pool = fetchMock.get(ANTHROPIC_HOST);
    pool
      .intercept({ path: /\/v1\/messages.*/, method: "POST" })
      .reply(200, fakeAnthropicSse(), {
        headers: { "content-type": "text/event-stream" },
      });

    // Patch only put() — spend tracking calls put() after the stream completes.
    // Spend-tracking KV failure must not bubble as an unhandled exception.
    const kv = getEnv().STATE;
    const originalPut = kv.put.bind(kv);
    kv.put = ((..._args: unknown[]) => {
      return Promise.reject(new Error("KV put failed (spend-tracking outage)"));
    }) as typeof kv.put;

    let res: Response | null = null;
    let threw: unknown = null;
    try {
      res = await runFetch(chatRequest());
      if (res.body) {
        await drainStream(res);
      }
    } catch (err) {
      threw = err;
    } finally {
      kv.put = originalPut;
    }

    expect(threw).toBeNull();
    expect(res).not.toBeNull();
    expect(typeof res!.status).toBe("number");
    expect(res!.status).toBeGreaterThanOrEqual(200);
    expect(res!.status).toBeLessThan(600);
  });
```

The comment 'Spend-tracking KV failure must not bubble as an unhandled exception' satisfies the `spend.{0,80}(fail|reject|throw)` grep pattern (case-insensitive).

**File reference:** src/__tests__/acceptance/test-kv-failure.test.ts
**Verification:** grep -Pi 'spend[\s\S]{0,80}(fail|reject|throw)' src/__tests__/acceptance/test-kv-failure.test.ts | grep -q . && echo 'spend pattern found — port complete'

### Step 3

Verify criterion-9 done_when pattern passes on the current test directory BEFORE deleting files: `grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q . && echo PASS || echo FAIL`. Must print PASS.

**File reference:** src/__tests__/acceptance/test-kv-failure.test.ts
**Verification:** grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q . && echo PASS

### Step 4

Delete the 3 duplicate acceptance test files. Run these three deletion commands:
```
rm src/__tests__/acceptance/admin-404-unconfigured.test.ts
rm src/__tests__/acceptance/chat-503-not-configured.test.ts
rm src/__tests__/acceptance/kv-and-spend-failure.test.ts
```

**File reference:** src/__tests__/acceptance/
**Verification:** test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts && test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts && test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts && echo 'all 3 duplicates deleted'

### Step 5

Re-run all 4 done_when structural checks to confirm they still pass after deduplication:
1. `test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts`
2. `test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts`
3. `test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts`
4. `grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q .`
5. `grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q .`
All 5 must exit 0.

**File reference:** src/__tests__/acceptance/
**Verification:** test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts && test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts && test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts && grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q . && grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q . && echo 'ALL 5 STRUCTURAL CHECKS PASS'

### Task verification

**Structural:**
- `test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts` (ws: primary)
- `test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts` (ws: primary)
- `test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts` (ws: primary)
- `grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q .` (ws: primary)
- `grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q .` (ws: primary)

**Functional:**
- `pnpm test 2>&1 | tail -5` (ws: primary)

## Task T-2: Run full quality gate and update acceptance report with live test count

**Type:** docs (modelTier: 1)

**Depends on:** T-1

**Files to modify:**
- docs/checkpoints/v2-acceptance-report.md (workspace: primary)

### Step 1

Run pnpm test and capture the full output. Look for the summary line showing N passing / 0 failing. Command: `pnpm test 2>&1`. Record the total passing count and file count from the summary line (e.g. 'Tests  289 passed | 289 total' or similar). This is the authoritative live count.

**File reference:** docs/checkpoints/v2-acceptance-report.md
**Verification:** pnpm test 2>&1 | grep -E 'pass|fail|Tests' | tail -10

### Step 2

Run pnpm build, pnpm typecheck, and pnpm lint. Each must exit 0. Run: `pnpm build && pnpm typecheck && pnpm lint && echo 'ALL GATES GREEN'`.

**File reference:** docs/checkpoints/v2-acceptance-report.md
**Verification:** pnpm build && pnpm typecheck && pnpm lint && echo 'ALL GATES GREEN'

### Step 3

Update docs/checkpoints/v2-acceptance-report.md: replace the stale 'Full suite total' line in the 'Aggregate counts' section with the actual live test count from step 1. Also update 'Acceptance test files' count to 8 (the 3 duplicates removed = 8 remaining in src/__tests__/acceptance/). The 'Date' line may remain as 2026-05-19. Keep all other table rows and sections intact. HARD CONSTRAINT: do not modify the per-test pass/fail table — all 20 tests remain PASS.

**File reference:** docs/checkpoints/v2-acceptance-report.md
**Verification:** grep -E 'Full suite total|Acceptance test files' docs/checkpoints/v2-acceptance-report.md

### Step 4

Run the full done_when criterion 2 verification: `pnpm test && pnpm build && pnpm typecheck && pnpm lint && test -f docs/checkpoints/v2-acceptance-report.md && test -f docs/SPEC_CORRECTIONS.md && ! grep -E '\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\b' wrangler.toml && echo 'DONE_WHEN_2 PASS'`. Must print DONE_WHEN_2 PASS.

**File reference:** docs/checkpoints/v2-acceptance-report.md
**Verification:** pnpm test && pnpm build && pnpm typecheck && pnpm lint && test -f docs/checkpoints/v2-acceptance-report.md && test -f docs/SPEC_CORRECTIONS.md && ! grep -E '\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\b' wrangler.toml && echo 'DONE_WHEN_2 PASS'

### Task verification

**Structural:**
- `test -f docs/checkpoints/v2-acceptance-report.md` (ws: primary)
- `test -f docs/SPEC_CORRECTIONS.md` (ws: primary)
- `! grep -E '\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\b' wrangler.toml` (ws: primary)

**Functional:**
- `pnpm test && pnpm build && pnpm typecheck && pnpm lint` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779192703496-sprint-1",
  "sprintName": "Deduplication and Final Verification",
  "specPath": "docs/specs/auto/2026-05-19-full-v2-acceptance-test-suite-and-regression-sweep.md",
  "phaseContext": "G8 final sprint: remove 3 duplicate acceptance test files that cause fetchMock singleton interference, port the unique spend-tracking it() block into the canonical file, then run full quality gate and update the acceptance report with the live test count.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Port spend-tracking it() block into test-kv-failure.test.ts then delete 3 duplicate acceptance test files",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/__tests__/acceptance/test-kv-failure.test.ts"
          }
        ],
        "create": []
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run the criterion-9 pre-deletion grep to confirm test-kv-failure.test.ts does NOT independently cover the spend-failure pattern. The command to run: `grep -Pi 'spend[\\s\\S]{0,80}(fail|reject|throw)' src/__tests__/acceptance/test-kv-failure.test.ts`. Expected: no output (exit 1), confirming the spend it() block must be ported. If the grep returns a match, skip step 2 (port not needed) and proceed to step 3.",
          "fileReference": "src/__tests__/acceptance/test-kv-failure.test.ts",
          "verification": "grep -Pi 'spend[\\s\\S]{0,80}(fail|reject|throw)' src/__tests__/acceptance/test-kv-failure.test.ts; echo exit=$?"
        },
        {
          "stepNumber": 2,
          "instruction": "Port the spend-tracking failure it() block into src/__tests__/acceptance/test-kv-failure.test.ts. Insert the following it() block INSIDE the existing describe() block, immediately after the closing brace of the first it() block (before the closing `});` of the describe). Do NOT add a new fetchMock.activate() call — the describe's beforeEach already handles activation. The block to insert:\n\n```typescript\n  it(\"POST /chat with KV.put rejecting on spend-key write returns well-formed response (spend-tracking fail-open)\", async () => {\n    // Mock Anthropic upstream — intercept for this spend-tracking test\n    const pool = fetchMock.get(ANTHROPIC_HOST);\n    pool\n      .intercept({ path: /\\/v1\\/messages.*/, method: \"POST\" })\n      .reply(200, fakeAnthropicSse(), {\n        headers: { \"content-type\": \"text/event-stream\" },\n      });\n\n    // Patch only put() — spend tracking calls put() after the stream completes.\n    // Spend-tracking KV failure must not bubble as an unhandled exception.\n    const kv = getEnv().STATE;\n    const originalPut = kv.put.bind(kv);\n    kv.put = ((..._args: unknown[]) => {\n      return Promise.reject(new Error(\"KV put failed (spend-tracking outage)\"));\n    }) as typeof kv.put;\n\n    let res: Response | null = null;\n    let threw: unknown = null;\n    try {\n      res = await runFetch(chatRequest());\n      if (res.body) {\n        await drainStream(res);\n      }\n    } catch (err) {\n      threw = err;\n    } finally {\n      kv.put = originalPut;\n    }\n\n    expect(threw).toBeNull();\n    expect(res).not.toBeNull();\n    expect(typeof res!.status).toBe(\"number\");\n    expect(res!.status).toBeGreaterThanOrEqual(200);\n    expect(res!.status).toBeLessThan(600);\n  });\n```\n\nThe comment 'Spend-tracking KV failure must not bubble as an unhandled exception' satisfies the `spend.{0,80}(fail|reject|throw)` grep pattern (case-insensitive).",
          "fileReference": "src/__tests__/acceptance/test-kv-failure.test.ts",
          "verification": "grep -Pi 'spend[\\s\\S]{0,80}(fail|reject|throw)' src/__tests__/acceptance/test-kv-failure.test.ts | grep -q . && echo 'spend pattern found — port complete'"
        },
        {
          "stepNumber": 3,
          "instruction": "Verify criterion-9 done_when pattern passes on the current test directory BEFORE deleting files: `grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q . && echo PASS || echo FAIL`. Must print PASS.",
          "fileReference": "src/__tests__/acceptance/test-kv-failure.test.ts",
          "verification": "grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q . && echo PASS"
        },
        {
          "stepNumber": 4,
          "instruction": "Delete the 3 duplicate acceptance test files. Run these three deletion commands:\n```\nrm src/__tests__/acceptance/admin-404-unconfigured.test.ts\nrm src/__tests__/acceptance/chat-503-not-configured.test.ts\nrm src/__tests__/acceptance/kv-and-spend-failure.test.ts\n```",
          "fileReference": "src/__tests__/acceptance/",
          "verification": "test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts && test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts && test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts && echo 'all 3 duplicates deleted'"
        },
        {
          "stepNumber": 5,
          "instruction": "Re-run all 4 done_when structural checks to confirm they still pass after deduplication:\n1. `test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts`\n2. `test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts`\n3. `test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts`\n4. `grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q .`\n5. `grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q .`\nAll 5 must exit 0.",
          "fileReference": "src/__tests__/acceptance/",
          "verification": "test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts && test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts && test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts && grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q . && grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q . && echo 'ALL 5 STRUCTURAL CHECKS PASS'"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "test ! -f src/__tests__/acceptance/admin-404-unconfigured.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "test ! -f src/__tests__/acceptance/chat-503-not-configured.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "test ! -f src/__tests__/acceptance/kv-and-spend-failure.test.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -rPil 'kv.{0,40}put.{0,80}(fail|reject|throw)|spend.{0,80}(fail|reject|throw)' src/__tests__ | grep -q ."
          },
          {
            "workspaceId": "primary",
            "command": "grep -rEi 'test[-_ ]?20|forgot[-_ ]?password' src/__tests__ -l | grep -q ."
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test 2>&1 | tail -5"
          }
        ]
      }
    },
    {
      "id": "T-2",
      "title": "Run full quality gate and update acceptance report with live test count",
      "type": "docs",
      "modelTier": 1,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "docs/checkpoints/v2-acceptance-report.md"
          }
        ],
        "create": []
      },
      "dependencies": [
        "T-1"
      ],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run pnpm test and capture the full output. Look for the summary line showing N passing / 0 failing. Command: `pnpm test 2>&1`. Record the total passing count and file count from the summary line (e.g. 'Tests  289 passed | 289 total' or similar). This is the authoritative live count.",
          "fileReference": "docs/checkpoints/v2-acceptance-report.md",
          "verification": "pnpm test 2>&1 | grep -E 'pass|fail|Tests' | tail -10"
        },
        {
          "stepNumber": 2,
          "instruction": "Run pnpm build, pnpm typecheck, and pnpm lint. Each must exit 0. Run: `pnpm build && pnpm typecheck && pnpm lint && echo 'ALL GATES GREEN'`.",
          "fileReference": "docs/checkpoints/v2-acceptance-report.md",
          "verification": "pnpm build && pnpm typecheck && pnpm lint && echo 'ALL GATES GREEN'"
        },
        {
          "stepNumber": 3,
          "instruction": "Update docs/checkpoints/v2-acceptance-report.md: replace the stale 'Full suite total' line in the 'Aggregate counts' section with the actual live test count from step 1. Also update 'Acceptance test files' count to 8 (the 3 duplicates removed = 8 remaining in src/__tests__/acceptance/). The 'Date' line may remain as 2026-05-19. Keep all other table rows and sections intact. HARD CONSTRAINT: do not modify the per-test pass/fail table — all 20 tests remain PASS.",
          "fileReference": "docs/checkpoints/v2-acceptance-report.md",
          "verification": "grep -E 'Full suite total|Acceptance test files' docs/checkpoints/v2-acceptance-report.md"
        },
        {
          "stepNumber": 4,
          "instruction": "Run the full done_when criterion 2 verification: `pnpm test && pnpm build && pnpm typecheck && pnpm lint && test -f docs/checkpoints/v2-acceptance-report.md && test -f docs/SPEC_CORRECTIONS.md && ! grep -E '\\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\\b' wrangler.toml && echo 'DONE_WHEN_2 PASS'`. Must print DONE_WHEN_2 PASS.",
          "fileReference": "docs/checkpoints/v2-acceptance-report.md",
          "verification": "pnpm test && pnpm build && pnpm typecheck && pnpm lint && test -f docs/checkpoints/v2-acceptance-report.md && test -f docs/SPEC_CORRECTIONS.md && ! grep -E '\\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\\b' wrangler.toml && echo 'DONE_WHEN_2 PASS'"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "test -f docs/checkpoints/v2-acceptance-report.md"
          },
          {
            "workspaceId": "primary",
            "command": "test -f docs/SPEC_CORRECTIONS.md"
          },
          {
            "workspaceId": "primary",
            "command": "! grep -E '\\b(MOCK_|TEST_|ANTHROPIC_MOCK|MOCK_ANTHROPIC|MSW_)\\b' wrangler.toml"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test && pnpm build && pnpm typecheck && pnpm lint"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: fetchMock intercept drainage between the two it() blocks in test-kv-failure.test.ts — afterEach calls fetchMock.deactivate() which resets the pool, so inter-test contamination is not possible; beforeEach re-activates for each test. Confirmed safe by reading lines 116-131 of test-kv-failure.test.ts.",
    "RF2: Live test count after deduplication is unknown until pnpm test runs; the acceptance report must be updated with the exact number from that run, not an estimate. T-2 step 1 captures this."
  ],
  "confidence": 0.92
}
```
