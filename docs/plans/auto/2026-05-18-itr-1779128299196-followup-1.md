# Sprint Plan: Follow-up: 1 unmet criteria

**Sprint ID:** ITR-1779128299196-followup-1
**Spec:** [2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md](docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md)
**Confidence:** 0.95

## Context

G6 inline recovery pages — final fix: add missing "Cloudflare dashboard" phrase to renderExpiredSetup so error-pages.test.ts line 21 passes

## Risk flags

- RF1: Confirm only renderExpiredSetup step 1 text is changed — do not alter surrounding list structure or other steps to avoid regressions in other assertions

## Task T-1: Add 'Cloudflare dashboard' phrase to renderExpiredSetup

**Type:** code (modelTier: 2)

**Files to modify:**
- src/views/error-pages.ts (workspace: primary)

### Step 1

Read src/views/error-pages.ts and locate the renderExpiredSetup function. Find step 1 of the recovery <ol> (line 89), which currently reads: `<li>Open <a href="https://dash.cloudflare.com" rel="noopener noreferrer">dash.cloudflare.com</a> and sign in to your Cloudflare account.</li>`. Change it to include the literal phrase 'Cloudflare dashboard': `<li>Open the <a href="https://dash.cloudflare.com" rel="noopener noreferrer">Cloudflare dashboard</a> at <code>dash.cloudflare.com</code> and sign in to your account.</li>`. This ensures the rendered HTML contains both the text 'Cloudflare dashboard' (satisfying the test at src/test/views/error-pages.test.ts:21) and the URL 'dash.cloudflare.com' (satisfying the structural criterion).

**File reference:** src/views/error-pages.ts
**Verification:** grep -i 'Cloudflare dashboard' src/views/error-pages.ts && grep 'dash.cloudflare.com' src/views/error-pages.ts

### Step 2

Run pnpm test to confirm all tests pass, paying particular attention to src/test/views/error-pages.test.ts. The test at line 21 `expect(html).toMatch(/Cloudflare dashboard/i)` must now pass.

**File reference:** src/test/views/error-pages.test.ts
**Verification:** pnpm test 2>&1 | tail -5

### Task verification

**Structural:**
- `grep -i 'Cloudflare dashboard' src/views/error-pages.ts` (ws: primary)
- `grep 'dash.cloudflare.com' src/views/error-pages.ts` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779128299196-followup-1",
  "sprintName": "Follow-up: 1 unmet criteria",
  "specPath": "docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md",
  "phaseContext": "G6 inline recovery pages — final fix: add missing \"Cloudflare dashboard\" phrase to renderExpiredSetup so error-pages.test.ts line 21 passes",
  "tasks": [
    {
      "id": "T-1",
      "title": "Add 'Cloudflare dashboard' phrase to renderExpiredSetup",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/views/error-pages.ts"
          }
        ],
        "create": []
      },
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read src/views/error-pages.ts and locate the renderExpiredSetup function. Find step 1 of the recovery <ol> (line 89), which currently reads: `<li>Open <a href=\"https://dash.cloudflare.com\" rel=\"noopener noreferrer\">dash.cloudflare.com</a> and sign in to your Cloudflare account.</li>`. Change it to include the literal phrase 'Cloudflare dashboard': `<li>Open the <a href=\"https://dash.cloudflare.com\" rel=\"noopener noreferrer\">Cloudflare dashboard</a> at <code>dash.cloudflare.com</code> and sign in to your account.</li>`. This ensures the rendered HTML contains both the text 'Cloudflare dashboard' (satisfying the test at src/test/views/error-pages.test.ts:21) and the URL 'dash.cloudflare.com' (satisfying the structural criterion).",
          "fileReference": "src/views/error-pages.ts",
          "verification": "grep -i 'Cloudflare dashboard' src/views/error-pages.ts && grep 'dash.cloudflare.com' src/views/error-pages.ts"
        },
        {
          "stepNumber": 2,
          "instruction": "Run pnpm test to confirm all tests pass, paying particular attention to src/test/views/error-pages.test.ts. The test at line 21 `expect(html).toMatch(/Cloudflare dashboard/i)` must now pass.",
          "fileReference": "src/test/views/error-pages.test.ts",
          "verification": "pnpm test 2>&1 | tail -5"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep -i 'Cloudflare dashboard' src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'dash.cloudflare.com' src/views/error-pages.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: Confirm only renderExpiredSetup step 1 text is changed — do not alter surrounding list structure or other steps to avoid regressions in other assertions"
  ],
  "confidence": 0.95
}
```
