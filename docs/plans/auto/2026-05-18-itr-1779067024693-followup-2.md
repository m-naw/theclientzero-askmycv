# Sprint Plan: Follow-up: 3 unmet criteria

**Sprint ID:** ITR-1779067024693-followup-2
**Spec:** [2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md](docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md)
**Confidence:** 0.92

## Context

G3 password-primary admin auth: fixing 3 regex/test criteria that survived attempts 1 and 2. Two code changes: bcrypt constant rename (criterion 1) and hex literal removal (criterion 3/pnpm test). Criterion 2 (setup_window_start timing) is already met by commit 2d703cc.

## Risk flags

- RF1: Criterion 2 (setup_window_start timing) appears already satisfied by commit 2d703cc — src/__tests__/setup/setup-acceptance.test.ts:125 contains 'setup_window_start is recorded within 5000 ms'. Plan verifies without modifying. If for any reason the regex check fails, add an explicit comment `// setup_window_start written within 5_000` adjacent to the assertion at line 134.
- RF2: bcryptjs hash() accepts both a number (rounds) and a pre-generated salt string as its second argument. Renaming the local constant from BCRYPT_ROUNDS to saltRounds does not change the numeric value passed (12), so bcrypt behavior is unchanged. Verify with pnpm typecheck.

## Task T-1: Fix bcrypt constant name and admin-form hex colors

**Type:** code (modelTier: 2)

**Files to modify:**
- src/auth/password.ts (workspace: primary)
- src/views/admin-form.ts (workspace: primary)

### Step 1

In src/auth/password.ts line 11: rename `const BCRYPT_ROUNDS = 12;` to `const saltRounds = 12;`. Then on line 18 update the usage: `return bcrypt.hash(plaintext, saltRounds);`. The constant is file-private (not exported), so no other file references it. The name `saltRounds` satisfies the done-when regex `/(?:cost|saltRounds|rounds)\s*[:=]\s*(1[2-9]|[2-9][0-9])/` because `saltRounds = 12` matches the `saltRounds` branch followed by `= 12`.

**File reference:** src/auth/password.ts
**Verification:** grep -P '(?:cost|saltRounds|rounds)\s*[:=]\s*(1[2-9]|[2-9][0-9])' src/auth/password.ts exits 0 and shows `saltRounds = 12`

### Step 2

In src/views/admin-form.ts lines 30-31: replace both occurrences of `var(--color-error, #c0392b)` with `var(--color-error)`. The CSS variable `--color-error` is unconditionally defined in design-tokens.ts:78 (`--color-error: #c92a2a;`), so dropping the hex fallback is safe. This removes the two `#c0392b` hex literals that cause the token-discipline test to fail.

**File reference:** src/views/admin-form.ts
**Verification:** grep '#[0-9a-fA-F]' src/views/admin-form.ts returns no matches (exit 1 from grep means no hex literals present)

### Step 3

Verify that src/__tests__/setup/setup-acceptance.test.ts already satisfies the criterion by confirming the string `setup_window_start is recorded within 5000 ms` exists in the file (added in commit 2d703cc). No changes needed to this file.

**File reference:** src/__tests__/setup/setup-acceptance.test.ts
**Verification:** grep 'setup_window_start.*within' src/__tests__/setup/setup-acceptance.test.ts exits 0

### Task verification

**Structural:**
- `fs:contains-regex src/auth /(?:cost|saltRounds|rounds)\s*[:=]\s*(1[2-9]|[2-9][0-9])/` (ws: primary)
- `fs:contains-regex src/__tests__ /setup_window_start[\s\S]{0,200}(within|<=?\s*5\b|5000\s*ms|5_000)/` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)

## Task T-2: Run full verification suite

**Type:** infrastructure (modelTier: 1)

**Depends on:** T-1

### Step 1

Run `pnpm test` and capture exit code. Report pass/fail count and any failing test names. The expected result is all tests passing (exit 0) with no token-discipline violations.

**Verification:** pnpm test exits 0

### Step 2

Run `pnpm build && pnpm typecheck` to confirm no TypeScript errors from the rename of BCRYPT_ROUNDS to saltRounds.

**Verification:** pnpm build && pnpm typecheck both exit 0

### Task verification

**Structural:**
- `fs:contains-regex src/auth /(?:cost|saltRounds|rounds)\s*[:=]\s*(1[2-9]|[2-9][0-9])/` (ws: primary)
- `fs:contains-regex src/__tests__ /setup_window_start[\s\S]{0,200}(within|<=?\s*5\b|5000\s*ms|5_000)/` (ws: primary)

**Functional:**
- `pnpm test` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779067024693-followup-2",
  "sprintName": "Follow-up: 3 unmet criteria",
  "specPath": "docs/specs/auto/2026-05-18-admin-password-authentication-admin-panel-additions-setup-window-tightening.md",
  "phaseContext": "G3 password-primary admin auth: fixing 3 regex/test criteria that survived attempts 1 and 2. Two code changes: bcrypt constant rename (criterion 1) and hex literal removal (criterion 3/pnpm test). Criterion 2 (setup_window_start timing) is already met by commit 2d703cc.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Fix bcrypt constant name and admin-form hex colors",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/auth/password.ts"
          },
          {
            "workspaceId": "primary",
            "path": "src/views/admin-form.ts"
          }
        ],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "In src/auth/password.ts line 11: rename `const BCRYPT_ROUNDS = 12;` to `const saltRounds = 12;`. Then on line 18 update the usage: `return bcrypt.hash(plaintext, saltRounds);`. The constant is file-private (not exported), so no other file references it. The name `saltRounds` satisfies the done-when regex `/(?:cost|saltRounds|rounds)\\s*[:=]\\s*(1[2-9]|[2-9][0-9])/` because `saltRounds = 12` matches the `saltRounds` branch followed by `= 12`.",
          "fileReference": "src/auth/password.ts",
          "verification": "grep -P '(?:cost|saltRounds|rounds)\\s*[:=]\\s*(1[2-9]|[2-9][0-9])' src/auth/password.ts exits 0 and shows `saltRounds = 12`"
        },
        {
          "stepNumber": 2,
          "instruction": "In src/views/admin-form.ts lines 30-31: replace both occurrences of `var(--color-error, #c0392b)` with `var(--color-error)`. The CSS variable `--color-error` is unconditionally defined in design-tokens.ts:78 (`--color-error: #c92a2a;`), so dropping the hex fallback is safe. This removes the two `#c0392b` hex literals that cause the token-discipline test to fail.",
          "fileReference": "src/views/admin-form.ts",
          "verification": "grep '#[0-9a-fA-F]' src/views/admin-form.ts returns no matches (exit 1 from grep means no hex literals present)"
        },
        {
          "stepNumber": 3,
          "instruction": "Verify that src/__tests__/setup/setup-acceptance.test.ts already satisfies the criterion by confirming the string `setup_window_start is recorded within 5000 ms` exists in the file (added in commit 2d703cc). No changes needed to this file.",
          "fileReference": "src/__tests__/setup/setup-acceptance.test.ts",
          "verification": "grep 'setup_window_start.*within' src/__tests__/setup/setup-acceptance.test.ts exits 0"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "fs:contains-regex src/auth /(?:cost|saltRounds|rounds)\\s*[:=]\\s*(1[2-9]|[2-9][0-9])/"
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains-regex src/__tests__ /setup_window_start[\\s\\S]{0,200}(within|<=?\\s*5\\b|5000\\s*ms|5_000)/"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    },
    {
      "id": "T-2",
      "title": "Run full verification suite",
      "type": "infrastructure",
      "modelTier": 1,
      "files": {
        "modify": [],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [
        "T-1"
      ],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Run `pnpm test` and capture exit code. Report pass/fail count and any failing test names. The expected result is all tests passing (exit 0) with no token-discipline violations.",
          "verification": "pnpm test exits 0"
        },
        {
          "stepNumber": 2,
          "instruction": "Run `pnpm build && pnpm typecheck` to confirm no TypeScript errors from the rename of BCRYPT_ROUNDS to saltRounds.",
          "verification": "pnpm build && pnpm typecheck both exit 0"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "fs:contains-regex src/auth /(?:cost|saltRounds|rounds)\\s*[:=]\\s*(1[2-9]|[2-9][0-9])/"
          },
          {
            "workspaceId": "primary",
            "command": "fs:contains-regex src/__tests__ /setup_window_start[\\s\\S]{0,200}(within|<=?\\s*5\\b|5000\\s*ms|5_000)/"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: Criterion 2 (setup_window_start timing) appears already satisfied by commit 2d703cc — src/__tests__/setup/setup-acceptance.test.ts:125 contains 'setup_window_start is recorded within 5000 ms'. Plan verifies without modifying. If for any reason the regex check fails, add an explicit comment `// setup_window_start written within 5_000` adjacent to the assertion at line 134.",
    "RF2: bcryptjs hash() accepts both a number (rounds) and a pre-generated salt string as its second argument. Renaming the local constant from BCRYPT_ROUNDS to saltRounds does not change the numeric value passed (12), so bcrypt behavior is unchanged. Verify with pnpm typecheck."
  ],
  "confidence": 0.92
}
```
