{"sprintId":"ITR-1778857568357-sprint-2","sprintName":"Acceptance tests 7/8/9 + verification","specPath":"docs/specs/auto/2026-05-15-anti-abuse-daily-budget-cap-f6-f7.md","phaseContext":"G6 goal: anti-abuse and daily budget cap layer. Sprint-1 implemented all guards in chat.ts and created acceptance tests under src/__tests__/acceptance/. Sprint-2 creates the spec-mandated src/test/chat-abuse.test.ts with Tests 7/8/9 using the exact done_when criteria (max_msgs_per_hour=3 for Test 8, spend-reset assertion for Test 9, KV failure test), and verifies the full suite passes at >= G5 baseline + 9 new test cases.","tasks":[{"id":"T-1","title":"Create src/test/chat-abuse.test.ts with Tests 7, 8, 9 and KV failure test","type":"code","modelTier":2,"files":{"modify":[],"create":[{"workspaceId":"primary","path":"src/test/chat-abuse.test.ts"}]},"artifact_refs":[],"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Read src/routes/chat.ts (lines 78-246) to confirm the guard pipeline order: UA check → config load → rate limit → budget preflight → Anthropic call → flush with addSpend. Note that _ctx is declared but addSpend is called without ctx in flush — errors are still caught by addSpend's internal .catch(). Also read src/abuse/rate-limit.ts to confirm the KV key format: ratelimit:<ip>:YYYY-MM-DD-HH.","fileReference":"src/routes/chat.ts","verification":"Confirm isBotUserAgent is called before request.json(), checkAndIncrement is called after config load, readSpend/budget check is after rate limit, and addSpend is called inside flush() without ctx."},{"stepNumber":2,"instruction":"Create src/test/chat-abuse.test.ts. The file MUST follow the imports and setup pattern from src/test/smoke.test.ts (cloudflare:test imports, env cast, fetchMock lifecycle). Structure the file with 4 describe blocks:\n\n**describe('Test 7 — bot UA rejection'):** 3 it() blocks, one per sub-case:\n- 'rejects curl UA with 403 and zero Anthropic calls': chatRequest with UA='curl/7.68.0', expect status 403, expect callCount unchanged (0)\n- 'rejects empty string UA with 403 and zero Anthropic calls': UA='', expect 403, callCount 0\n- 'rejects python-requests UA with 403 and zero Anthropic calls': UA='python-requests/2.28.0', expect 403, callCount 0\nEach test shares a local callCount variable reset in beforeEach. Set up fetchMock intercept once per test in beforeEach with .times(5). Seed config in beforeEach.\n\n**describe('Test 8 — per-IP rate limit with max_msgs_per_hour=3'):** 3 it() blocks:\n- '3 requests from same IP succeed (200)': config.max_msgs_per_hour=3; loop 3 times sending chatRequest('10.0.1.1'), each must be 200, drain body; assert callCount===3\n- '4th request from same IP returns 429 after 3 succeed': prime the KV counter by writing '3' to the ratelimit key directly (kv.put(ratelimitKey, '3', {expirationTtl:3600})), then send 1 request, expect 429, callCount===0 this test\n- 'after rate-limit KV key is cleared, next request succeeds': prime counter to 3 via kv.put, send → 429; then kv.delete(ratelimitKey); send → 200, drain body\n\nFor Test 8, compute the ratelimit key in the test as: const today=new Date().toISOString().slice(0,10); const hour=new Date().getUTCHours().toString().padStart(2,'0'); const ratelimitKey=`ratelimit:10.0.1.1:${today}-${hour}`;\n\n**describe('Test 9 — daily budget cap'):** 2 it() blocks:\n- 'returns 503 when seeded spend equals daily_budget_usd; zero Anthropic calls': config.daily_budget_usd=1; kv.put(spendKey,'1'); POST /chat → 503; callCount===0\n- '200 after spend KV key is cleared (budget reset)': config.daily_budget_usd=1; kv.put(spendKey,'1'); first POST → 503; kv.delete(spendKey); second POST → 200, drain body; callCount===1\n\n**describe('KV failure tolerance'):** 1 it() block:\n- 'spend KV write failure does not break visitor 200 stream': import vi from vitest; seed config; spy on env.STATE.put to throw for keys starting with 'spend:' (allow ratelimit and config keys through by calling the original); POST /chat; expect status 200; consume body with res.text(); expect body to contain 'message_stop'; restore spy.\n\nFor all tests, use a browser-like User-Agent ('Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36') for legitimate requests. Use cf-connecting-ip header. Set ANTHROPIC_BASE_URL binding to the mock host. Use fetchMock.activate/disableNetConnect in beforeEach and fetchMock.deactivate in afterEach. Clear config, spend, and ratelimit keys in beforeEach/afterEach via kv.delete.","fileReference":"src/test/smoke.test.ts","verification":"File exists at src/test/chat-abuse.test.ts with 4 describe blocks and at least 9 it() blocks total."},{"stepNumber":3,"instruction":"Run the new test file in isolation to verify all tests pass before running the full suite: pnpm vitest run src/test/chat-abuse.test.ts. Check the output for pass/fail counts and any error messages. If the vi.spyOn KV failure test fails because miniflare's KV.put is not spy-able, fall back to an alternative: instead of spying, test that addSpend swallows errors by calling addSpend with a broken KV stub directly in a unit-style sub-test inside the describe block. Specifically: construct a fake KV object whose put() throws, call addSpend(fakeKv, 'spend:2026-01-01', 0.01), and assert the promise resolves (does not throw). Then verify the integration path by sending a POST /chat and asserting 200.","fileReference":"src/test/chat-abuse.test.ts","verification":"pnpm vitest run src/test/chat-abuse.test.ts exits 0 with all tests passing."},{"stepNumber":4,"instruction":"Run the full test suite to confirm no regressions and that the total passing count is >= 128 (G5 baseline 119 + 9 new from this sprint): pnpm vitest run 2>&1 | grep 'Tests'. The output should show a number >= 128 in the 'passed' field. Also confirm pnpm build and pnpm typecheck exit 0.","fileReference":"src/test/chat-abuse.test.ts","verification":"pnpm vitest run exits 0; grep 'Tests' shows >= 128 passed; pnpm build exits 0; pnpm typecheck exits 0."}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:exists src/test/chat-abuse.test.ts"},{"workspaceId":"primary","command":"fs:contains src/test/chat-abuse.test.ts \"Test 7\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-abuse.test.ts \"Test 8\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-abuse.test.ts \"Test 9\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-abuse.test.ts \"KV\""},{"workspaceId":"primary","command":"fs:contains src/test/chat-abuse.test.ts \"max_msgs_per_hour: 3\""}],"functional":[{"workspaceId":"primary","command":"pnpm vitest run src/test/chat-abuse.test.ts 2>&1 | grep -E 'Tests|passed|failed'"},{"workspaceId":"primary","command":"pnpm vitest run 2>&1 | grep 'Tests' | grep -E '[0-9]+ passed'"},{"workspaceId":"primary","command":"pnpm build 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm typecheck 2>&1 | tail -5"}]}}],"riskFlags":["RF1: vi.spyOn on miniflare KV namespace may fail if the KV object's put method is non-configurable or Proxy-based. Mitigation: fallback to a unit-style addSpend test with an inline fake KV stub (an object with a put() that throws) to verify error swallowing, combined with the integration 200 assertion which relies on the existing try/catch in flush(). This satisfies the done_when KV failure criterion without requiring spy capability.","RF2: Test 8 'exactly 3 calls' assertion depends on the mock intercept surviving 3 requests. The fetchMock intercept must be registered with .times(5) or higher to allow 3 successful requests plus the allowed post-clear request. If not set high enough, undici's MockAgent throws 'No matching mock entry' on the 3rd or 4th call, breaking the test."],"confidence":0.85}