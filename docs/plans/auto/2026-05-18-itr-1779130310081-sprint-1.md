# Sprint Plan: Inline recovery content and JSON error response

**Sprint ID:** ITR-1779130310081-sprint-1
**Spec:** [2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md](docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md)
**Confidence:** 0.82

## Context

G6 follow-up: All structural done_when criteria already met by prior sprint commits. Remaining work: replace hardcoded 600_000 in error-pages.ts with SETUP_WINDOW_MS import (per objectives), then verify pnpm build and pnpm test both exit 0.

## Risk flags

- RF1: pnpm test and pnpm build have not been run against the current workspace state — prior commits (b71529b, 9d058a6, f06d11c) implemented all structural criteria but build/test pass state is unverified. If a latent TypeScript error or test regression exists from a different goal's commits, it will block completion. Mitigation: T-1 step 4/5 diagnose and fix within the affected-area scope.

## Task T-1: Add SETUP_WINDOW_MS import to error-pages.ts and verify build/test

**Type:** code (modelTier: 2)

**Files to modify:**
- src/views/error-pages.ts (workspace: primary)

### Step 1

Read src/views/error-pages.ts and confirm: (a) line 89 contains 'dash.cloudflare.com' (done_when criterion 1), (b) renderExpiredSetup contains at least 5 <li> items (done_when criterion 2), (c) the expiry computation on line 69 uses hardcoded 600_000 instead of the SETUP_WINDOW_MS constant.

**File reference:** src/views/error-pages.ts
**Verification:** grep 'dash.cloudflare.com' src/views/error-pages.ts && grep -c '<li' src/views/error-pages.ts | awk '{exit ($1 < 5)}'

### Step 2

Add 'import { SETUP_WINDOW_MS } from "../state/machine";' at the top of src/views/error-pages.ts, immediately after the existing import { escapeHtml } from './escape' line. Then replace the hardcoded literal 600_000 (at the expiry computation line) with SETUP_WINDOW_MS. The constant is already exported from src/state/machine.ts at line 17 as 'export const SETUP_WINDOW_MS = 600_000'. Do not modify any other part of the file.

**File reference:** src/views/error-pages.ts
**Verification:** grep 'import { SETUP_WINDOW_MS }' src/views/error-pages.ts && grep 'SETUP_WINDOW_MS' src/views/error-pages.ts | grep -v import

### Step 3

Verify all six structural done_when criteria are met by running the verbatim grep commands from done_when: (1) grep 'dash.cloudflare.com' src/views/error-pages.ts — must exit 0; (2) grep -c '<li' src/views/error-pages.ts — must return >= 5; (3) grep -r 'admin_password_hash' src/views/ — must exit 0; (4) grep -r '<details' src/views/ — must exit 0; (5) grep 'recovery_summary' src/routes/setup.ts — must exit 0; (6) ! grep 'recovery_url' src/routes/setup.ts — must exit 0.

**File reference:** src/views/error-pages.ts
**Verification:** grep 'dash.cloudflare.com' src/views/error-pages.ts && grep -r 'admin_password_hash' src/views/ && grep -r '<details' src/views/ && grep 'recovery_summary' src/routes/setup.ts && ! grep 'recovery_url' src/routes/setup.ts

### Step 4

Run pnpm build. It must exit 0 with zero TypeScript errors. If it fails with 'Cannot find module ../state/machine' or similar, verify the import path is correct relative to src/views/error-pages.ts (should be '../state/machine' since machine.ts is at src/state/machine.ts).

**File reference:** src/views/error-pages.ts
**Verification:** pnpm build

### Step 5

Run pnpm test. It must exit 0 with zero test failures. Pay special attention to setup-acceptance.test.ts Test 5 (around line 374) which asserts the POST /setup expired-window gate returns 403 JSON with recovery_summary and no recovery_url. If any test fails, read the failure output and diagnose without touching files outside the affected areas (src/views/error-pages.ts, src/views/login.ts, src/routes/setup.ts, src/__tests__/setup/setup-acceptance.test.ts).

**File reference:** src/__tests__/setup/setup-acceptance.test.ts
**Verification:** pnpm test

### Task verification

**Structural:**
- `grep 'dash.cloudflare.com' src/views/error-pages.ts` (ws: primary)
- `grep -c '<li' src/views/error-pages.ts | awk '{exit ($1 < 5)}'` (ws: primary)
- `grep -r 'admin_password_hash' src/views/` (ws: primary)
- `grep -r '<details' src/views/` (ws: primary)
- `grep 'recovery_summary' src/routes/setup.ts` (ws: primary)
- `! grep 'recovery_url' src/routes/setup.ts` (ws: primary)
- `grep 'import { SETUP_WINDOW_MS }' src/views/error-pages.ts` (ws: primary)

**Functional:**
- `pnpm build` (ws: primary)
- `pnpm test` (ws: primary)

---

## Raw plan (machine-readable)

```json
{
  "sprintId": "ITR-1779130310081-sprint-1",
  "sprintName": "Inline recovery content and JSON error response",
  "specPath": "docs/specs/auto/2026-05-18-inline-recovery-pages-with-verified-cloudflare-dashboard-navigation.md",
  "phaseContext": "G6 follow-up: All structural done_when criteria already met by prior sprint commits. Remaining work: replace hardcoded 600_000 in error-pages.ts with SETUP_WINDOW_MS import (per objectives), then verify pnpm build and pnpm test both exit 0.",
  "tasks": [
    {
      "id": "T-1",
      "title": "Add SETUP_WINDOW_MS import to error-pages.ts and verify build/test",
      "type": "code",
      "modelTier": 2,
      "files": {
        "modify": [
          {
            "workspaceId": "primary",
            "path": "src/views/error-pages.ts"
          }
        ],
        "create": []
      },
      "artifact_refs": [],
      "dependencies": [],
      "steps": [
        {
          "stepNumber": 1,
          "instruction": "Read src/views/error-pages.ts and confirm: (a) line 89 contains 'dash.cloudflare.com' (done_when criterion 1), (b) renderExpiredSetup contains at least 5 <li> items (done_when criterion 2), (c) the expiry computation on line 69 uses hardcoded 600_000 instead of the SETUP_WINDOW_MS constant.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "grep 'dash.cloudflare.com' src/views/error-pages.ts && grep -c '<li' src/views/error-pages.ts | awk '{exit ($1 < 5)}'"
        },
        {
          "stepNumber": 2,
          "instruction": "Add 'import { SETUP_WINDOW_MS } from \"../state/machine\";' at the top of src/views/error-pages.ts, immediately after the existing import { escapeHtml } from './escape' line. Then replace the hardcoded literal 600_000 (at the expiry computation line) with SETUP_WINDOW_MS. The constant is already exported from src/state/machine.ts at line 17 as 'export const SETUP_WINDOW_MS = 600_000'. Do not modify any other part of the file.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "grep 'import { SETUP_WINDOW_MS }' src/views/error-pages.ts && grep 'SETUP_WINDOW_MS' src/views/error-pages.ts | grep -v import"
        },
        {
          "stepNumber": 3,
          "instruction": "Verify all six structural done_when criteria are met by running the verbatim grep commands from done_when: (1) grep 'dash.cloudflare.com' src/views/error-pages.ts — must exit 0; (2) grep -c '<li' src/views/error-pages.ts — must return >= 5; (3) grep -r 'admin_password_hash' src/views/ — must exit 0; (4) grep -r '<details' src/views/ — must exit 0; (5) grep 'recovery_summary' src/routes/setup.ts — must exit 0; (6) ! grep 'recovery_url' src/routes/setup.ts — must exit 0.",
          "fileReference": "src/views/error-pages.ts",
          "verification": "grep 'dash.cloudflare.com' src/views/error-pages.ts && grep -r 'admin_password_hash' src/views/ && grep -r '<details' src/views/ && grep 'recovery_summary' src/routes/setup.ts && ! grep 'recovery_url' src/routes/setup.ts"
        },
        {
          "stepNumber": 4,
          "instruction": "Run pnpm build. It must exit 0 with zero TypeScript errors. If it fails with 'Cannot find module ../state/machine' or similar, verify the import path is correct relative to src/views/error-pages.ts (should be '../state/machine' since machine.ts is at src/state/machine.ts).",
          "fileReference": "src/views/error-pages.ts",
          "verification": "pnpm build"
        },
        {
          "stepNumber": 5,
          "instruction": "Run pnpm test. It must exit 0 with zero test failures. Pay special attention to setup-acceptance.test.ts Test 5 (around line 374) which asserts the POST /setup expired-window gate returns 403 JSON with recovery_summary and no recovery_url. If any test fails, read the failure output and diagnose without touching files outside the affected areas (src/views/error-pages.ts, src/views/login.ts, src/routes/setup.ts, src/__tests__/setup/setup-acceptance.test.ts).",
          "fileReference": "src/__tests__/setup/setup-acceptance.test.ts",
          "verification": "pnpm test"
        }
      ],
      "verification": {
        "structural": [
          {
            "workspaceId": "primary",
            "command": "grep 'dash.cloudflare.com' src/views/error-pages.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep -c '<li' src/views/error-pages.ts | awk '{exit ($1 < 5)}'"
          },
          {
            "workspaceId": "primary",
            "command": "grep -r 'admin_password_hash' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep -r '<details' src/views/"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'recovery_summary' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "! grep 'recovery_url' src/routes/setup.ts"
          },
          {
            "workspaceId": "primary",
            "command": "grep 'import { SETUP_WINDOW_MS }' src/views/error-pages.ts"
          }
        ],
        "functional": [
          {
            "workspaceId": "primary",
            "command": "pnpm build"
          },
          {
            "workspaceId": "primary",
            "command": "pnpm test"
          }
        ]
      }
    }
  ],
  "riskFlags": [
    "RF1: pnpm test and pnpm build have not been run against the current workspace state — prior commits (b71529b, 9d058a6, f06d11c) implemented all structural criteria but build/test pass state is unverified. If a latent TypeScript error or test regression exists from a different goal's commits, it will block completion. Mitigation: T-1 step 4/5 diagnose and fix within the affected-area scope."
  ],
  "confidence": 0.82
}
```
