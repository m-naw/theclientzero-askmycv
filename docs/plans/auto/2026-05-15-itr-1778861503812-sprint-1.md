{"sprintId":"ITR-1778861503812-sprint-1","sprintName":"Admin GET route: identity gate + HTML rendering","specPath":"docs/specs/auto/2026-05-15-admin-configuration-editing-behavior-f5.md","phaseContext":"Goal G7: Wire GET /admin and POST /admin/save with strict identity verification against KV config. This sprint implements the GET /admin identity gate and HTML rendering layer. POST /admin/save follows in sprint 2.","tasks":[{"id":"T-1","title":"Create src/routes/admin.ts and update src/worker.ts","type":"code","modelTier":2,"files":{"create":[{"workspaceId":"primary","path":"src/routes/admin.ts"}],"modify":[{"workspaceId":"primary","path":"src/worker.ts"},{"workspaceId":"primary","path":"src/test/smoke.test.ts"}]},"dependencies":[],"steps":[{"stepNumber":1,"instruction":"Create src/routes/admin.ts. Export three items: (1) async function checkAdminIdentity(request, env) returning { ok: true, identity: VerifiedAccessIdentity, config: StoredConfig } or { ok: false, response: Response }; (2) export async function handleGetAdmin(request, env, _ctx); (3) export async function handlePostAdminSave(request, env, _ctx).\n\ncheckAdminIdentity logic (in order):\n- Read token = request.headers.get('cf-access-jwt-assertion'). If falsy → return { ok: false, response: 403 HTML renderAccessDenied({ reason: 'no_jwt' }) }\n- const source = await resolveJwksSource(env); let identity; try { identity = await verifyAccessJwt(token, source) } catch { return { ok: false, response: 403 HTML renderAccessDenied({ reason: 'signature_invalid' }) } }\n- const raw = await env.STATE.get('config'). If null → return { ok: false, response: 403 HTML renderAccessDenied({ reason: 'no_jwt' }) }\n- const parsed = parseStoredConfig(JSON.parse(raw)). If !parsed.ok → return { ok: false, response: 503 text 'configuration error' }\n- const config = parsed.value\n- if (identity.team_domain !== config.access_team_domain) → return { ok: false, response: 403 HTML renderAccessDenied({ reason: 'team_domain_mismatch' }) }\n- if (identity.aud !== config.access_aud) → return { ok: false, response: 403 HTML renderAccessDenied({ reason: 'aud_mismatch' }) }\n- if (identity.email !== config.access_email) → return { ok: false, response: 403 HTML renderAccessDenied({ reason: 'email_mismatch' }) }\n- return { ok: true, identity, config }\n\nhandleGetAdmin logic:\n- const result = await checkAdminIdentity(request, env). If !result.ok → return result.response\n- const { identity, config } = result\n- const html = renderAdminForm({ prefill: { display_name: config.display_name, headline: config.headline, cv_markdown: config.cv_markdown, daily_budget_usd: config.daily_budget_usd, location: config.location, linkedin_url: config.linkedin_url, github_url: config.github_url, pdf_cv_url: config.pdf_cv_url, max_msgs_per_hour: config.max_msgs_per_hour }, email: identity.email })\n- return new Response(html, { status: 200, headers: { 'content-type': 'text/html; charset=utf-8' } })\n\nhandlePostAdminSave logic (stub for this sprint — POST /admin/save belongs to sprint 2, but export it now so worker.ts can import both without a second edit):\n- const result = await checkAdminIdentity(request, env). If !result.ok → return result.response\n- Parse form body; merge editable fields (display_name, headline, cv_markdown, daily_budget_usd, location, linkedin_url, github_url, pdf_cv_url, max_msgs_per_hour) into merged config preserving existing anthropic_api_key when form field is absent or empty after trim. If anthropic_api_key non-empty: run Anthropic test call (same shape as setup.ts:131-144); on failure return 400 JSON { error: 'anthropic_api_key rejected: ...' }. On success: await env.STATE.put('config', JSON.stringify(mergedConfig)); return 200 JSON { ok: true }\n\nImports needed: Anthropic from '@anthropic-ai/sdk', verifyAccessJwt + VerifiedAccessIdentity from '../auth/access', resolveJwksSource from './jwks-source', renderAdminForm from '../views/admin-form', renderAccessDenied from '../views/error-pages', parseStoredConfig + StoredConfig from '../types/config', Env from '../env'","fileReference":"src/routes/admin.ts","verification":"grep -n 'export async function handleGetAdmin\\|export async function handlePostAdminSave\\|async function checkAdminIdentity' src/routes/admin.ts | wc -l | xargs test 3 -eq"},{"stepNumber":2,"instruction":"Update src/worker.ts:\n- Add import: import { handleGetAdmin, handlePostAdminSave } from './routes/admin';\n- Remove the entire inline async function handleAdmin (lines 21-43) — it is replaced by handleGetAdmin from the new module.\n- Remove the import of verifyAccessJwt (if it is now unused after removing handleAdmin — check that no other usage remains).\n- Change the GET /admin dispatch from `return handleAdmin(request, env, ctx)` to `return handleGetAdmin(request, env, ctx)`.\n- Add a new dispatch block directly after the GET /admin block: `if (url.pathname === '/admin/save' && request.method === 'POST') { return handlePostAdminSave(request, env, ctx); }`\n- Do NOT change any public-route blocks (GET /, POST /chat, GET /health, POST /setup).","fileReference":"src/worker.ts","verification":"grep -n 'handleGetAdmin\\|handlePostAdminSave' src/worker.ts | wc -l | xargs test 4 -eq"},{"stepNumber":3,"instruction":"Update src/test/smoke.test.ts to remove/update the two /admin tests that assert the old JSON stub behavior (lines ~103-164). These tests assert status 200 with JSON body { email: '...' }, which conflicts with the new HTML response. Replace with minimal smoke assertions that verify the new behavior:\n- The 'Worker /admin accepts a valid JWT' test: after replacing handleAdmin, a valid JWT without KV config will return 403 (no config seeded), so update to assert status 403 and content-type text/html. Alternatively, seed KV with a matching config so the full happy path works and assert status 200 + content-type text/html.\n- The 'Worker /admin returns 403 when header absent' test: this still passes (403 is correct) but content-type is now text/html; update the assertion to not call res.json() and simply assert status 403.\n\nSimplest correct update: update the valid-JWT test to seed KV with a config whose access_email/access_aud/access_team_domain match the test JWT claims, then assert status 200 and res.headers.get('content-type') contains 'text/html'. Keep the absent-header test as-is (status 403 assertion only, remove the json parse).","fileReference":"src/test/smoke.test.ts","verification":"pnpm vitest run src/test/smoke.test.ts 2>&1 | tail -5"}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:exists src/routes/admin.ts"},{"workspaceId":"primary","command":"grep -n 'handleGetAdmin' src/worker.ts"},{"workspaceId":"primary","command":"grep -n 'handlePostAdminSave' src/worker.ts"},{"workspaceId":"primary","command":"grep -n 'handleAdmin' src/worker.ts | grep -v 'handleGetAdmin\\|handlePostAdminSave' | wc -l | xargs test 0 -eq"}],"functional":[{"workspaceId":"primary","command":"pnpm build 2>&1 | tail -5"},{"workspaceId":"primary","command":"pnpm tsc --noEmit 2>&1 | tail -10"}]}},{"id":"T-2","title":"Create src/test/admin.test.ts covering all GET /admin done_when criteria","type":"code","modelTier":2,"files":{"create":[{"workspaceId":"primary","path":"src/test/admin.test.ts"}]},"dependencies":["T-1"],"steps":[{"stepNumber":1,"instruction":"Create src/test/admin.test.ts. Import from: vitest (describe, it, expect, beforeEach, afterEach), cloudflare:test (env, createExecutionContext, waitOnExecutionContext), ../worker (default), ../test-utils/jwt-harness (createJwtHarness, mintAccessJwt), ../types/config (StoredConfig).\n\nSetup: In a beforeEach, create a jwt harness using createJwtHarness(), write the JWKS document to KV under '__test_jwks' (JSON.stringify of harness.jwks), and seed a base config into KV under 'config'. The base config must have access_email='owner@example.com', access_aud='aud-abc', access_team_domain='team.cloudflareaccess.com', plus all required fields (display_name, headline, cv_markdown, anthropic_api_key, daily_budget_usd, setup_timestamp). In afterEach, delete both KV keys.\n\nHelper: async function adminGet(jwt?: string) — builds Request to 'https://example.test/admin' with GET, sets header 'cf-access-jwt-assertion' to jwt if provided, calls worker.fetch and returns Response.\n\nHelper: async function mintOwnerJwt(harness) — calls mintAccessJwt with aud='aud-abc', iss='https://team.cloudflareaccess.com', email='owner@example.com', privateKey=harness.privateKey.","fileReference":"src/test/admin.test.ts","verification":"grep -n 'describe\\|it(' src/test/admin.test.ts | wc -l | xargs test 7 -le"},{"stepNumber":2,"instruction":"Write describe block 'GET /admin — identity gate (403 cases)' with 5 tests:\n\n1. 'returns 403 HTML with reason no_jwt when header is absent': call adminGet() with no JWT; assert res.status === 403; assert res.headers.get('content-type') contains 'text/html'; assert body contains 'no_jwt'.\n\n2. 'returns 403 HTML with reason signature_invalid for a forged JWT': mint a JWT but sign it with a different private key (create a second harness, use its privateKey to sign but leave the JWKS from the first harness in KV); assert status 403 + body contains 'signature_invalid'.\n\n3. 'returns 403 HTML with reason email_mismatch when JWT email differs': mint a JWT with email='other@example.com' but all other claims matching config (aud='aud-abc', iss='https://team.cloudflareaccess.com'); assert status 403 + body contains 'email_mismatch'.\n\n4. 'returns 403 HTML with reason aud_mismatch when JWT aud differs': mint a JWT with aud='wrong-aud', email='owner@example.com', iss='https://team.cloudflareaccess.com'; assert status 403 + body contains 'aud_mismatch'.\n\n5. 'returns 403 HTML with reason team_domain_mismatch when JWT iss differs': mint a JWT with iss='https://other-team.cloudflareaccess.com', aud='aud-abc', email='owner@example.com'; assert status 403 + body contains 'team_domain_mismatch'.","fileReference":"src/test/admin.test.ts","verification":"grep -n 'no_jwt\\|signature_invalid\\|email_mismatch\\|aud_mismatch\\|team_domain_mismatch' src/test/admin.test.ts | wc -l | xargs test 5 -le"},{"stepNumber":3,"instruction":"Write describe block 'GET /admin — 200 success path' with 1 test:\n\n'returns 200 HTML with admin form pre-filled with current config values': mint a valid JWT (owner email, correct aud, correct iss); call adminGet(jwt); assert res.status === 200; assert res.headers.get('content-type') contains 'text/html'; const body = await res.text(); assert body contains the display_name value seeded in KV config; assert body contains the headline value seeded in KV config; assert body contains the cv_markdown value seeded in KV config; assert body does NOT contain 'anthropic_api_key' value (i.e., the actual key string 'sk-test-key' should not appear in HTML — verify the api key value is not embedded); assert body contains 'admin/save' (form action).","fileReference":"src/test/admin.test.ts","verification":"grep -n 'renderAdminForm\\|admin/save\\|display_name\\|cv_markdown' src/test/admin.test.ts | wc -l | xargs test 2 -le"},{"stepNumber":4,"instruction":"Write describe block 'Acceptance Test 10' matching spec §12 steps: Given an owner who completed setup, when they visit /admin with a valid CF Access JWT matching the stored identity, they see the admin form pre-filled with current config. Formally: seed config, mint matching JWT, GET /admin, assert 200 HTML containing display_name.\n\nWrite describe block 'Acceptance Test 11' matching spec §12 steps: Given a different person with a different email on the same Access team, when they visit /admin, they see the access-denied page with reason email_mismatch. Formally: seed config with access_email='owner@example.com', mint JWT with email='intruder@example.com' (same aud, same iss), GET /admin, assert 403 HTML containing 'email_mismatch'.","fileReference":"src/test/admin.test.ts","verification":"grep -n 'Acceptance Test 10\\|Acceptance Test 11' src/test/admin.test.ts | wc -l | xargs test 2 -eq"},{"stepNumber":5,"instruction":"Run the full test suite to confirm all tests pass, test count does not regress below 180 (G6 baseline), and admin-specific tests all pass.","fileReference":"src/test/admin.test.ts","verification":"pnpm vitest run 2>&1 | grep 'Tests' | grep -E '[0-9]+ passed'"}],"verification":{"structural":[{"workspaceId":"primary","command":"fs:exists src/test/admin.test.ts"},{"workspaceId":"primary","command":"grep -n 'no_jwt\\|signature_invalid\\|email_mismatch\\|aud_mismatch\\|team_domain_mismatch' src/test/admin.test.ts"}],"functional":[{"workspaceId":"primary","command":"pnpm vitest run src/test/admin.test.ts 2>&1 | tail -10"},{"workspaceId":"primary","command":"pnpm vitest run 2>&1 | grep 'Tests' | grep -E '[0-9]+ passed'"}]}}],"riskFlags":["RF1: smoke.test.ts lines 103-164 test old handleAdmin JSON behavior — after replacing with handleGetAdmin (which returns HTML), these tests will fail unless updated; plan addresses this in T-1 step 3 but the exact line numbers may drift from the baseline snapshot","RF2: Smoke test seeds no config in KV — the new handleGetAdmin reads config to perform identity comparison; the happy-path smoke test must seed KV with a matching config or it will get a 403 (no config), not a 200; executor must be aware of this when updating smoke.test.ts"],"confidence":0.92}